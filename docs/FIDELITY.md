# Fidelity — is the rebuild *faithful*, not just self-consistent?

Vinland's goal is a **faithful** rebuild of *Cultures – 8th Wonder* that can *then* be modded and
improved. "Faithful" is a different axis from "correct" in the test sense, and that difference is the
whole reason this file exists.

- **`npm test` proves self-consistency + determinism.** The pyramid (determinism, invariants — goods
  conservation, no-deadlock, path validity — golden traces) runs against the **synthetic fixture**. It
  proves the sim is reproducible and internally lawful. It does **not** prove the sim behaves like the
  original game. An agent can build a fully-green, fully-deterministic economy that plays nothing like
  *Cultures*, and no test would fire.
- **This file tracks the other axis: does each mechanic match the original?** It is a conformance
  ledger, maintained by `/iterate` (the per-step `fidelityBasis`) and `/reflect` (audits + tending).

## Why there is no automatic mechanics oracle

Fidelity is *partly* free and *mostly* not (see `docs/SOURCES.md`):

| Layer | Oracle? | How fidelity is pinned |
|---|---|---|
| **Data params** (recipes, ranges, timings, worker counts, atomic vocab) | **Yes — by construction** | Extracted from the original `.ini`/`.cif`; the pipeline verifies record counts (65 goods, 55 jobs, 105 weapons, …). Faithful as long as extraction is faithful. |
| **Assets** (`.pcx`/`.bmd` → PNG/atlas) | **Yes — OpenVikings pixel-oracle** | OpenVikings renders the originals; diff decoded output pixel-for-pixel + decoder round-trip tests. (Pending an owned game copy + the oracle.) |
| **Sim behavior** (atomic planner, economy loops, AI, pathing, combat, atomic timings) | **No** | OpenVikings' logic tick is a stub counter; the behavior is in neither the data nor the reverse-engineering. It can only be pinned by (a) the data-derived parameters, (b) the readable `.ini` *semantics*, (c) **calibration-by-observation** against the running original. Because nothing automatic catches drift here, **it must be tracked in this file.** |

## The faithful-first rule

A mechanic must match the original's behavior, pinned to one of the sources above. **Conscious
deviations are deferred and recorded — never the default.** If you intentionally diverge (a bug-fix, a
quality-of-life change, a simplification), log it under *Deviations* with the reason, so the faithful
baseline stays knowable and a future "faithful mode" / mod toggle remains possible. The content-is-data
architecture exists precisely so fidelity lives in data, not hard-coded systems.

## Conformance ledger

Status: `not-started` · `approximated` (running but not pinned to the original) · `faithful` (matches a
named source). Update the relevant row when a mechanic lands or is calibrated.

**Keep each row to one line: `status` + how it's pinned (the source file + the *one* thing that could
be wrong) + what stays `approximated` and why (the deferred pointer).** The blow-by-blow — record
counts, id enumerations, "0 dangling", the hands-on transcript — belongs in the commit message, not
the cell; a row that re-narrates its implementation has stopped being a ledger. Extend a row in place;
don't append a fresh "and now X also lands" clause each iteration.

### Pipeline / formats (Phase 0–1)
| Area | Status | Source / how pinned |
|---|---|---|
| `.cif` decrypt + container | faithful | round-trip tested; layout solved vs `XBStorable.cs`/`XBTools.cs` (SOURCES.md) |
| `.lib` / `.pcx` / `.bmd` / `.ini` decoders | faithful (structure) | round-trip tests + real-data record counts; **pixel-oracle diff still pending** |
| GUI/HUD extraction (`ls_gui_window`/`ls_gui_bubbles` bobs, `gui/palettes` LUT, `ingamegui*` strings, `.cur` cursors) | faithful (structure) | reuses the round-trip-tested `.bmd`/`.pcx`/`.cif` decoders + a new round-trip-tested `.cur` decoder (`decoders/cursor.ts`, `stages/gui.ts`); frame counts match the containers exactly (window **193** bobs, bubbles **23**, LUT 256×14); the palette↔element pairing + the `iconsleft` default-preview choice are pinned to the OpenVikings `CGuiBaseDataManager`/`CGuiManager` oracle (SOURCES.md "GUI"); strings parsed via the `[control]/[text]` `stringn`/`string` grammar (818/lang, keyed by in-game string id, CP1250-decoded); cursors decode to 32×32 with the hotspot taken from the **selected** (8-bpp) image to match the original's Win32 `LoadCursorFromFileW` best-fit on the 32-bpp display → **(1,1) for all three** (`MouseRight`'s stray (10,10) lives only on its 1-bpp fallback the engine never selects); only 8/24/32-bpp cursor DIBs are decoded (the shipped cursors always carry the selected 8-bpp entry). APPROXIMATED: the RGBA **preview** colours the whole window sheet with one palette (`iconsleft`) for human inspection only — the shipped **indexed** atlas + palette LUT preserve the real per-element colorization; the OpenVikings **pixel-oracle diff stays pending** (the *Decoded-asset pixel fidelity* row). Hands-on: `npm run pipeline` on the real game → 2 atlases (216 frames), 14-palette LUT, eng+pol 9 tables each, 3 cursors, 0 skips; the preview atlas reads as HUD chrome (papyrus panels, tool-panel border, command icons), not noise. |
| UI-font extraction (`.fnt` CFont → glyph atlas + colour LUT + metrics) | faithful (structure + layout formulas) / **baseline approximated** | A `.fnt` is a **CFont (`0x3F5`) wrapping the same CBobManager (`0x3F4`) `.bmd`** the settlers/HUD use, so `decoders/fnt.ts` reads the 16-byte CFont prefix and reuses the round-trip-tested `decodeBmd` wholesale for the glyph bobs (round-trip `encodeFnt` tested against a synthetic CFont fixture). **PINNED to the OpenVikings `CFont.cs` oracle:** the glyph lookup `bobId = char − 0x20`; the pen **advance** `spacing + rect.X + rect.W + 1` (`GetCharacterWidth`), glyph **extent** `rect.H + rect.Y + 1` (`GetCharacterHeight`), and **line height** = max extent over glyphs (`GetPixelHeight`) — the exact quantities the original computes, just over the whole glyph set. The oracle's `GetBobAreaRectanglePtr` **nulls a `Type == 0` (empty) bob** (as well as an out-of-range id), so advance/extent are **0** for an empty slot and empty bobs are skipped for line height — the port replicates this guard (an undefined CP1250 slot advances 0, and a stale rect on an empty bob can't inflate the height). `spacing` (CFont+0x10) is **not** stored (applied externally via `SetSpacing`), so decoded advances use spacing 0 — the value the fonts draw with unless the GUI overrides it. Glyph pixels are palette **indices**, so — exactly like the player/GUI colour LUTs — the pipeline emits an **indexed** atlas + a `256 × 4` **font-colour LUT** (`font_{white,dark,dimmed,red}.pcx`) and colouring is deferred to draw time. The `value0C` word empirically equals the nominal pixel size (8/10/12) — carried as an *observation*, not load-bearing (real layout comes from the per-glyph rects). **APPROXIMATED / deliberately not the literal oracle:** (a) **whitespace** — only the *width* redirect to bob `0x49` (`GetPixelWidth`) is reproduced; a space **draws nothing**. The literal `PrintCharacter`/`GetBobIdForPrint` would *blit* bob `0x49`, which in a `0x20`-based font is the `'i'` glyph — the original's own quirk that real text layout sidesteps by advancing the pen and skipping the blit. (b) the emitted **baseline** is a heuristic (a reference capital's bottom edge) — the original has **no baseline concept**, it lays glyphs out top-anchored (blit each at `pen + (offsetX, offsetY)`, advance by the advance), which reproduces the original layout exactly *without* a baseline; the baseline is a convenience for a consumer aligning mixed content. The OpenVikings **pixel-oracle diff stays pending** (the *Decoded-asset pixel fidelity* row). Hands-on: `npm run pipeline` on the real game → 12 fonts (default/latin/rus × 08/10/12/debug), the real font10 = **224 glyphs** (chars `0x20‑0xFF`), lineHeight 18, and the composited sample line "Wikingowie 0123" + the Polish range renders legibly in all four colours. |
| `map.dat` `hoix` container + `pck`/`X8el`/`X6el` packed layers | faithful (structure) | container ported from oracle `CIoHelper.cs`; the X8el inner header reverse-engineered + cross-validated across 5 real maps (69 layers, 0 mismatches, real grids `pack→unpack` byte-exact); the codec is the `.bmd` packed-line family. **X6el (`empa`/`empb` 2-byte ownership) now decoded** — identical inner header, the same RLE family over little-endian u16 elements (run = `count` copies of one u16; literal = `count` u16s); cross-validated across **all 130 real maps (260 layers, 0 mismatches, all `pack→unpack` byte-exact)**, each yielding exactly `width×height` u16 ids (id 0 = unowned). No behavioral oracle for what the ids *mean* (territory/object ownership is a Phase-5 concern) — the codec itself is pinned by the round-trip + exact cell-count |
| `lmlt` half-cell object lane → per-cell landscape-typeId grid → `content/maps/<id>.json` | approximated (semantics now pinned) | **CORRECTED (this slice):** the lane is a row-major **`2W × 2H` half-cell grid** (not per-cell corner quads — rendering the lanes as images pins the layout: half-cell row-major draws the map's island shapes cleanly, the old interleave draws two side-by-side half-res copies), and it is the **logic landscape-OBJECT lane**: the raw value **IS** the 1-based `LandscapeType.typeId` of the object standing there, raw **0 = no object**. Pinned by the `[GfxLandscape]` records' explicit `LogicType` with exact per-lane count matches on a probed real map (clay-mine decals raw 12 = `mud_mine` ×3605 = the map's clay `emla` decals ×3605; iron 18 ×2083 = ×2083; gold 21 ×1390 = ×1390; palms → 4 `tree`, wave fx → 1 `void`). **The old `+1` shift was a real bug** (it mapped tree→`tree_falling`, mud_mine→`mud_ore` — it "verified" only because every value+1 also exists in the 87-type table); raw values now pass through unshifted, `0` → `void`. What stays APPROXIMATED: the 2×2-block dominant-value reduction (lowest-value tie-break) — a deterministic bulk choice for the cell-graph input with no behavioral oracle. Hands-on: all 130 real maps re-emitted; the probe map's typeId histogram is semantically sane (void 60977, tree 253, bushes, mud/iron/gold/mushroom mines, rock). |
| Terrain **ground-graphics tables** (`pattern.cif` → `GfxPattern`, `trianglepatterntypes.cif` → `TrianglePatternType`) | faithful (params) | extracted verbatim from the `.cif`-only `Data/engine2d/inis/patterns/pattern.cif` (CamelCase keys/header, decoded via `decodeCifStringArray`→`cifLinesToSections` like `extractLandscapeGraphics`) and `Data/logic/trianglepatterntypes.cif`. `GfxPattern` (the **texture→cell binding**) maps 1:1 to `EditName` / `EditGroups` (multi-value, kept verbatim) / `LogicType` / `GfxTexture` (normalized path) / `GfxCoordsA`,`GfxCoordsB` (6-int UV tuples); it carries **no explicit id**, so `id` is the **0-based position** in the list — the extractor keeps **every** record (skipping one would renumber the rest), reading a wrong-arity coord line to `undefined` rather than aborting the batch. `TrianglePatternType` (the logic classification, the cross-ref target of `GfxPattern.logicType`) maps 1:1 to `type` / `debugname` / the 0-1 flags → booleans (`iswater` / `humancanwalkon` / `housecanbebuildon` / `biocangrowon` / `biocanplanton` / `island`) / `moveresistance` / `debugcolor` (RGB tuple), absent-flag = false. DOC-CORRECTION: `trianglepatterntypes.cif` is **10 records** (type ids 1..10: water/land/blocked/mountain/sand/beach/desertstone/moor/snow/plaster), not 82 — "82" was the `.cif` *string* count (10 headers + 72 property lines), now fixed in SOURCES.md. **Now consumed twice**: the approximated typeId→pattern map for synthetic grids (`buildTerrainPatterns`), and — since the map-import slice — the FULL table is emitted as `gfxPatterns` IR and joined **by `EditName`** from a decoded map's `ground.patterns` (the 1:1 per-triangle render; the names are unique across all 927 records). Hands-on: the real extractors over the real game `.cif` → **927 `GfxPattern`** (ids contiguous 0..926, 0 missing texture, 0 wrong-arity coords, 56 distinct textures, logicType ∈ {0..10}) + **10 `TrianglePatternType`**, and the cross-ref is **100% sound** — every `logicType ≠ 0` resolves to a `trianglepatterntype.type` (0 unresolved; `logicType 0` = the misc/border tiles that classify to no logic type). |
| Terrain **ground-texture render** — decoded maps (`ground` lanes → `buildGroundTerrain`) | **faithful (1:1 pattern choice)** / approximated (projection) | **the "oracle-blocked pattern algorithm" is moot — the map stores the ANSWER.** The `empa`/`empb` u16 lanes carry the final per-triangle `GfxPattern` pick, referenced through the map's own `eapd` name dictionary (927 names mirroring `pattern.cif` — the engine's version-robust by-NAME join); the pipeline emits them as `maps/<id>.json` `ground` (compact per-map name list + per-cell A/B indices) and `WorldRenderer.buildGroundTerrain` replays them verbatim — coastlines/`block *` transition tiles join up exactly as authored. The UV convention is pinned across all 927 records (`coordsA` = tile (TL,BR,BL), `coordsB` = (TL,TR,BR)), so triangle A/B land on the iso diamond's left/right halves (`terrain.ts`, unit-tested). What stays APPROXIMATED: our square-diamond iso **projection** itself — the original is a **triangle mesh with hexagonal (6-way) node adjacency** (confirmed from the shipped Funatics header `Data/GameSourceIncludes/logicdefines.inc` `THexagonDirection`/`TMapTrianglePatternTypes`), which we approximate as a 4-connected square-diamond grid; the exact vertex layout + the half-texel sampling stay unpinned. **Pitch is a HUMAN-GATED knob (2026-07), default `32×16`.** `TILE_HALF_W/H` was `32×16` "tuned to the art" by eye and is the master sprite-vs-terrain scale. The footprint-vs-sprite calibration (a placed object's `LogicWalkBlockArea` in CELLS vs its bob in PIXELS, `bobW/(fpW+fpH)`) put flat objects at ~17–21 px/cell and a first pass took that literally (`20×10`) — but observation showed it OVERSHOT: the world went too zoomed-in (nature too big, too little free space, water tiles visibly repeating), because the bridge's collision footprint (20×12, a wide walk area, not the visible deck) skewed the estimate low. Reverted to `32×16`. The separate "buildings too small" report is fixed independently by dropping the `0.7` building fudge (buildings draw NATIVE now, `BUILDING_SCALE=1`), NOT by the pitch. Because the pitch IS the whole look and an agent can't self-judge pixels, it's now LIVE-TUNABLE: the live entry reads `?pitch=<fullTileWidth>` → `setTilePitch(w/2, w/4)` (kept iso 2:1), so a human sweeps it and reports the value. If nature reads right but buildings/bridge don't at the same pitch, that's a per-CLASS scale need (a single pitch can't satisfy both), the next lever. Hands-on: `?map=arabskie_wyspy_wolna_gra_mieszana` and `?map=kraina_starych_bohatero_w` draw seamless sand/meadow/rock/shallow-water ground with correct transition blocks (`?objects=off` shows the bare ground tiling with no seams); `?map=specjalna_mosty_na_rzece&pitch=72` etc. sweeps the scale live. Water ground now renders (the earlier "water never renders" gap is closed — sea cells are `block water shallow` patterns). |
| Terrain ground — SYNTHETIC grids (`terrainPatterns` typeId→family map) | approximated (deviation) | a synthetic grid (scenes, the strip) has no `ground` lanes, so it keeps the coarse family approximation: `buildTerrainPatterns` classifies each landscape typeId by NAME (`water`/`mountain`/`land`) onto ONE representative `GfxPattern` — uniform-per-family ground. Unchanged this slice; decoded maps no longer use it. |
| **Landscape-object layer** (`emla`+`eald` → `objects` → `setMapObjects`) | faithful (placement + graphics join) / approximated (state pick, phase, wave blend) | every pre-placed map object (trees, stones, bushes, mine decals, palisades, bridges, chests, the sea's wave fx) decodes from the `emla` half-cell lane through the map's `eald` name dictionary onto the FULL extracted `[GfxLandscape]` table (`landscapeGfx` IR, 866 records — `extractLandscapeGfx`: logic type/valency/workability + walk/build/work footprints + bob libs/palette + per-state `GfxFrames` + `GfxStatic`/`GfxLoopAnimation`/`GfxDynamicBackground`), and draws via the `(bmd, palette)` atlases: flat decor (no `LogicWalkBlockArea`) batches into per-block meshes under the entity sprites, tall objects (trees/stones) depth-sort against settlers by feet anchor, loop-animated records (waves, swaying trees) play their frame list at the sim tick rate. PINNED: placement half-cell coords + the by-`EditName` join (0 dangling on the probe map; eald names unique across all 866). APPROXIMATED: (a) the drawn **state** is the record's first `GfxFrames` list (full-grown) — per-object growth state is not read from the map yet; (b) loop **phase** is a deterministic spatial GRADIENT (`hx + hy`, not a uniform 0) — adjacent half-cells differ by ~one frame so the sheet stays continuous, but the sea no longer pulses as one identical stamp (the "water repeated too many times / unnatural" report); the map stores no per-object phase, so the gradient is our choice; (c) `GfxDynamicBackground` (exactly the 8 wave records) is read as a translucent blit at **alpha 0.5** — the original's exact blend is unpinned (`PrintBob_UsingTransparency` exists in the oracle but its factor is not extracted); (d) the animation **cadence** (one frame per sim tick) is a render-only choice; (e) objects are render-only — no sim entity, no collision (the extracted block-area footprints are the future collision input); (f) the `fx wave*`/`fx *` records point at `test_effect.bmd` with no palette (engine-fx placeholders) and stay undrawn (24085 of 65953 placements on the probe map — the plain `wave *` records cover the sea). Hands-on: both maps above draw forests/palisades/bridges/mines; waves animate over the water ground. |
| **Sound bank** (`soundfx.cif` → `SoundBank`) | faithful (params) | extracted verbatim from the `.cif`-only `Data/engine2d/inis/soundfx/soundfx.cif` via `decodeCifStringArray`→`cifLinesToSections`→`extractSounds` (case-insensitive — the file mixes `SFX`/`sfx`, `SoundFXAmbient`/`SoundFxAmbient`). Three record kinds map 1:1: `SoundFXStatic` groups → `staticGroups` (`Name`, optional `LogicSoundType` engine trigger, one-or-more `SFX "<path>" <vol>`), `SoundFXAmbient` terrain beds → `ambient` (`Name`, `PatternGroup`/`LandscapeGroup` keys, `SFX` + probability params), `SoundFXJingle` life-event stingers → `jingles` (`MusicType`, `SFX`). Wav paths are normalized relative to the sounds root (`Data\Engine2D\Bin\Sounds\Gui\Click_Confirm.wav` → `gui/click_confirm.wav`). Hands-on: real `soundfx.cif` → **225 static (118 with `LogicSoundType`) / 15 ambient / 9 jingles**, all 731 referenced wavs present on disk (0 missing). The `.wav`s are 16-bit mono 22050 Hz PCM (browser-native — no transcode); audio *quality* fidelity is a human ear check (Assets row). |
| Data extraction (goods/jobs/tribes/weapons/buildings/atomics) | faithful (params) | extracted from readable `.ini`; counts verified; cross-refs resolve |
| Goods-graph input side (`productionInputs`) | faithful (params) | extracted verbatim from `Data/logic/goodtypes.ini` `productionInputGoods` (the base `.ini`; the mod ships no `goodtypes` twin). A repeated good id encodes the amount, collapsed to `{goodType, amount}` (e.g. `tile <- 2x mud, 1x wood`). Hands-on: 42/65 goods carry inputs, 0 dangling refs (cross-ref-checked). The **output-side join** into building `recipe`s **has now landed** — see the *Production* row. |
| Job-experience tracks (`humanjobexperiencetypes` → `HumanJobExperienceType`) | faithful (params) | extracted verbatim from `Data/logic/humanjobexperiencetypes.ini` (the base `.ini`; no mod twin) — each `[humanjobexperiencetype]` maps 1:1 to its source keys (`type`/`name`/`job`/`good`/`experiencefactor`/`baserepeatcounter`), no interpretation. A track is a `(job, good?)` specialization (44/70 good-specific, 26 "general"). The numeric semantics (how `experienceFactor`/`baseRepeatCounter` shape the XP curve) are **captured raw, deferred** — interpreting them is the Phase-3 ProgressionSystem's concern, not this extraction. Hands-on: `npm run pipeline` on the real game → **70 tracks** (44 with a good, 3 with a baseRepeatCounter, 26 distinct jobs, experienceFactor 1..250), all `job`/`good` refs cross-ref-resolving (0 dangling). |
| Job→{good,house,job,vehicle} tech-graph edges (`tribetypes` `jobEnables*` → `TribeType.jobEnables`) | faithful (params) | extracted verbatim from the mod's readable `DataCnmd/tribetypes12/tribetypes.ini` (golden rule #4; the base `tribetypes.cif` is the encrypted twin). The four source keys `jobEnablesGood`/`jobEnablesHouse`/`jobEnablesJob`/`jobEnablesVehicle <jobType> <targetId>` unify into one `{jobType, kind, targetId}` edge discriminated by `kind` — a settler of `jobType` present in the tribe *unlocks* the target. Edges are kept in **exact source file order** (the real data interleaves the four kinds within a job's block, so a single file-order pass keeps them verbatim rather than regrouping); repeats kept verbatim. This is the **gate** half of progression (availability gated on a job); how a job is in turn gated by training/XP (`trainforjob`/`needfor*`) is a later slice, and **no sim yet consumes these edges** (data-only this iteration). Cross-refs: **all four kinds** are now resolved by `validateCrossReferences` — good/house(building)/job into their tables, and the **vehicle** kind into the `vehicletypes` `type` (`logicvehicletype`) namespace now that `VehicleType.typeId` is extracted (the *Vehicle type table* row). The vehicle namespace is **distinct** from the building namespace, so an empty buildings list doesn't mask a dangling vehicle edge. Hands-on: `npm run pipeline` on the real game → **1325 edges across the 5 playable tribes** (285 good / 880 house / 110 job / 50 vehicle; animals carry none), **all four kinds resolving with 0 dangling** — the 50 vehicle edges' distinct target ids `{1,2,3,4,5}` are a subset of the 6 vehicle typeIds `{1..6}`. |
| Job/good **experience requirements** (`tribetypes` `{need,train}for{job,good}` → `TribeType.jobRequirements`) | faithful (params) | extracted verbatim from the mod's readable `DataCnmd/tribetypes12/tribetypes.ini` (golden rule #4; the base `tribetypes.cif` is the encrypted twin) — the **threshold** half of progression, sitting under the `jobEnables*` *who-unlocks-it* gate (row above). The four source keys `{need,train}for{job,good} <targetId> <amount> <expType> [expType2]` unify into one `{requirement, target, targetId, amount, experienceTypes[]}` record on two orthogonal dimensions: `requirement` = `need` (`needfor*` — the XP a settler must already have accrued to unlock the target) vs `train` (`trainfor*` — the schooling cost paid at a training house), `target` = `job` (`*forjob`) vs `good` (`*forgood`). Records kept in **exact source order** (the data interleaves `need`/`train` blocks), repeats verbatim, malformed lines (non-int target/amount) skipped — the `jobEnables`/`setatomic` stance. **No sim consumes these yet** (data-only this iteration, like `jobEnables` was). Cross-refs: `targetId` resolves against the job/good table by `validateCrossReferences`; the **`experienceTypes` are NOT checked** — they span an id space wider than the 70-entry `humanjobexperiencetypes` table (observed `need` ids 72/73/75 and the synthetic "school" markers 57/77 on `train`), so resolving them would false-positive (the `experienceTypes` are the one cross-ref still deliberately left unchecked — every `jobEnables` kind, vehicle included, now resolves). DEVIATION-FROM-DOC: the mod's `tribetypes - info.txt` says `trainfor*`'s school expType is "always 77", but the real data also uses 57 (30 of 270 train lines) — captured the actual value, not the documented constant. Hands-on: `npm run pipeline` on the real game → **575 requirements across the 5 playable tribes** (145 needforjob / 115 trainforjob / 160 needforgood / 155 trainforgood; animals carry none), 20 lines with two expTypes, all 575 `targetId` refs resolving (0 dangling); e.g. viking `needforjob 19 10 45` → `{need, job, targetId:19, amount:10, experienceTypes:[45]}`. |
| Vehicle type table (`vehicletypes` → `VehicleType`, incl. carry capacity + cargo allow-list) | faithful (params) | extracted verbatim from the base `Data/logic/vehicletypes.ini` (plain `.ini` like `goodtypes`/`landscapetypes`; the mod ships no `vehicletypes` twin). Each `[vehicletype]` maps 1:1 to its source keys: `type`/`name` plus the three numeric fields a vehicle is — `stockslots` → `stockSlots` (the **carry capacity**: handcart 15, oxcart 30, ship small 50, ship big 200, catapult 0), `passengerslots` → `passengerSlots`, `logicsize` → `logicSize` — **and now the `logicgood` cargo allow-list** → `cargoGoods` (the goodtype ids a vehicle's hold may carry, read via `getIntList` since each `logicgood N` is a repeated single-value line; the catapult lists none). This is the **WHAT a boat-as-mobile-store can hold** filter the Sea/Northland slice gates loading on — distinct from `stockSlots` (how *much*). The `logicpassenger` board-list, slot/vector graphics, draft-animal and `debug*` extras are still **deferred** (the embark + graphics slices, not this type-table extract). **The sim now consumes `stockSlots`** — `carrierCarryCapacity` (the *Carrier* row below) sizes a carrier's haul batch by the largest `stockSlots` among the tribe's unlocked vehicles (falling back to 1 on foot), the first `vehicle`-edge consumer — and a `cargoGoods` read side (`vehicleCargoGoods`/`vehicleMayCarry`, `systems/readviews/vehicles.ts`) exposes the per-hold load gate. This table also **resolves the `jobEnablesVehicle` edges** (the `vehicle` kind, row above): `validateCrossReferences` keys those edges into `VehicleType.typeId`, and the real data's 50 vehicle edges (ids `{1..5}`) all land within the 6-entry table. Hands-on: `npm run pipeline` on the real game → **6 vehicles**, stockSlots `{15,30,30,50,200,0}` exactly matching the `.ini`, and `cargoGoods` length **49 for every hauler (both ships + 3 carts, first ids `[16,17,43,1,2]`) / 0 for the catapult** — matching the per-section `logicgood` line counts exactly (no silent drop), each stamped `source.file = Data/logic/vehicletypes.ini`. |
| Armor + weapon type tables (`armortypes`/`weapontypes` → `ArmorType`/`WeaponType`) | faithful (params) | extracted verbatim from the base `.ini`s (no mod twin); keys map 1:1 (case-sensitive — mixed casing: camelCase `mainType`, lowercase `munitiontype`/`damagetype`). `ArmorType.typeId` is the **armor class** a weapon's `damage[class]` keys against; class **0** = unarmored (no record). 4 armor records (woolen/leather/chain/plate, classes 1..4, blockingValue 5). **Every extracted field on both tables now has a sim read view** (`systems/readviews/combat.ts`: class/material/weight groupings + accessors); a weapon's `goodtype 0` is the natural-weapon (fist/claw) sentinel → `undefined`, and `goodType` cross-refs resolve. The damage table is now keyed by the victim's armor **`materialType`** (== `typeId` for the 4 base armors — the *Combat damage read side* row); the weapon `damagevalue` columns **6/7** are the vs-tree/wall and vs-**building** damage (`damageVsWood`/`damageVsBuilding`), NOT armor tiers, so they are surfaced as read views rather than mis-treated as unarmored. The combat BEHAVIOUR that consumes these (loadout, equip drive) stays oracle-blocked/deferred. |
| Animal type table (`animaltypes` → `AnimalType`) | faithful (params) | extracted verbatim from base `Data/logic/animaltypes.ini` (no mod twin), **keyed on `tribetype`** not `type` (an animal's identity IS its tribe; a record missing the key — a disabled stub — is dropped, not thrown). All flags (`aggressive`/`getangry`/`catchable`/`warrantable`/`cannotbeattacked`/`ignorehouses`/…) + magnitudes (`hitpoints_adult`/`_baby`, group sizes, `movespeed`/`runspeed`) map 1:1, and **every field now has a sim read view** — the aggression/anger/hitpoints ones drive combat (`isAggressiveAnimal`/`angryGameTimeOf`/`animalHitpoints`, the `Anger` timer); the rest seed oracle-blocked/deferred behaviours (herding, animal pathing). `hitpoints_baby` is read independent of adult (they diverge). 35 animals, all `tribeType`s resolve (0 dangling); kept as an array (duplicate `tribetype 23` preserved), not a Map. |
| Combat **damage read side** (`combatDamage`) | faithful (params) | a pure content-derived view (`systems/readviews/combat.ts`): one `CombatProfile` per weapon, a `CombatDamageRow` per armor **material** with `damage = weapon.damagevalue[material]` — the value the original **pre-resolves per material** (`logicdefines.inc ARMOR_MATERIAL_TYPE`: 0 unarmored, 1 wool, 2 leather, 3 chain, 4 plate). **Armor works by COLUMN SELECTION, not subtraction:** the per-material value IS the resolved damage, so **no `blockingValue` is applied** (that uniform 5 has an UNKNOWN engine role — row below — and is deliberately NOT subtracted). The materials covered are the union of unarmored 0 + each `[armortype]`'s `materialType` (== `typeId` for the 4 base armors). The **structure columns** `WOOD` (6) / `HOUSE` (7) are NOT armor rows — they are the vs-tree/wall and vs-**building** damage, surfaced as `damageVsWood`/`damageVsBuilding` read views (the towers/siege step's input), no longer mis-treated as unarmored fallbacks. `weaponDamageVsMaterial` is the single-column join shared with the CombatSystem's per-hit resolution, so the table and the live hit can't drift. Returned as an **array** (105 profiles), not a Map — no weapon key is unique (animal weapons reuse `(tribeType,typeId)`). |
| **Armor `blockingValue` engine role** (uniform `5` on every base `[armortype]`) | unknown → recorded, deliberately unapplied | Every base armor record carries `blockingValue 5` (woolen/leather/chain/plate alike). The original's combat resolves damage by the weapon's per-material `damagevalue` **column** (the *Combat damage read side* row), which already encodes the armor's protection per material — so **subtracting `blockingValue` on top would double-count**, and the readable data gives no formula that uses it. Its true engine role (a separate mitigation pass? a block-chance? a shield-only value?) is **not readable** — OpenVikings' sim is a stub, and no `.ini` documents it. So the sim **extracts it faithfully but does not apply it** (the earlier `max(0, damage − blockingValue)` join was dropped in the material-column re-pin). Recorded here as a KNOWN UNKNOWN; if calibration-by-observation later reveals its role, it lands as its own mechanic. |
| Goods-graph **node layers** (`GoodClassification`) | faithful (params) | the raw-vs-produced-vs-input layering is read verbatim from each `[goodtype]`'s boolean flags — `isProducedOnMapFlag` → `producedOnMap` (raw/map-gathered), `isProducedInHouseFlag` → `producedInHouse` (workplace output), `isInputGoodFlag` → `inputGood` (recipe-consumable). Three **independent** booleans, not a mutually-exclusive enum: the source itself sets several at once (e.g. `leather` carries all three — gathered from animals *and* house-produced *and* an input). Hands-on on the real game: 65 goods → 16 raw / 48 in-house / 17 input; `food_simple`/`food_extra` correctly in-house terminal, `wheat`/`stone`/`wood` raw+input, `flour` in-house+input (the intermediate tier). These layers + the `productionInputs` edges are the explicit goods-graph IR. |
| Gathering pipeline (`goodtypes` `landscapeTo*` + `landscapetypes` lifecycle → `GoodType.gathering`/`.landscapeType`, `LandscapeType.name`/`.transitions`, resolved `gatheringPipeline`) | faithful (params) | extracted verbatim from the base plaintext `Data/logic/goodtypes.ini` + `Data/logic/landscapetypes.ini` (the mod ships **no** overriding logic twin). A raw good's three-stage chain `landscapeToHarvest`/`Pickup`/`Store` + `isBioLandscapeFlag` → `GoodType.gathering`; the universal `landscapetype` (on-ground lane, present on all 65 goods) → `GoodType.landscapeType`; each `[landscapetype]`'s `name` + raw `transition` tuples → `LandscapeType`. `buildGatheringPipeline` materializes the good→landscape→gfx join once as `gatheringPipeline` (stage id → the `LandscapeGfx.index` values whose `logicType` matches — the `[GfxLandscape].LogicType` cross-ref, the houses' `[GfxHouse].LogicType` analog). **DEFERRED (raw, not decoded):** the `transition` tuple **field-semantics** are unknown — captured VERBATIM (variable arity: mostly 5 ints, the 4 `mine` types 2) and **no meaning is read into the positions**; interpreting them is a future landscape-lifecycle slice, tracked here (the one known-unknown of this extract). No sim consumes any of it yet (data-only). Hands-on (`npm run pipeline` on the real game): **11 gathering pipelines** — wood `tree(4) → trunk(6) → wood(7)` atomic 24 bio, stone `15→16→17` a25, mud `12→13→14` a26, iron `18→19→20` a27, gold `21→22→23` a28, wheat `27→28→29` a29, herb `33→34→35` a31, mushroom `36→36→37` a32, leather `79→79→25` a33, meat `80→80→44` a33, honey `(no harvest)→32→32`; **all 28 stage ids resolve to a defined `[landscapetype]` and every stage resolves to ≥1 `[GfxLandscape]` record (0 empty joins)**; 65/65 goods carry `landscapeType`, 11 a `gathering` chain; 85/87 landscape records carry ≥1 raw transition (cross-refs 0 dangling). |
| Decoded-asset **pixel** fidelity | not-verified | OpenVikings pixel-diff not yet run (an agent can't self-judge; needs human + owned copy) |

### Simulation mechanics (Phase 2+)
Two axes are pinned independently per mechanic: its **structure/parameters** (often data-pinned) and
its **behavior** (the planner/loop shape, which has no oracle — see the table above). A row is only
`faithful` when *both* are pinned to a named source; a faithful parameter under an unpinned behavior
is still `approximated` overall, with the basis spelled out.

| Mechanic | Status | Source / how pinned |
|---|---|---|
| Terrain cell-graph + walkability/valency | faithful (params) | `landscapetypes.ini` `walkable` + `maximumValency`; the cell graph is the engine's nav model, not the triangle render mesh (docs/ECS.md) — 8-connected for movement (see the *A\* pathfinding* row), while `neighbours`/`walkableNeighbours` stay 4-connected for placement/valency adjacency. **The placement-LAYER flags now also have a sim consumer** (`systems/readviews/landscape.ts`) — the **full triple**: `landLayerLandscape`/`isLandLayerType` reads `allowedonland`, `waterLayerLandscape`/`isWaterLayerType` reads `allowedonwater`, and `universalLayerLandscape`/`isUniversalLayerType` reads `allowedoneverything` (the terrain graph reads only `walkable`/`maxValency`; `walkable`/`buildable` themselves keep schema defaults, NOT these flags — see `tools/asset-pipeline/src/decoders/ini.ts`). Adds no mechanic — a derived classification over the already-extracted IR. Hands-on (real game IR, 87 landscape types): `landLayerLandscape` = the **86** land types (every row but the layer-agnostic `void`), `waterLayerLandscape` = exactly the 3 wall/gate structures (`wall`/`wall_gate_closed`/`wall_gate_open`, typeIds 82/83/84), `universalLayerLandscape` = exactly `void` (typeId 1) — land(86)+universal(1) partition the 87 rows exactly, matching the source `allowedon{land,water,everything}` lines. This **closes the landscape placement-layer consumer coverage** (every extracted `allowedon*` flag now has a sim read view). It is the placement-side type-table half — distinct from water-VALENCY *cell* terrain (which cells are water; map-decode-blocked, row 44). |
| Uniform per-step walk cost | faithful | `landscapetypes.ini` carries **no** per-type movement weight (only valency + placement flags — verified, see LESSONS [4ef956f]); movement is gated by walkability/valency, so uniform cost is the faithful model, not a placeholder. |
| A\* pathfinding (8-connected, octile cost, canonical tie-break, per-tick budget) | approximated | Behavior has no oracle (OpenVikings' logic tick is a stub). The search is now **8-connected** (`steps()` emits orthogonal N/E/S/W + diagonal NE/SE/SW/NW), diagonals cost **√2** (`SQRT2`, minted via `fx.isqrt`) under the **octile** heuristic (`cellOctileDistance`), with a strict **no-corner-cut** rule (a diagonal is legal only when both shared orthogonal cells are passable — a unit never clips a wall/building corner). A settler therefore walks **straight at its target** instead of the axis-aligned staircase the old 4-connected search produced (the "walks in an arc" report was that staircase skewed by the iso projection), matching the original's visibly-8-directional humans. All of it — 8-connectivity, the √2 cost, the corner policy, the canonical tie-break — is *our* deterministic choice; the engine's actual pather/path-cache/diagonal policy is unknown, so whether the real engine is 8-connected is unpinned. `PATHFINDING_BUDGET_PER_TICK`=8 is unpinned (calibration-by-observation pending). |
| Movement step speed | approximated | `MOVE_SPEED_PER_TICK`=¼ tile/tick is an unpinned constant — `atomicanimations` carries `startdirection`/`length` but no traverse speed found yet (the only readable `movespeed` keys are `animaltypes.ini`'s, humans have none); calibration-by-observation pending. **Recalibrated by observation (2026-07): ¼ → ⅛ tile/tick.** The reviewer read ¼ as clearly too fast against the original, and the walk skated (the 12-frame leg cycle at 1 frame/tick spanned 3 tiles per cycle; at ⅛ it spans 1.5 — much closer to feet planting on ground). Golden fallout was the pace-only signature: the atomic TRACE shifted to later ticks with the SAME actions in the same order (all 8 planks still produced) and the FINAL settled state hash was byte-identical (469da255) — behaviour unchanged, clock slower. Still calibration-by-observation (no readable human `movespeed`); a finer pass (e.g. ONE/12 for a zero-skate cycle) stays open. |
| Atomic durations (harvest/pileup/pickup) | faithful (params) | duration = the tribe's `setatomic (jobType,atomicId)→animation` binding → `atomicanimations.ini` `length` (`atomicDuration`). `DEFAULT_ATOMIC_DURATION`=4 only when the chain is absent (unpinned fallback). **The remaining extracted `AtomicAnimation` fields now all have a sim read view too** (`systems/readviews/animations.ts`): `atomicAnimationByName` (the canonical name→record resolver the duration/cadence lookups spelled out inline), `isInterruptibleAtomic`/`atomicStartDirection` (the `interruptable`/`startdirection` scalars, the deferred preempt/facing drives' seed), and **`atomicEventChannelDelta`** — the **net signed delta** an animation contributes to one `event <at> <type> <value>` channel (the sum of `value` over events of that `type`; `ATOMIC_EVENT_CHANNEL` names the four need bars `REST=1`/`HUNGER=2`/`LEISURE=3`/`PIETY=4`). This turns the channel-restore magnitudes the needs/eat/sleep/pray/enjoy rows below assert only in *prose* (needs.ts/ai.ts/atomic.ts comments) into a single data-pinned lookup read straight off the extracted `events` array — pinned to the mod's readable `atomicanimations.ini` `event <at> <type> <value>` semantics (golden rule #4). A value-less cue event (`event <at> <type>` with no magnitude) contributes 0; an unknown name returns 0. The summed delta deliberately collapses both event streams, so the last *per-event* field — `AtomicEvent.extended` (the `eventx` vs `event` source key) — now gets its own read side too: **`atomicHasExtendedEvents`** (does the animation carry any `eventx` line?). In the real data the `eventx` stream brackets a *production* run (`eventx 0 22` / `eventx 99 23` start/end markers) and carries the worker's own need-drains while labouring (`eventx 50 2 -100` hunger), distinct from the plain `event` good-yield stream — only **43 of ~2900** event lines are `eventx`, **all 14 carriers `*_produce_*` smith animations**, so the predicate doubles as the data-pinned "producing animation that self-drains the worker" marker (the deferred production/needs drive's seed). Pinned to the extracted `extended` flag (`eventx` source key, golden rule #4). **This closes the atomic-animation read-view coverage to the per-event level — every extracted `AtomicAnimation` field (`length`/`interruptible`/`startDirection`/`events`) AND every `AtomicEvent` field (`at`/`type`/`value`/`extended`) now has a sim read view** (the event-driven *needs DRIVE* that would replace the approximated per-tick rise/reset constants with these real deltas stays deferred — no oracle for the trigger cadence). **First consumers landed (combat only):** `atomicEventChannelDelta` now drives the *Combat swing need-drain* row (an attacker pays its swing's REST/HUNGER drain), and a new sibling **`atomicEventFrame`** reads the mid-animation `event <frame> 25` ATTACK cue (`ATOMIC_EVENT_TYPE_ATTACK`) that lands a swing's blow at that frame (the *Combat hit resolution* row) — `isInterruptibleAtomic` also gains its first consumer there (the stagger interrupt rule). The *general* event-driven needs drive stays deferred. Hands-on (fresh `npm run pipeline`, 896 animations): `viking_..._eat_slot_food`→HUNGER `+4000`, `..._sleep`→REST `+500` (the per-tick `+100` ticks summed), `..._enjoy`/`..._make_love`→LEISURE `+4000`, `..._pray`→PIETY `+4000`; cross-channel (eat→REST) and unknown-name both `0`; per-channel restore counts 41 sleep / 57 eat / 112 leisure / 5 pray over the real table; 14 animations carry `eventx`, all `*_produce_*` (e.g. `viking_smith_produce_spear_iron` hasExtended=true, hunger net `-100`), a plain eat/idle and an unknown name both `false`. |
| Sprite animation cadence (walk/chop frame rate) | approximated (render) | The renderer advances every `[bobseq]` at a FIXED cadence **locked to game ticks** — `DirectionalAnim.ticksPerFrame` (default **1 frame/sim-tick**), `phase = floor(clock / ticksPerFrame) % cycle` (`packages/render/src/data/sprites.ts`). The cadence is **never stretched to an action's duration**, so every swing is the same speed: an action runs on its atomic's `elapsed` clock, `phase = (phaseStart + step) % cycle`. This replaced an earlier `progress = elapsed/duration` *stretch* that made a 4-tick deposit replay the 15-frame swing at ~4× speed (variable-speed, mid-swing cutoff). **Frame layout + start-offset verified by per-frame screenshot** (the only oracle for *which* pose — a filmstrip of all 15 frames): the `viking_chop` `[bobseq]` is a continuous loop where frames **0..8 are the axe coming DOWN to the tree (the strike, impact ~frame 8)** and **9..14 are the axe RISING (the windup)**. A `[bobseq]` has no inherent first frame, so the chop sets `DirectionalAnim.phaseStart: 9` to begin on the windup: it plays 9..14 (raise the axe) then 0..8 (swing down), so a single chop **starts** with the windup and **ends** on the strike landing in the tree (frame 8). `viking_chop` length **16** → the renderer draws elapsed 1..15 = the full 15 frames, then the chop completes and wood is taken. (Earlier missteps, recorded: playing 0..14 from frame 0 *ended* mid-recovery on frame 14 = "swings but never finishes"; truncating to 0..8 dropped the windup entirely = "no zamach". The offset fixes both.) **Deferred / approximated:** running MULTIPLE swings per harvest (the original woodcutter swings repeatedly to fell a tree) is coupled to the one-swing-per-harvest planner, deferred — a longer `length` would simply loop `phaseStart`-rotated cycles; and the impact/harvest **frame** (the `event <at>` index the wood-yield fires on in `atomicanimations.ini`) is approximated by observation (~8), not yet wired from the extracted `events`. **Frame RANGES now pinned to data (was hard-coded):** the `[bobseq]` start/length each animation draws from is now **extracted** into the IR's `bobSequences` (`extractBobSequences`), and the `?atlas=real` settler binding derives its `DirectionalAnim`s from them by sequence name (`buildHumanBindings`/`directionalAnimFromSeq`, `stride = length / 8`) instead of the old hard-coded `start 1988`/`5106`/`4580` constants — those stay only as a graceful fallback for a checkout without `content/` and match the extracted ranges byte-for-byte (verified: walk 1988/96, chop 5106/120, walk_wood 4580/96). **Still unpinned:** the original's per-`bobseq` frame *duration* isn't extracted (`animations.ini` carries frame *ranges*, not a frame-rate), so 1 frame/tick is calibration-by-observation; `ticksPerFrame` exists so a pinned cadence is a data swap, not a code change. **Deferred:** an atomic with no decoded animation (deposit/pickup 22/23) renders the standing idle pose, not a dedicated carry/place swing (no such bob decoded yet) — it must NOT borrow the woodcut swing. |
| Job→atomic gating (which job may harvest what) | faithful (params) | `jobtypes` `allowatomic`/`baseatomics` (∪/−) gate the resource good's `goodtypes` `atomicFor*` harvest atomic (`jobAtomics`/`nearestHarvestableFor`) — the data-driven "woodcutter cuts trees, not ore" rule. |
| Atomic-utility planner (harvest→carry→pileup, target choice) | approximated | Behavior, no oracle. The harvest *atomic id* is data-driven, but the planner shape (nearest-Manhattan target, load-state state machine, utility=distance) is *our* design; the original's settler AI is the undocumented "soul" (Risks). |
| Producer self-service (a workshop worker fetches its own recipe inputs + hauls its own output) | faithful (recipe-driven / the original's role split) / approximated (dispatch, no oracle) | A worker bound to a recipe workshop now runs its OWN supply→produce→deliver loop (`systems/conflict/ai-supply.ts`, `planProducer`): while a cycle can run (`workplaceProductiveIfStaffed`, reusing ProductionSystem's `canStartCycle`) it holds the tile; else it FETCHES a recipe input it lacks from any *other* store that holds it (`nearestMissingInputSource` — carrying exactly the shortfall, "tylko te wymagane") and HAULS its finished output out (`workplaceOutputToHaul`). This closes the loop when inputs sit in a **warehouse** the harvester delivers to rather than being dropped straight into the shop — the "kowal zabiera surowce z magazynu, robi miecz, odnosi" behavior. **Faithful (structure):** every branch is recipe-driven, no per-job/per-good code, and *which* good is an input/output is the extracted `recipe` (not invented); the delivery routing (`deliveryTargetFor`) sends a fetched input to the workshop and a harvested/collected good to the settler's bound store. **Approximated (behavior, no oracle):** *whether* the original has the single worker self-fetch vs. a dedicated porter do all logistics (the user's model has the smith fetch its own; real Cultures leans on Träger — both are plausible, the sim does the former per the request), and the dispatch (nearest-Manhattan source/sink, "never deliver into the producer") is the same our-design stand-in the carrier row records. Determinism: canonical (ascending-id, Manhattan+cell-id) scans, no RNG/wall-clock. Exercised by the vertical-slice golden (the carpenter now also drains the HQ's 10 starting wood → 18 planks, goods conserved) and `test/economy/producer-supply.test.ts`. |
| Porter ground-pile collection + gatherer flag-drop (loose goods → the store they belong to) | faithful (the two-target delivery model) / approximated (behavior, no oracle) | Models the original's "gatherers drop at a warehouse OR a ground flag; a porter ferries loose piles into its warehouse". A **ground pile / flag** is a bare `Stockpile` on a positioned entity with no `Building` (a loose heap). A **porter** — a settler bound (`JobAssignment`) to a *storage* fixture, `isPorterBoundToStore` — with nothing to harvest or produce collects the nearest non-empty ground pile (`nearestGroundPile`) and carries it to its warehouse; a **gatherer** bound to a warehouse *or* a flag delivers its harvest there (`deliveryTargetFor` routes to the bound store, not merely the nearest one, so a porter never re-dumps onto the pile it just cleared and a gatherer drops at its assigned flag). **Approximated (no oracle):** the porter *dispatch* (nearest pile, one good at a time) and the flag/pile as a bare stockpile (the original's flag is an engine construct not in the readable `.ini`) are our deterministic design; OpenVikings' sim is a stub. Determinism: canonical scans, no RNG. Covered by `test/economy/producer-supply.test.ts`. |
| Carrier (haul workplace outputs to a store) | faithful (carry-capacity param + vehicle-unlock edge) / approximated (carrier→vehicle pairing, dispatch) | **Pinned (the capacity + the unlock):** the carry batch a carrier hauls per swing is now the largest `stockSlots` (`vehicletypes` carry capacity — handcart 15 / oxcart 30 / ship 50,200) among the vehicle types its tribe has **unlocked**, falling back to **1** (a single unit on foot) before any vehicle is available. `carrierCarryCapacity` (`systems/progression.ts`, called from `aiSystem`'s carrier pickup) is the sim's **first consumer of the `vehicle` kind of the `jobEnables` tech-graph** (the same `tribeUnlockEnabled` read side the house/good gates use): a vehicle is unlocked when no `jobEnablesVehicle` edge gates its `typeId` (an ungated start vehicle) OR a settler of any gating job is alive in the tribe; the capacity is the best `stockSlots` over the unlocked set, so a carrier hauls with the biggest cart its tribe can field. `pickupFromStore` caps the move to what the source actually holds, so the batch never conjures goods. **Approximated (behavior, no oracle):** the carrier→vehicle **pairing** — the original assigns a specific vehicle per haul and a carrier visibly fetches/parks a cart; here a carrier abstractly hauls at its tribe's best unlocked capacity (no per-carrier vehicle entity yet), the *carrier dispatch* (which carrier, which haul) is still our nearest-Manhattan design, "never deliver back into the producer" remains an unpinned choice, and the per-vehicle `logicgood` carry-allow-list is **not** applied — a later vehicle-entity slice models cart logistics. **This deferral is now a recorded conscious deviation** (see Deviations — *Carrier→vehicle pairing*): the data is consumed, the divergence is knowable, and the faithful path is named, but the per-carrier vehicle entity is oracle-blocked (`vehicletypes.ini` carries no carrier→vehicle binding/dispatch key; OpenVikings' sim is a stub) so modelling it now would be invented. Inert in the golden slice (its tribe ships no `vehicles`, so capacity stays the floor 1 → golden untouched). Hands-on: the real `aiSystem` schedule — a carrier whose tribe gates a handcart (`jobEnablesVehicle 9 5`, stockSlots 15) behind a carter → with **no** carter present `carrierCarryCapacity = 1` and the pickup atomic loads `amount: 1`; with a carter alive it returns **15** and the pickup loads `amount: 15`. |
| Multi-hit harvest / felling (chop → tree falls → trunk drop → collect) | faithful (the tree→trunk→wood lifecycle structure) / approximated (observed chop count + yield, no oracle) | A wood `Resource` node carries a `Felling{chopsLeft}` — stamped by the **placement code** from the good's `gathering.chopsToFell` (data, never a hardcoded goodType; there is no resource-spawn *system* yet — today scenes/tests place the nodes, and the content→`Felling` gate lands with the map/resource spawn path). The sim keys the fell path on the `Felling` component's PRESENCE, so a node without it stays single-hit. Each completed harvest atomic CHOPS it (yielding NOTHING onto the back); on the swing that zeroes `chopsLeft` the standing node is DESTROYED and drops its whole `Resource.remaining` yield at its cell as a bare `Stockpile` trunk (a `GroundDrop`), leaving a `Stump` decor (`atomic.ts` `fellNode`, `resourceFelled` event). The felling collector then prefers its own fresh trunk over walking to the next tree (`nearestCollectablePileFor` weighed against `nearestHarvestableFor` in `ai.ts`) and delivers it via the EXISTING pickup/porter/delivery machinery, a load at a time (multiple trips at the on-foot carry). Goods are conserved (the trunk holds exactly the standing node's worth — nothing created/lost by the fall), and the felled node is REMOVED so the planner never re-scans a depleted stump-to-be (fixing the old "skip a `remaining<=0` node forever"); an emptied trunk `GroundDrop` is auto-reaped (a collected trunk vanishes) while a designated flag (a marker-less bare `Stockpile`) persists, and a `GroundDrop` is never a delivery SINK (`nearestStoreFor` excludes it) so a collector never deposits wood back into the trunk it just lifted from. **FAITHFUL (structure):** the `tree → "tree falling" → trunk → wood` lifecycle is the original's, pinned to `landscapetypes.ini` / the good's `landscapeTo{Harvest,Pickup,Store}` chain; the chop is the good's `atomicForHarvesting`. **APPROXIMATED / OBSERVED (no oracle — the readable `.ini` carries neither, verified absent, no `baserepeatcounter` for the collector job):** the **chops-to-fell** count and the **per-tree wood yield** are OBSERVED calibration constants held as CONTENT (`GoodGathering.chopsToFell` / `yieldPerNode`, default 0 = single-hit) so tuning is a data diff — the extractor emits 0 (pending calibration), the golden fixture sets its own demonstrative value, and the scene reads the ONE global felling calibration (`catalog/felling.ts` — 6 chops / 3 wood, not a per-scene number). **RENDER (now faithful, per-stage graphics):** the freshly-felled trunk on the ground draws its good's `landscapeToPickup` stage (the felled LOG), DISTINCT from the tidy delivered `landscapeToStore` heap (`GroundDrop` → its own `grounddrop` DrawKind + `trunk` binding, `resource-gfx.ts` `buildTrunkBinding`); and the delivery flag stays planted UNDER its growing wood heap (drawn behind it) so the collection marker never vanishes. **DEFERRED (Step 4):** other gathered goods (stone/clay/…) keep the single-hit gather (one unit per swing, the node persisting) — they share the drop-on-ground shape but per-unit, a later slice reuses this machinery. See the *Instant fell* Deviation for the missing falling animation. Covered by `test/conflict/felling.test.ts` + the vertical-slice golden (the woodcutter fells its two trees → 2 stumps, goods conserved 18 wood → 18 planks). |
| Production recipe **inputs/outputs** (the goods transformed per cycle) | faithful (params) | the **output-side join now lands** (`fillBuildingRecipes`): a workplace's `logicproduction` output good → that good's `goodtypes.productionInputGoods` materializes the building `recipe.inputs`; `recipe.outputs` = each produced good at amount 1 (the `logicproduction <good>` semantics carry no per-output quantity). Hands-on on the real game: 26/28 workplaces get a recipe (22 with non-empty inputs), 0 dangling refs, and the recipes are recognisably the original economy (mill `wheat→flour`, bakery `water+flour→bread`, brewery `water+honey→mead`). The sim no longer needs the synthetic sawmill stand-in. |
| Production **per-cycle ticks** | faithful (params, reference-tribe) | `recipe.ticks` is now resolved from the produce atomic's animation `length` (`fillBuildingRecipes` → `resolveRecipeTicks`): the building's worker `jobType` + the primary produced good's `goodtypes.atomicForProduction` form the `(jobType, atomicId)` key into the **reference tribe's** (lowest-`typeId`, deterministic) `setatomic <job> <atomicId> "anim"` table (last-wins) → that `atomicanimations` `length`. Hands-on on the real game: **22/26 producing workplaces** pin to a real length (mill flour=200, brewery mead=50, pottery brick=80, sewery shoes=160, …; distribution {50,80,100,120,150,160,200,240}), the 4 left at `DEFAULT_RECIPE_TICKS`=20 being raw-good producers with no `atomicForProduction` (well/hive/farm). **APPROXIMATED on two recorded axes (see Deviations):** (a) production length **varies per tribe** in the source (viking coiner=200 vs frank coiner=60), so the reference-tribe value loses the per-tribe spread — a per-tribe recipe table is the fully-faithful model, deferred; (b) a multi-output workplace has one length per output atomic, collapsed to the primary output's (the merged recipe carries a single `ticks`). |
| Production system (consume-at-start / deposit-at-completion / output-capacity gate / **built gate**) | faithful (the built gate's behavior) / approximated (the loop shape) | The *system* shape is data-shaped (recipe read from CONTENT) but the loop (reserve inputs at cycle start, deposit outputs at completion, never start unless outputs fit) is *our* design — the engine's production loop has no oracle. **The built gate now lands faithfully:** `productionSystem`'s start loop skips any workplace still `built < ONE`, so **an under-construction workshop produces nothing** — the original's named behavior (a workshop doesn't function until it is constructed; see the *ConstructionSystem* row's faithful baseline). This was previously only an *incidental* side effect: a construction site's `stockCapacity` is its outstanding `construction` materials, so an output good not in the cost got room 0 and a cycle "happened" not to start — but a recipe whose **input** overlapped the delivered build material could have been raided by production (idx 11 in `SYSTEM_ORDER`) before `constructionSystem` (idx 13) built the site. The explicit `built >= ONE` gate closes that, making "unbuilt ⇒ no production" guaranteed, not emergent. Inert on the golden (every golden building is placed `built = ONE`). |
| Production **worker-presence gate** (produce only while staffed) | faithful (params) / approximated (behavior) | **Params faithful:** the worker requirement is the building type's `workers` slot (`logicworker <job> <count>` → `extractBuildings`); a workplace produces only while a `Settler` whose `jobType` matches a slot stands on its tile (`workerPresentAt`), matching the original's "a workshop runs only while its worker is inside" — a sawmill with no operator makes no planks. A type with no `workers` slot is unstaffed-by-design and produces freely. **Behavior approximated:** "present == shares the integer tile", "pause-and-hold `elapsed` when the worker leaves / resume on return", and the planner's "pin a settler standing on its bound workplace" (`staffsBoundWorkplaceHere`) are *our* minimal model. The JobSystem now assigns an idle settler the job **and binds it to a specific building** (`JobAssignment`), and the walk-to-bound-workplace drive sends it there (rows below), so the assignment→bind→walk→staff loop closes and the worker stays latched to *its* workplace (the per-building binding replaced the old tribe-wide head-count stand-in). What the original does when a worker is mid-walk has no oracle — it may run production off an *assigned* (not physically-present) worker; refine when calibration-by-observation lands. |
| Stock capacity enforcement | faithful (params) | per-good caps from the building type's `logicstock <good> <cap> <init>` slots (`extractBuildings` → `stockCapacity`). |
| Housing capacity read model (`homeSize` consumed by `housingCapacity`/`tribePopulation`) | faithful (params) / approximated (the births mechanic it gates — see the ReproductionSystem row) | **Pinned (the param):** a residence's shelter capacity is the building type's `homeSize`, extracted verbatim from `logichousetype` `logichomesize` (`extractBuildings`) — home level 00 → 1, level 01 → 2, … level 04 → 5 (the only 5 keys in `houses.ini`, on the 5 `logicmaintype 2` "home" blocks). `housingCapacity` (`systems/shared.ts`) is the sim's **first consumer** of `homeSize`: it sums the `homeSize` of a tribe's placed, **built** (`built >= ONE`) `kind === 'home'` buildings — the ceiling the population grows into. Its companion `tribePopulation` counts the tribe's living settlers. Together they are the housing-vs-population read model the ROADMAP's `house leveling → population capacity → births→housing→births` loop needs (and the now-landed `populationWithinHousing` invariant). **Behavior now wired (births):** what the capacity *gates* — births — is the ReproductionSystem (row below), the first WRITER of this read model; a tribe now grows toward this ceiling. **CORRECTION (the build cost is NOT below the `.ini`):** the earlier "deliver materials → build has no readable oracle" claim was wrong — it conflated the *logic* table (`DataCnmd/types/houses.ini`, `[logichousetype]`, which indeed carries no cost key) with the *graphics* table (`DataCnmd/budynki12/houses/houses.ini`, `[GfxHouse]`), whose `LogicConstructionGoods` lines DO carry the per-level build-material cost. That table is now extracted (`construction` row below); only the construction-system *behavior* (deliver-then-build, the upgrade trigger) remains deferred — the cost data is no longer the blocker. Hands-on: `npm run pipeline` on the real game → the 5 `home` buildings (typeIds 2..6, `home_level_00..04`) carry `homeSize` `{1,2,3,4,5}` exactly matching the `.ini`, 0 non-home buildings with a stray `homeSize`; the unit tests verify the sum picks out only built `home`-kind buildings, is per-tribe, and excludes a still-constructing residence. |
| Build-material cost (`construction` — the goods to construct/upgrade a building) | faithful (params, reference-tribe) / approximated (per-tribe collapse) | **Pinned (the param):** a building's build cost is now extracted from the graphics table's `[GfxHouse]` `LogicConstructionGoods` line (`DataCnmd/budynki12/houses/houses.ini`, `extractConstructionCosts`) and overlaid onto the logic-table `BuildingType` by `typeId`. The line is a flat good-id list where a **repeat encodes quantity** (`3 3 26` = 2× stone + pillar), collapsed to `{goodType, amount}` exactly like a recipe's `productionInputGoods`. The home **level chain** surfaces here: the same `[GfxHouse]` block's several `LogicType <sizeIdx> <typeId>` levels (`home level 00..04` = typeIds 2..6) each pair with their OWN per-level `LogicConstructionGoods` — an upgrade-cost-per-tier, not cumulative. **APPROXIMATED on one recorded axis (see Deviations — *Per-tribe construction cost*):** the cost is genuinely keyed by **(tribe, typeId)** — the source carries a `LogicTribeType` per block and the SAME logic `typeId` carries a different cost per civilization (viking/frank/byzantine model a home as an upgrade *chain* — level 4 = `27 27`, ornaments only — while egypt/saracen model the same typeId as a standalone *full* build, the cumulative list). To keep a single flat `BuildingType.construction` field, the overlay collapses to the **lowest-`LogicTribeType`** record (the deterministic "reference tribe" convention `fillBuildingRecipes` already uses); a per-tribe construction table is the fully-faithful model, deferred with the per-tribe recipe table. Cross-ref: each `construction` goodType is checked against the good table by `validateCrossReferences` (same dangling class as `stock`/`produces`). Hands-on: `npm run pipeline` on the real game → **53/55 buildings carry a cost**, 2 free (headquarters typeId 1; `work_murek` typeId 55, the one type the gfx table omits); the viking home chain resolves `[wheat,2×wood,mud] → [wheat,2×stone,pillar] → 2×brick → 2×tile → 2×ornament`, the per-level upgrade cost. The construction-system *behavior* (deliver materials → build, the level-up trigger) is still our future design (no behavioral oracle), but the cost *data* is now faithful. |
| ConstructionSystem (build-completion + home level-up) | faithful (material-cost param) / approximated (loop shape, no oracle) | A building placed `underConstruction` enters `built = 0`; once its `Stockpile` holds the full extracted `construction` cost the system consumes it and flips to `built = ONE` (a built home then joins `housingCapacity`). A built `home` that accumulates the NEXT tier's cost upgrades to the next typeId — the level chain is the consecutive `home_level_00..04` typeIds (`homeNextTier`), purely data-driven. **PINNED:** the material cost is the verbatim extracted `construction` param; the system only *spends* it. **APPROXIMATED (no oracle):** the build/upgrade LOOP shape ("materials all present → consume → flip", ≤1 tier/tick) is our design — the engine's per-tick build cadence is below the readable data. Both build- and upgrade-material DELIVERY reuse the EXISTING carrier path with no construction-specific transport code (`stockCapacity` advertises the outstanding cost as carrier demand; a finished/maxed building reverts to its stock cap and stops attracting materials). Inert on the golden (no `underConstruction`/`home` fixture). |
| Building ground footprint (walk-block body / build-exclusion zone / door point) | faithful (the cell data) | The original's per-house ground model, extracted from the graphics table's `[GfxHouse]` records (`extractBuildingFootprints`): `LogicWalkBlockArea <sizeIdx> <x> <y> <run>` (the walls — per size level), `LogicBuildBlockArea <x> <y> <run>` (the build-exclusion zone — defined ONCE per record with **no level index**, so a level-0 hut reserves exactly its top level's space, verbatim the original's rule), `LogicDoorPoint <sizeIdx> <x> <y>` (the entry cell — adjacent to the walls for every house; the defence-wall records put it INSIDE the walk-block, because a wall's door is its passable GATE — the nav overlay carves the door cell out, see the blocking row). Emitted per typeId as `BuildingType.footprint` (`blocked` / `familyBody` = the union of every level's walls / `reserved` = familyBody ∪ the zone; the union matters — a few real records have wall cells the zone misses). 54/55 real buildings carry one (the decorative `work_murek` has no collision data). Cross-tribe same-typeId records collapse to the lowest tribe (the reference-tribe convention — the same rule `extractConstructionCosts` applies, though NOT yet record-for-record consistent with it: footprints split the mod's lumped multi-house `[GfxHouse]` brackets (`splitGfxHouseRecords`), while the cost extractor still reads them whole — the pre-existing lumping bug flagged in its JSDoc, so a lumped saracen/egypt typeId can source its footprint and its cost from different sub-records until that lands). |
| Free building placement (collision + minimum distance, replacing "big fields") | faithful (the areas) / **approximated (the overlap rule — no oracle)** | `canPlaceBuilding` (`systems/footprint.ts`, gating `placeBuilding`): a house places ANYWHERE its footprint fits — its `reserved` zone in-bounds, on walkable terrain, clear of `Resource` nodes (the minimum distance from a tree / blocking terrain), and **body-vs-zone symmetric** against every existing building (my `familyBody` out of its `reserved`, its `familyBody` out of mine; a footprint-less building counts as a 1-cell body/zone). The AREAS are the extracted params; the overlap RULE is our reading — the engine's check has no oracle. The chosen symmetric rule lets two houses' margin rings overlap (gap = the larger margin → houses pack closely, the original's "very free" placement); the stricter alternative (zone-vs-zone disjoint → gap = both margins summed) is named here in case observation of the original later contradicts. Settlers never block placement (the foundation appears under them; they walk off). |
| Buildings block movement (walk-block collision) | faithful (the cells + from-foundation timing) / approximated (routing details) | A standing building's CURRENT-level `blocked` cells are unwalkable from the **placement tick** (a grey foundation already occupies its ground, like the original): `buildingBlockedCells` (derived per tick from the live `Building`s — rebuilt, never stored, so it cannot drift across upgrades/demolitions) feeds `findPath` as a dynamic overlay (a goal inside the walls fails; a blocked START is exempt so an entity standing where a foundation just appeared steps OFF and never back in — our design). A building's own DOOR cell is carved out of the overlay even when the source lists it inside the walk-block — the defence wall (`work_pottery_02`) does exactly that, its door being the passable gate; without the carve-out a walk-to-door goal would permanently wedge the walker. An in-flight path crossing a new foundation — or a home-upgrade's newly GROWN walls (the tier swap runs after pathfinding in SYSTEM_ORDER) — is not invalidated (repath-on-collision deferred); the original's exact avoidance behavior has no oracle. |
| Door-cell interaction (settlers enter/work at the door) | faithful (the door param) / approximated (interaction collapse) | Every walk-to-building target and every "at the building" check resolves through ONE seam — the building's **interaction tile** (`interactionTile`: anchor + `footprint.door`, the anchor itself for a footprint-less type): the AI walk goals + arrival checks (`entityCell`), the operator pin (`staffsBoundWorkplaceHere`), the production worker-presence gate (`workerPresentAt`), and the JobSystem adopt bucket. Faithful to "settlers enter through the door, not any side"; APPROXIMATED in that all interaction happens AT the door cell — the original's settlers then step INSIDE and vanish, and an OPEN workshop (the `GfxOverlay`-carrying types, e.g. the bakery) shows its worker inside while a closed house hides it. That enter/hide split is deferred (the `GfxOverlay` semantics are not decoded); today every worker stays visible at the door, which matches the OPEN case and diverges on closed houses. |
| Construction progress = delivered-material fraction | approximated (no oracle) | While `built < ONE` the ConstructionSystem now sets `built` to the site's **delivered fraction** (Σ min(held, needed) / Σ needed, floored fixed-point — strictly < ONE until complete, so the `built >= ONE` production/housing gates stay exact). In the original a site rises as **builders work it** (the `constructionworker_Work_Hammer` atomic + `LogicConstructionWorkArea` stand cells — both in the data, unconsumed); the delivery fraction is the faithful-leaning proxy that gives the render a real progress signal until a builder mechanic lands. |
| Construction-stage graphics (grey foundation → stages → finished body) | faithful (the layer table) / approximated (the `upgrade` rows + shadows) | `GfxBobConstructionLayer <sizeIdx> <upgrade> <bobId> <shadowBobId\|-1> <fromPct> <toPct>` extracted per typeId (`extractConstructionLayers` → IR `constructionLayers`, 896 rows); the render (`resolveConstructionDraws` + the WorldRenderer multi-sprite stack) draws every layer whose range contains the site's progress percent, STACKED in file order — at 0% the grey foundation alone, mid-build the overlapping stages, the finished body on top — from the same decoded house atlases the finished body uses. The `upgrade = 1` rows (each references the NEXT level's body) are extracted but **not consumed** — they belong to the original's upgrade-in-progress overlay, semantics not decoded; the per-layer `shadowBobId` is recorded but not drawn (shadows are the blocked shadow-palette rung). |
| ReproductionSystem (birth — population grows to the housing ceiling, as babies) | approximated (cadence/sex) / faithful (age-class param) | **The faithful baseline this matches:** a Cultures colony grows on its own — with spare housing, new settlers appear and the population climbs toward (and stops at) the housing ceiling. `reproductionSystem` (`systems/lifecycle/reproduction.ts`) is the **first WRITER** of the housing read model (the rows above were the read side): it creates one settler per tribe per tick while `tribePopulation(tribe) < housingCapacity(tribe)`, born a **baby** (NOT an instantly-employable adult) at the tribe's lowest-id **built** `home` tile (a birth at a residence). The cadence IS the gate — deterministic (no RNG, no birth-rate constant) and **self-limiting**: once population reaches capacity the gate is false and growth stops, so the new **`populationWithinHousing` invariant** (a content-bound factory in `invariants.ts`, since the `Invariant` signature carries only the world but the check needs the `homeSize` param) can never be breached by a birth. **PINNED (the age-class structure — newly landed):** a newborn's `jobType` is the data-pinned youngest age class `NEWBORN_AGE_CLASS` = `baby_female` (id 1), pinned to `logicdefines.inc` `JOB_TYPE_HUMAN_BABY_FEMALE = 1` + the `jobtypes.ini` `name "baby_female"` record — in the original the first five `jobtypes` records (`baby_female`/`baby_male`/`child_female`/`child_male`/`woman`) are **age/sex classes**, not working trades, a settler passes through before an adult job. `systems/lifecycle/ageclass.ts` is the sim-side recognition (`isBaby`/`isChild`/`isNonWorkingAge`, ids 1–4 non-working); the JobSystem leaves a baby unemployed (its `jobType` is non-null → skipped by the idle-only assignment; no workplace lists a baby in its `workers` slots → never adopted). **APPROXIMATED (no oracle, recorded as a Deviation):** the **birth rate** (one/tribe/tick), the **anchor tile** (lowest-id home), and the **sex** (`baby_female` chosen deterministically — no readable sex oracle) are unpinned — the original's birth cadence / family model / where a child appears live below the readable `.ini` (there is no birth-rate key in `houses.ini`/`tribetypes.ini`; the `make_love` atomic restores the leisure **channel 3**, it carries no birth yield — see LESSONS [8302ea7]). The faithful *shape* ("grow to the ceiling and stop, as babies") is matched; the rate + the **growth cadence** (baby→child→adult, the next ROADMAP item) are calibration-by-observation. Inert in the golden/slice (their content has no `home`-kind building → 0 births → golden hash + trace unchanged, the [6264132] "only fires on a state the goldens never construct" pattern). Hands-on: the **real `Simulation.step()` schedule** on a one-`home_small` (homeSize 3) tribe → population grows `1,2,3` then holds at 3, all three newborns `jobType = 1` (baby_female), none employed; two seed-7 runs hash-equal across 6 ticks (`0e36e4b3`). |
| GrowthSystem (a born settler matures baby→child→adult-eligible) | approximated (cadence) / faithful (age-class structure + sex split) | **The faithful baseline this matches:** in the original a settler is born young and grows into a worker — the first `jobtypes` records are age/sex classes (`baby_female`/`baby_male`/`child_female`/`child_male`) a settler passes through before an adult trade. `growthSystem` (`systems/lifecycle/ageclass.ts`) ages each born settler — the **`Age{ticks}`** optional component the ReproductionSystem adds at birth (a separate optional component like `JobAssignment`, so only a born-young settler carries it; an adult never does) — and **promotes** its age-class `jobType` baby→child after `GROWUP_TICKS`, child→adult-eligible (`jobType` null) after another, **sex preserved** (`baby_female`→`child_female`, `baby_male`→`child_male`), then **removes** the `Age` component so a grown settler is just an idle adult the JobSystem employs. The AI planner skips a still-growing settler (keyed on the **`Age` component**, not `isNonWorkingAge(jobType)` — a synthetic fixture's adult job id can collide with a real age-class id, e.g. the golden slice's woodcutter is jobType 1 = the `baby_female` id, but only a born-young settler carries `Age`), so a baby/child does NOT run the adult eat/sleep/pray drives — faithful to "a baby is cared for, it doesn't self-feed". **PINNED (faithful):** the age-class ids + the female/male split are the `logicdefines.inc`/`jobtypes.ini` records (no interpretation). **APPROXIMATED (no oracle — Deviation):** the **cadence** `GROWUP_TICKS`=8192/stage is unpinned (there is no readable "grows up after N ticks" key in `jobtypes.ini`/`tribetypes.ini`/`houses.ini`) — the established hunger-rise-style constant, calibration-by-observation pending. Inert in the golden/slice (no `home`-kind content → 0 births → no `Age`-bearing settlers → golden hash + trace unchanged, the [6264132] pattern). Hands-on: the **real `Simulation.step()` schedule** births a baby (`jobType=1`), then at tick 8192 it is `jobType=3` (child_female, sex kept), at tick 16384 `jobType=null` with no `Age` (employable adult); two seed-7 runs hash-equal (`6b24b429`). |
| Resource depletion (finite nodes) | approximated | `HARVEST_YIELD`=1 unit/harvest (node survives exactly N harvests) — goods-conserving and structurally sound, but the per-node yield/regrowth isn't pinned to a source yet. |
| Hunger rise (NeedsSystem) | approximated | The original drives hunger through `atomicanimations.ini` `event <at> 2 <delta>` tuples — an activity animation drains a fixed amount (e.g. `event 30 2 -100`) while an `eat_slot_food` animation restores it (`event 30 2 +4000`, candy `+6000`), on a large integer scale where one meal ≈ +4000 (verified across all tribes' `eat_slot_food`/`eat_slot_candy` records). That **event-driven, per-animation** model needs the atomic `event (type,value)` vocabulary decoded (a deferred Phase-1 extraction — ROADMAP risks). For now `needsSystem` raises hunger at a CONSTANT `HUNGER_RISE_PER_TICK`=ONE/4096 (an empty bar fills in 4096 ticks), clamped at ONE so the `hungerInRange` invariant holds; the `eat` atomic resets it to 0 (AtomicSystem). Hands-on: 5000 ticks through the real schedule → hunger rises and clamps exactly at ONE, 0 invariant violations, deterministic. The **rise/reset loop shape is the original's core** ("hunger grows over time, eating restores it"); the per-activity `event 30 2 <delta>` rates + the eat-restores-by-`+4000` magnitude are the faithful target (deferred — see Deviations). The non-food needs (`pray`/`enjoy`/social/`make_love`) are later slices. |
| Fatigue rise (NeedsSystem, the rest/sleep need) | approximated | The first non-food need: `needsSystem` raises `fatigue` at a CONSTANT `FATIGUE_RISE_PER_TICK`=ONE/8192 (half hunger's rate so a settler eats ~twice per sleep), clamped at ONE (the `fatigueInRange` invariant). The original ticks rest via per-animation events the same way hunger does — `viking_civilist_sleep` carries `event <at> 1 +4000` tuples (type 1 = the rest channel, as type 2 is hunger, on the same ~10000-scale bar; verified across tribes) — needing the atomic `event (type,value)` vocabulary decoded (the same deferred Phase-1 extraction hunger waits on). The pairing **reset** is the `sleep` atomic id **8**, pinned to the original `tribetypes` `setatomic <job> 8 "..._sleep"` bindings (bound for every job/tribe, even babies). The rise/reset loop shape is the original's core ("tiredness grows, sleeping restores it"); the per-activity rates + the eventual sleep DRIVE are deferred. Hands-on: 200 ticks through the real schedule → fatigue rises at half hunger's rate, stays below it, 0 invariant violations, two seed-7 runs hash-equal (`98993f42`). |
| Piety rise (NeedsSystem, the first target-bound need) | approximated | The first **target-bound** non-food need (satisfied by praying *at a temple*, not at a store like eat or in place like sleep): `needsSystem` raises `piety` at a CONSTANT `PIETY_RISE_PER_TICK`=ONE/16384 (half fatigue's rate so a settler prays ~once per two sleeps), clamped at ONE (the new `pietyInRange` invariant). The original ticks devotion via per-animation events the same way hunger/fatigue do (numbered channels: type 1 = rest, type 2 = hunger; the religious channel is another id), needing the atomic `event (type,value)` vocabulary decoded (the same deferred Phase-1 extraction). The pairing **reset** is the `pray` atomic id **12**, pinned to the original `tribetypes` `setatomic 6 12 "..._pray"` bindings (the civilist job, across all tribes — verified in the mod `tribetypes.ini`). The rise/reset loop shape is the original's core ("devotion lapses, praying restores it"); the per-activity rates are deferred, but the pray **DRIVE** (planner walking the settler to a temple to pray) **has now landed** — see the *Pray drive* row. Hands-on: 20000 ticks through the real schedule → piety rises at half fatigue's rate, clamps exactly at ONE at tick 16384, piety ≤ fatigue ≤ hunger every tick, 0 invariant violations, two seed-7 runs hash-equal (`0a20b59b`). |
| Enjoyment rise (NeedsSystem, the recreation/leisure need) | approximated | The leisure need: `needsSystem` raises `enjoyment` at a CONSTANT `ENJOYMENT_RISE_PER_TICK`=ONE/32768 (half piety's rate — the least-pressing of the four bars), clamped at ONE (the new `enjoymentInRange` invariant). The original ticks leisure via per-animation events the same way the other needs do — `viking_civilist_enjoy` carries `event <at> 3 <delta>` tuples (channel 3 = the enjoy/leisure need, as channel 1 = rest, 2 = hunger; verified across tribes), needing the atomic `event (type,value)` vocabulary decoded (the same deferred Phase-1 extraction). The pairing **reset** is the `enjoy` atomic id **17**, pinned to the original `tribetypes` `setatomic 6 17 "..._civilist_enjoy"` bindings (the civilist + woman jobs, across all tribes — verified in the mod `tribetypes.ini`) — the `enjoy` `AtomicEffect` zeroes `enjoyment` (AtomicSystem, no goods consumed — like sleep/pray). The rise/reset loop shape is the original's core ("leisure lapses, having fun restores it"); the per-activity rates are deferred. **Unlike piety, the DRIVE is deferred:** `enjoy` has **no readable building satisfier** — the only no-recipe/no-worker/no-stock houses in `houses.ini` are `work temple` (lt 37) and `work murek` (a decorative wall, lt 55), so the `isTemple`-style structural signature does **not** distinguish a leisure site; the satisfier→need binding lives below the readable rule files and needs a content building→need extraction before a drive can be pinned (so the reset is wired but no planner branch chooses it yet). Hands-on: through the real schedule → enjoyment rises at half piety's rate (200 after 100 ticks), stays ≤ piety every tick, clamps exactly at ONE at tick 32768, 0 invariant violations, two seed-5 runs hash-equal (`1f0add20`). |
| Make-love reset (`make_love` atomic restores the leisure need) | faithful (atomic id + shared channel) / deferred (drive) | **Pinned:** the make-love atomic id is **78** (`#define MAP_MOVEABLES_ATOMIC_ACTION_TYPE_MAKE_LOVE 78` in `logicdefines.inc`), bound for the woman + civilist jobs across every tribe (`setatomic 5 78 "..._woman_make_love"` / `setatomic 6 78 "..._civilist_make_love"` in the mod `tribetypes.ini`). It is **NOT a separate need:** the `..._make_love` animation restores the **same channel 3** as `enjoy` via `event <at> 3 +800` tuples (a bigger leisure boost than `enjoy`'s `+100`) — channel 3 = the leisure/`enjoyment` bar — so the new `make_love` `AtomicEffect` zeroes the **same `enjoyment` field** the `enjoy` effect does (AtomicSystem, no goods consumed — like enjoy/sleep/pray). This is the last named non-food need (eat/sleep/pray/enjoy/make_love); social is a render/grouping concern, not a settler bar. **Deferred (drive):** like `enjoy`, `make_love` has **no readable building satisfier** in `houses.ini` (same reasoning) — so only the reset is wired (no planner branch chooses it yet). Hands-on: a settler with `enjoyment`=ONE running `make_love` (id 78, duration 3) through the real `Simulation.step()` schedule resets `enjoyment` at tick 3, CurrentAtomic removed on completion, two seed-5 runs hash-equal (`e52c6f77`). |
| Sleep drive (planner chooses `sleep` at a fatigue threshold) | faithful (atomic id) / approximated (trigger + in-place rest) | **Pinned:** the sleep atomic id is **8**, read straight from the original `tribetypes` `setatomic <job> 8 "..._sleep"` bindings (id 8 = the sleep slot across every tribe/job, even babies) — the same `(jobType, atomicId)`→animation→`length` join the other atomics use (the slice fixture binds 8→"viking_sleep", length 6). When fatigue ≥ threshold the planner starts a `sleep` atomic (below the eat drive — a starving settler eats before it can rest — but above harvest/haul/staffing, so a worn-out operator stops working); the `sleep` effect then zeroes fatigue (AtomicSystem, no goods consumed — resting is free) — closing the rise→sleep→reset loop. **Approximated:** (a) the settler sleeps **in place** (no target site) — the original sleeps at *home*, but the housing/home system that would give a sleep target doesn't exist yet, so resting where it stands is the slice stand-in; (b) the **`FATIGUE_SLEEP_THRESHOLD`=¾·ONE** trigger is unpinned, mirroring the eat trigger — the original sleeps off the per-animation rest events (`event <at> 1 <delta>`) with no single readable "sleep at X" level; both refine when the atomic `event` vocabulary is decoded + calibration lands (see Deviations). Hands-on: a settler crossing the threshold starts the sleep atomic tick 1, fatigue resets to 0 by tick 6 (the 6-tick animation), peak never breached ONE, 0 invariant violations, two seed-5 runs hash-equal (`8e2e10b0`). |
| Eat drive (planner chooses `eat` at a hunger threshold) | faithful (atomic id) / approximated (trigger + food id) | **Pinned:** the eat atomic id is **10**, read straight from the original `tribetypes` `setatomic <job> 10 "..._eat_slot_food"` bindings (id 10 = the eat slot across every tribe; id 11 = candy) — the same `(jobType, atomicId)`→animation→`length` join the other atomics use (the slice fixture binds 10→"viking_eat"). When hunger ≥ threshold the planner starts an `eat` atomic (above harvest/haul/staffing), consuming **one unit of food** from the settler's carried load or the nearest store holding food; the `eat` effect then zeroes hunger (AtomicSystem) — closing the rise→eat→reset loop with goods conserved (food destroyed on the bite, never conjured). **Approximated:** (a) *which good is food* is inferred from the `food` id prefix (`food_simple`/`food_extra`, the original's slot-food goods) — `goodtypes.ini` carries no explicit "iseatable" flag, the food→eat-slot binding lives below the readable rule files; (b) the **`HUNGER_EAT_THRESHOLD`=¾·ONE** trigger is unpinned — the original eats off the per-animation `event 30 2 <delta>` cadence with no single readable "eat at X" level; both refine when the atomic `event` vocabulary is decoded + calibration-by-observation lands (see Deviations). Hands-on: a settler crossing the threshold beside a larder walks over, eats at tick 11 (hunger 49312→0), consumes exactly 1 food unit, 0 invariant violations. |
| Pray drive (planner chooses `pray` at a piety threshold — the first **target-bound** need) | faithful (atomic id) / approximated (trigger + temple inference) | **Pinned:** the pray atomic id is **12**, read straight from the original `MAP_MOVEABLES_ATOMIC_ACTION_TYPE_PRAY = 12` (`logicdefines.inc`) + the `setatomic 6 12 "..._pray"` bindings (the civilist job) — the same `(jobType, atomicId)`→animation→`length` join the other atomics use (the slice fixture binds 12→"viking_pray", length 7). When piety ≥ threshold the planner does the genuinely-new **need→satisfier→building-target lookup**: find the nearest **temple** and set a `MoveGoal` to walk there (unlike eat at a store / sleep in place — the first need that requires reaching a specific *building*); once standing on it, start the `pray` atomic, whose effect zeroes piety (AtomicSystem, no goods consumed — like sleep, praying is free) — closing the rise→pray→reset loop. The drive sits **below eat + sleep** (survival outranks devotion); a devout settler with no temple anywhere falls through to normal work (piety stays pinned at ONE — a settlement with no temple can't pray, like the original). **Approximated:** (a) *which building is a temple* is inferred from the structural signature of the original's "work temple" (`HOUSE_TYPE_WORK_TEMPLE` = `logictype 37`, `logicmaintype 3`): a `kind === 'workplace'` building with **no recipe, no workers, no stock** (`isTemple`) — the building→pray-need link lives below the readable rule files (no "issatisfier" flag in `houses.ini`), so it is inferred like the food→eat-slot binding; (b) the **`PIETY_PRAY_THRESHOLD`=¾·ONE** trigger is unpinned, mirroring the eat/sleep triggers; both refine when the atomic `event` vocabulary is decoded + a content building→need binding is extracted + calibration lands (see Deviations). Hands-on: a devout settler a few cells from a temple walks over and prays through the real schedule, piety resets to 0, peak never breached ONE, 0 invariant violations, two seed-5 runs hash-equal. |
| Job-experience **accrual** (XP granted on a completed work atomic) | faithful (specialization key) / approximated (per-work amount + curve) | **Pinned (the key):** XP accrues into a `(job, good)` **specialization**, not just per job — the original's `humanjobexperiencetypes` tracks are keyed exactly that way (a track names its owning `job` and, when good-specific, the `good` it trains on; e.g. "collector wood" = job 8 + good 5). On a completed **work** atomic that yields a good (today the only one is `harvest`, which carries `goodType`), `grantWorkExperience` (`systems/progression.ts`, called from AtomicSystem's effect-apply — XP is event-shaped, and sim events are render-only) looks up the matching track for `(settler.jobType, goodType)` — preferring the good-specific track over the job's "general" one — and adds its `experienceFactor` to the settler's per-specialization XP (keyed by the track's `typeId` on `Settler.experience`). No track for the pairing → no XP (not every activity trains a specialization); no job / gone settler → no-op. **Approximated:** (a) *one grant of `experienceFactor` per completed work* is the basic "repetition builds expertise" accrual — the original's exact per-activity grant cadence has no readable rule (it lives in the same below-the-`.ini` logic the per-animation `event` deltas do); (b) the XP→**level curve** (how accrued XP and `baseRepeatCounter` map to a competence tier that gates output/speed) is **deferred** — captured raw in the IR, not yet interpreted, and is the *gating* half of the ProgressionSystem (the `progressionSystem` stub). Hands-on: the real `Simulation.step()` vertical slice (seed 7, 1000 ticks) → the woodcutter (job 1) accrues exactly **80 XP** in track 1 (8 wood harvests × `experienceFactor` 10), the carrier (pickup-only, no yield-bearing work) and the mill operator accrue nothing; behavior unchanged (golden atomic trace + 8-plank output identical — XP is new state, not a new action), golden hash moved deliberately `d1ac5fbe → f0edd147`. |
| Building-placement **tech gate** (`jobEnablesHouse` consumed at `placeBuilding`) | faithful (params) / approximated (the "present == alive in tribe" rule) | **Pinned (the params):** a house is enabled for a tribe once a settler of the enabling job is present — the original's `tribetypes` `jobEnablesHouse <jobType> <houseType>` edge, extracted verbatim into `TribeType.jobEnables` (see the edge-extraction row above). `buildingEnabled` (`systems/progression.ts`, called from CommandSystem's `placeBuilding`) reads those edges: a building with **no** `house` edge gating it is ungated (placed freely, e.g. the headquarters); one with gating edges is placeable only while a `Settler` of **any** enabling job is alive in the **same** tribe. A gated-out placement is a recoverable boundary failure — skipped but still command-logged, so replay stays faithful. A tribe absent from the content tribe table gates nothing (a map with no tribe-type data still places its start buildings). **Approximated:** "enabling job present == a settler of that job is currently alive in the tribe" is *our* minimal reading — the original may instead latch the unlock permanently once the job has *ever* existed (a tech-tree node, not a live-presence check), which has no readable oracle; refine when the schooling/`trainforjob` half lands and the original's persistence is observed. Only the **house** kind is consumed here; the sibling kinds land in their own gates (the `good` unlock in the production gate, the `vehicle` unlock in the carrier carry-capacity helper, the `job` unlock in the JobSystem assignment gate — all four kinds now consumed). Hands-on: compiled `dist` `Simulation.step()` — a `placeBuilding` smithy (gated by `jobEnablesHouse 2 4`) with no carpenter → **0 entities, command logged**; after spawning the carpenter then retrying → the smithy (type 4) places; a carpenter in a *different* tribe does **not** unlock it. |
| Experience **threshold** read side (`needfor*` consumed against accrued `Settler.experience`) | faithful (params + key) / approximated (the sum-of-named-tracks reading) | **Pinned (the params + the key):** a `needfor{job,good} <targetId> <amount> <expType>` line is the XP a settler must already have accrued to unlock the target — extracted verbatim into `TribeType.jobRequirements` (the threshold-extraction row above). `experienceRequirementMet` / `settlerMeetsNeed` (`systems/progression.ts`) read those `need` requirements against the settler's per-specialization XP, **keyed by the same `humanjobexperiencetypes` track typeIds** `grantWorkExperience` accrues onto `Settler.experience` — so the threshold consumes exactly the XP the accrual half produces (the join key is faithful, not invented). A target with no `need` requirement is unthresholded; one with several must clear every one. `train` requirements are skipped (a schooling COST paid at a training house — the JobSystem/school slice — not an accrued-XP threshold). **Now consumed by the harvest planner** (the `needforgood` row below); the `needforjob` half still awaits the JobSystem. **Approximated:** (a) a two-`expType` line is read as "sum the named tracks ≥ amount" — whether the original means "sum" or "either alone" has no readable oracle (same below-the-`.ini` XP logic the per-animation `event` deltas live in); (b) the threshold *value* (`amount`) is faithful but how it composes with `baseRepeatCounter` into a multi-tier competence curve (output quality/speed) is still deferred. Hands-on: the real game IR → **305 `need` requirements across 41 tribes**, their 26 distinct `expType`s spanning 3..75 (23 resolving to a real track typeId, the rest the 72/73/75 wider-id-space the extractor leaves unchecked); `experienceRequirementMet` on a real `needforjob 19 10 45` gates at the boundary (9 XP → false, 10 → true), is monotone non-decreasing (first met exactly at `amount`, never re-locks), and a `train` line is vacuously met (skipped). |
| Production **tech gate** (`jobEnablesGood` consumed at the production cycle-start) | faithful (params) / approximated (the "present == alive in tribe" rule, shared with the house gate) | **Pinned (the params):** producing a good is enabled for a tribe once a settler of the enabling job is present — the original's `tribetypes` `jobEnablesGood <jobType> <goodType>` edge, the sibling of `jobEnablesHouse` on the same `TribeType.jobEnables` graph. `goodEnabled` (`systems/progression.ts`, called from ProductionSystem's `canStartCycle`) reads those edges and shares the read side with `buildingEnabled` (one `tribeUnlockEnabled(kind, targetId)` helper): a good with **no** `good` edge gating it is ungated (made freely); one with gating edges is producible only while a `Settler` of **any** enabling job is alive in the **same** tribe. A workplace whose output good is gated-out simply never starts a cycle — inputs untouched, no waste — exactly like the worker-presence / capacity gates. **Approximated:** the same "present == alive in tribe" reading as the house gate (refine to a latched tech-tree node if the original's persistence is later observed); **all four** kinds are now consumed — `good` (here), `house` (placement gate), `vehicle` (carrier carry-capacity helper), and `job` (the JobSystem assignment gate, row below). Hands-on: compiled `dist` `Simulation.step()` over a sawmill (output PLANK gated by `jobEnablesGood 1 2`) with a carpenter operator on its tile but **no woodcutter** → **0 planks, wood untouched at 5, cycle never started**; spawn a woodcutter in the tribe → 1 plank produced, wood consumed; a woodcutter in a *different* tribe leaves it gated (0 planks); two seed-7 runs hash-equal (`bb6ef2e9`). |
| Harvest **XP-threshold gate** (`needforgood` consumed at the AI harvest planner) | faithful (params + key) / approximated (sum-of-named-tracks reading, shared with the threshold read side) | **Pinned (the params + the key):** a settler may only harvest a resource whose harvested good its **own accrued XP** clears (`tribetypes` `needforgood <good> <amount> <expType…>`) — the *who-may-do-it* gate, the per-**settler** sibling of the production-side tribe-presence `jobEnablesGood` gate (row above). `nearestHarvestableFor` (`systems/conflict/ai.ts`) now calls `settlerMeetsNeed(ctx, tribe, 'good', good, experience)` alongside the existing job-`allowedAtomics` gate: a resource whose good has an unmet `need` requirement is **not even targeted** (no MoveGoal, no atomic), so a low-XP settler is held out of the goods it hasn't trained for. The settler *trains* a good's track by harvesting it (`grantWorkExperience`), so the gate is self-consistent — XP accrues on the very work the threshold guards. An unthresholded good (no `needforgood`) is harvestable from 0 XP, so the gate is inert where no requirement exists (the slice fixture thresholds only PLANK, never harvested → the golden is untouched). **Approximated:** inherits the threshold read side's sum-of-named-tracks reading (row above) and its deferred `baseRepeatCounter` competence curve; the `needforjob` half (gating which *job* a settler may take) now lands in the JobSystem row below. Hands-on: compiled `dist` `Simulation.step()`, a wood good gated `needforgood 1 20 [1]` — a woodcutter with **19** wood-track XP → **0 harvests, tree untouched (remaining 5)**; the same with **20** XP → harvesting begins (tree 5→4 within 100 ticks); two same-seed runs hash-equal. |
| JobSystem **assignment + per-building binding** (idle settler takes an open workplace job, `needforjob` consumed; bound to its workplace) | faithful (params + key) / approximated (assignment policy) | **Pinned (the params + the key):** an idle settler (`jobType === null`) takes the worker job of an **open** workplace and is **bound to that specific building** (`JobAssignment{workplace}`) — the original's "assign colonists to buildings". `jobSystem` (`systems/economy/jobs.ts`, scheduled before `aiSystem` so the assignment takes effect same-tick) calls, for each idle settler in canonical order, `openJobAt`: the first same-tribe building whose type declares a `workers` slot (`logicworker <job> <count>` → `buildingWorkerJobs`) that is (a) **tech-enabled** for the tribe (`buildingEnabled`, the same `jobEnablesHouse` read side the placement gate uses), (b) **understaffed at that building**, AND (c) whose `needforjob` XP threshold the settler clears (`settlerMeetsNeed(ctx, tribe, 'job', jobType, experience)` — the *who-may-take-the-job* gate, the per-settler sibling of the harvest-side `needforgood` gate above, keyed on the **same** `humanjobexperiencetypes` track typeIds the accrual writes). This is the deferred ProgressionSystem `needforjob` consumer. The system WRITES `Settler.jobType` + adds the `JobAssignment` binding (and *adopts* a pre-employed-but-unbound settler standing on a workplace it staffs — binding it to the mill under its feet, so a settler spawned onto its station becomes authoritative with no behavior change); the assigned settler then walks to / staffs the workplace through the AI planner's walk-to-workplace drive (row below) + the binding-keyed staffs-here pin + the production worker-presence gate. **Inert in the golden slice's *trace*** (every golden/scenario/app settler spawns with an explicit non-null job, so no idle settler is ever assigned); the golden carpenter is *adopted* (gains a `JobAssignment` component → the canonical hash shifts f0edd147→469da255, the trace + 8-plank output unchanged — new state, not a new action). **Approximated (behavior, no oracle):** *which* open job an idle settler takes — first-in-canonical-order — is *our* deterministic policy; the original's actual job-dispatch (preference, distance, priority) is the undocumented settler-AI "soul". **Now per-building, not tribe-wide:** understaffing counts settlers *bound to that building* (`JobAssignment.workplace === building`) < the slot `count`, so two same-type workplaces staff **independently** (the prior tribe-wide head-count stand-in is gone). **Binding lifecycle — demolition closes it:** the `demolish` command (`systems/conflict/command.ts`) now *unbinds* every settler bound to the building before destroying it (`unbindWorkersOf` — drop the `JobAssignment`, reset `jobType = null`), so a worker isn't stranded latched to a dead workplace (the AI/JobSystem consumers only *defend* against a stale binding, none *cleared* it) — it returns to idle and the JobSystem re-employs it next tick (or same-tick, since `jobSystem` runs after `commandSystem`). This matches the obvious original behavior (pulling down a workshop turns its workers into job-seekers), pinned to that behavior, not to a data param. **The job-tech gate now lands too** — `openJobAt` also gates on `jobEnabled` (the `jobEnablesJob` row below), so **all four** `jobEnables` kinds (house/good/vehicle/job) are consumed and the `tribeUnlockEnabled` read side is complete; `needforjob` (the XP threshold) is the orthogonal per-settler gate, not a `jobEnables` edge. Hands-on: the REAL `Simulation.step()` schedule (seed 7) — TWO sawmills + two idle settlers → each settler is assigned `jobType = 2` (carpenter) and bound to a **distinct** mill (one carpenter per mill), each then walks to and latches onto ITS bound station; two runs hash-equal; demolishing a mill with one bound carpenter → the carpenter loses its binding + goes idle, and with a second mill present it is re-bound to that mill the next tick. Unit tests cover the per-building understaffing cap (1-slot sawmill → only the lowest-id idle settler takes it), the multi-slot fill (HQ's 3 woodcutter slots → exactly 3 assigned), the tech gate, the `needforjob` gate (29 XP → not assigned, 30 → assigned), and the demolish-unbind (a bound operator → idle + unbound after its workplace is demolished). |
| Job-assignment **tech gate** (`jobEnablesJob` consumed at `openJobAt`) | faithful (params) / approximated (the "present == alive in tribe" rule, shared with the house/good gates) | **Pinned (the params):** a worker job is unlockable for a tribe once a settler of the enabling job is present — the original's `tribetypes` `jobEnablesJob <jobType> <targetJob>` edge (a settler of `jobType` unlocks the *target* trade, e.g. a smith unlocking a weaponsmith), the **fourth and last** sibling of `jobEnablesHouse`/`jobEnablesGood`/`jobEnablesVehicle` on the same `TribeType.jobEnables` graph. `jobEnabled` (`systems/progression.ts`, called from `jobSystem`'s `openJobAt`) reads those edges through the **same** `tribeUnlockEnabled` helper the house/good/vehicle gates use: a job with **no** `job` edge gating it is an ungated start trade (any idle settler may take it); one that *is* gated is offered only while a `Settler` of **any** enabling job is alive in the **same** tribe. An idle settler is simply not offered a gated-out job — it stays idle (no `jobType` written, no `JobAssignment` bound) until the prerequisite trade exists, then the JobSystem assigns it next tick. This **completes the read side** — all four `jobEnables` kinds are now consumed (`tribeUnlockEnabled` covers house/good/vehicle/job). **Approximated:** the same "present == alive in tribe" reading as the house/good gates (refine to a latched tech-tree node if the original's persistence is later observed). Inert in the golden slice (its tribe carries no `job` edge → every job is ungated → the golden is untouched). Hands-on: the REAL compiled `dist` `jobSystem` (seed 7) over a sawmill with a carpenter slot — **(A)** no `job` edge → idle settler assigned `jobType = 2`, bound; **(B)** carpenter gated by `jobEnablesJob 1 2` with no woodcutter alive → idle settler stays `null`, unbound; **(C)** same gate with a woodcutter present → idle settler assigned `jobType = 2`, bound. Unit tests cover (B) the gated-out idle (no woodcutter → `null`) and (C) the unlock once an enabling-job settler exists. Real data: 110 `jobEnablesJob` edges across the 5 playable tribes (the edge-extraction row), all resolving (0 dangling). |
| Combat **hit resolution** (an `attack` atomic lands its blow at the ATTACK-event frame) | faithful (damage column + hit-frame params) / approximated (hitpoint pool; XP trigger; stagger interruptibility; need-drain scale) | **The combat BEHAVIOR** — the read-side `combatDamage` table now has a consumer. An `attack` `AtomicEffect` (`atomic.ts` → `resolveAttackHit`) subtracts its carried `damage` from the target's optional `Health{hitpoints, max}`, **clamped at 0** (a hit never heals). **The blow lands MID-animation at the ATTACK-event frame, not at completion:** the swing resolves the hit when its `elapsed` reaches the animation's `event <frame> 25` (`ATOMIC_ANIMATION_EVENT_TYPE_ATTACK`, `logicdefines.inc` l.745 — verified frames: sword_short @9 of 12, spear_iron @17 of 27, sword_long @16 of 29, bow_short @10 of 12), the follow-through then playing out to the swing's length; an animation with **no** ATTACK event **falls back to the completion frame** (the `hitAt`-absent path). **PINNED (the damage param + hit-frame):** the `damage` the effect carries is the **weapon's `damagevalue[targetMaterial]` COLUMN** (the target's armor `materialType`, or 0 unarmored) — armor selects the column, **no `blockingValue` subtracted** (the *Armor blockingValue engine role* row). So *how much a hit takes off* AND *when in the swing it lands* are data-pinned. A survivor is re-targeted next idle tick, so **swings repeat at the animation length** — the cadence IS the swing length, no invented cooldown. **APPROXIMATED (behavior, no oracle):** (a) the **hitpoint pool** — humans have NO readable `hitpoints` key (`jobtypes.ini` carries none; the value is below the `.ini`), only **animals** do (`animaltypes.ini` `hitpoints_adult` 200..20000, e.g. wolf 1000 / bear 7000 / mammoth 20000), so a combatant's max HP is a per-content stamp on the `Health` component (faithful for animals, unpinned for humans) on that same large-integer scale; (b) the **XP-accrual trigger** — a **damaging swing** accrues fight XP (the *Combat fight-XP* row); whether the original accrues per swing or per kill is unreadable, so per-damaging-swing is the deterministic reading; (c) the **stagger interruptibility** — a struck civilian gets its data-driven `82` flinch only when its current action is interruptible; the exact "can a blow interrupt X" table is below the `.ini`, so the `interruptable` flag is the deterministic reading; (d) the **need-drain scale** — the attack animation's `−20`/`−100` reserve drain is scaled onto the sim's 0..ONE need bar by the documented ~10000 reserve span (the exact max is unreadable); direction and the 5×(woman:soldier) ratio are faithful. The **death/cleanup** at 0 HP has landed (its own row); a 0-HP target is reaped the same tick. `Health` is a **separate optional component** (like `JobAssignment`/`Age`): only a combatant carries one, so the golden slice has none and the hash is untouched. Hands-on (`combat-cadence.test.ts`, synthetic-but-numerically-faithful soldier data): a plate-armored target takes **2090** from an iron spear (its material-4 column) and **950** from a long sword (the AP asymmetry — the mirror on chain); a spear swing drains the target at frame **17** of its 27-frame animation (untouched frames 1–16, one blow only, completes at 27); a no-ATTACK-event swing falls back to completion; a survivor is re-struck exactly **27 ticks** apart (cadence = length); two seed-7 squad runs hash-equal (goldens byte-identical — combat inert on the golden content). |
| Combat **fight XP** (a damaging swing trains the weapon class) | faithful (the accrual param + bucket ids) / approximated (the per-swing trigger; no level curve yet) | A **damaging** swing accrues fight XP (`atomic.ts` `resolveAttackHit` → `progression.ts` `grantFightExperience`) into the bucket for the swinging weapon's **class** — `WEAPON_MAIN_TYPE` → `JOB_EXPERIENCE_TYPE_FIGHT_*` (`logicdefines.inc` l.598: FIST 71 / SPEAR 72 / SWORD 73 / AXE 74 / BOW 75 / CATAPULT 76), on `Settler.experience`. **PINNED:** these are the **SAME expType id space the `needfor*` soldier-upgrade gates read** — the viking `needforjob` for the iron-spear soldier requires expType **72**, the long-sword soldier **73**, the long-bow soldier **75** (verified in the extracted `jobRequirements`) — so combat XP unlocks the better soldier classes through the existing `settlerMeetsNeed` gate with **no new mechanism** (the combat sibling of the `(job,good)` work-XP the *Job-experience tracks* row extracts). The accrual **rate** is the `soldier general` track's `experienceFactor` (type 69, factor 1/swing) — the fight buckets 71-76 carry no `HumanJobExperienceType` record of their own, so the rate comes from that general track. **APPROXIMATED (no oracle):** the **trigger** — per-damaging-swing vs per-kill is unreadable (OpenVikings' sim is a stub), so per-damaging-swing is the deterministic reading; and **saber** (`WEAPON_MAIN_TYPE_SABER` 4) maps to no fight bucket (no `FIGHT_SABER` in the data, no saber-soldier `needfor` reads one) → a saber swing trains nothing. Raw XP accrues only; the XP→**level→combat-stat** curve is a later calibration slice (the `baseRepeatCounter` / bonus effects stay deferred). Inert on the goldens (no `Health`-bearing settler ever swings → hash untouched). |
| Combat **stagger** (a struck civilian plays its `82` ATTACKED flinch) | faithful (the data-driven `82` binding) / approximated (the interrupt rule) | On a landed blow the **surviving** victim plays its `setatomic <job> 82 "..._attacked"` animation (`atomic.ts` `applyStagger`) — a `CurrentAtomic` carrying an **`idle`** effect (no state mutation) for the ATTACKED animation's length (viking woman/civilist 50 ticks). **PINNED / data-driven:** *which* classes flinch is read straight off the `82` binding — among the **playable** civilizations only the civilian classes (woman/civilist) bind it, so a playable soldier/hero never staggers; the **monster tribes** (weresnake/werewolf/bear-weresnake) also bind `82` for their creature-soldier classes (jobs 31/33/35 in `tribetypes.ini`), so a struck were-monster flinches too — the zero-per-job data-driven design working, not a special case. A class with no `82` binding is a no-op. Purely visual occupancy: the struck civilian visibly staggers and can't act for the flinch, then frees up — render consumes it later. **APPROXIMATED (no oracle):** the **interrupt rule** — the flinch replaces the victim's current action only when that action is **interruptible** (the extracted `interruptable` flag; a mid-swing or mid-flinch — both `interruptable 0` — is NOT cut short, so no stunlock), and a felled victim isn't staggered (it's being reaped). Whether the original interrupts on exactly this predicate is below the `.ini`; the flag is the deterministic reading. Inert on the goldens (no combat → no flinch). |
| Combat **swing need-drain** (an attacker pays the swing's rest/hunger cost) | faithful (the per-animation deltas) / approximated (the reserve→bar scale) | On swing completion the attacker pays the attack animation's **need cost** (`atomic.ts` `paySwingNeedCost`) — the animation's `event <at> {1,2} <delta>` REST/HUNGER channel drains (`atomicEventChannelDelta`), applied to the attacker's `fatigue`/`hunger`. **PINNED (the per-animation deltas):** read straight off the extracted `events` — a **soldier** swing carries `event 2 1 -20` + `event 2 2 -20` (−20 each), a **woman/civilist** swing −100 each (verified in the IR), so a woman's swing costs **5×** a soldier's; the direction (a reserve drain *raises* the need) and that ratio are faithful. The **first combat consumer** of the extracted event deltas (the *Atomic durations* row's `atomicEventChannelDelta`). **APPROXIMATED (no oracle):** the raw reserve delta is scaled onto the sim's 0..ONE need bar by the documented **~10000 reserve span** (`NEED_EVENT_RESERVE`) — the exact reserve max is unreadable (the *general* event-driven needs drive, which would replace the approximated per-tick rise/reset constants with these deltas everywhere, stays deferred); this drain is **scoped to combat atomics** only. Inert on the goldens (no combat → no drain). |
| Combat **death/cleanup** (`cleanupSystem` reaps a 0-HP combatant, emits `settlerDied`) | approximated (no oracle) / faithful (the "0 HP ⇒ dead and removed" shape) | The back half of the combat targeting+death loop: `cleanupSystem` (`systems/lifecycle/cleanup.ts`, runs **last** in `SYSTEM_ORDER`) destroys every entity whose `Health.hitpoints` has reached 0 and emits a `settlerDied{entity, cause:'damage'}` event for render/audio. **The faithful shape:** "a combatant at 0 hitpoints is dead and leaves the field" — running last means a lethal `attack` landed earlier this tick (the *hit resolution* row) is reaped in the **same** tick, so nothing downstream observes a 0-HP zombie and the corpse is gone by the snapshot render reads. **APPROXIMATED (no oracle):** *what* it reaps (the hitpoint pool) and *who* drained it (the targeting drive) are the approximated halves above — this system only removes a pool another mechanic emptied; the original's death animation/decay/corpse-good (`animaltypes` carcasses) and whatever else 0 HP triggers are below the readable data and deferred. The `cause` string is a render/audio hint, not simulated state — `'damage'` for every death today since a completed `attack` is the only thing that drains `Health`. Determinism: the dead set is COLLECTED (canonical ascending-id) before any `world.destroy` — mutating the store mid-`query` is a footgun (LESSONS [71f13ab]) and a canonical destroy order makes the death-event sequence reproducible; entity ids are monotonic/never recycled so removal can't confuse a later id. The reaped entity holds its own cross-references (a worker's `JobAssignment` points settler→building, never building→settler), so destroying it leaves no dangling binding (the reverse hazard — a *building* destroyed under a bound worker — stays handled at the `demolish` seam). Inert on the goldens/slice (no `Health`-bearing entity → no death → hash untouched, the [6264132] pattern). Hands-on: the REAL `Simulation.step()` schedule — a 1-tick `attack` for 100 damage against a 20-HP target drives it to 0 (AtomicSystem) and `cleanupSystem` reaps it the same tick: entity count `2→1`, target no longer alive, one `settlerDied{entity:1, cause:'damage'}` event on the snapshot; a non-lethal 35-damage hit leaves the survivor alive at hp 965 with 0 death events; two seed-7 runs hash-equal (`98808b76`). |
| Combat **targeting drive** (`combatSystem` picks a target + issues the `attack` atomic) | faithful (damage-column param + atomic id) / approximated (target acquisition + re-target trigger) | **The FRONT half of the targeting+death loop** — the genuinely-new "who attacks whom": `combatSystem` (`systems/conflict/combat.ts`, graduated from the stub) gives each idle, living **combatant** (a `Settler` carrying a `Health` pool — a fighter; the separate-optional-component pattern of `JobAssignment`/`Age`) a target. For an idle one (no `CurrentAtomic`, not travelling, `hitpoints > 0`) it finds the nearest **enemy** (`Health`-bearing settler of a *different, non-animal* tribe) within the attacker's weapon range and starts an `attack` `CurrentAtomic` carrying the `combatDamage`-resolved net damage — closing the targeting→attack→hit→death loop with the AtomicSystem `attack` effect (the hit) + `cleanupSystem` (the death). **PINNED:** (a) the **attack atomic id is 81**, read straight from the original `tribetypes` `setatomic <job> 81 "..._attack"` bindings (id 81 = the attack slot across every fighting job — `viking_soldier_attack_*`, `viking_hunter_attack`; verified in `DataCnmd/tribetypes12/tribetypes.ini`), the same `(jobType, atomicId)`→animation→`length` join every other atomic uses; (b) the **damage amount** is the weapon's `damagevalue[targetMaterial]` COLUMN (the target's armor material, or 0 unarmored), the same param `combatDamage`/the `attack` effect pin — armor SELECTS the column, with **no `blockingValue` subtracted** (the *Armor blockingValue engine role* row). The swing also carries the animation's ATTACK-event **hit-frame** and the weapon's **class** (for fight XP). The weapon's **reach is a `[minRange, maxRange]` band**, both verbatim `weapontypes` params: a target closer than `minRange` is too near to hit (a ranged weapon can't fire on an adjacent target — the real bows/crossbows/catapult, `minRange` 3/4/8) and one past `maxRange` is out of reach (see the *Hunter strike* row). The weapon resolves two ways, both data-pinned: a **settler with a job** by `(tribeType, jobType)` (the way the original binds a weapon to a job); a **jobless spawned animal** (`jobType: null` on an `isAnimalTribe` tribe — what `spawnAnimalHerd` places) by **`tribeType` alone**, because an animal's combat identity IS its tribe (each animal tribe carries essentially one attack weapon — `claw`/`bearfist`/`wolvefist`, all at `typeId 1`; the weapon's `jobType` in the data is the creature's monster combat-class, not a player-assignable trade). Without the tribe-keyed path a spawned aggressive animal would resolve no weapon and do **zero** damage despite `mayAttack` engaging it (the gap the *Animal herd spawn* row's approximation-(b) named, now closed). The first match in source-array order is the stable pick (an animal tribe's weapon set may have >1 row — the array-not-Map stance `combatDamage` documents). (c) the **player-vs-player scope** — a different-civilization combatant is an enemy. (The animal-exclusion this row originally described is now **superseded** by the *Combat civ-vs-animal aggression* row: targeting consults a single `mayAttack` relation that adds the civ⇄aggressive-animal cross-species fight; a passive/decorative animal is still left out, but an aggressive one is now engaged both ways.) The boundary is honest about the unknown case: a different-tribe combatant with **no record at all** (a synthetic enemy) is NOT an animal and stays a valid PvP enemy. **APPROXIMATED (no oracle):** (a) **target acquisition** — *nearest enemy in range* (canonical ascending-id tie-break) and "enemy == a different *civilization* tribe" are *our* deterministic combat design; the original's target-acquisition AI is the undocumented "soul" (Risks); (b) the **re-target trigger** — a survivor is re-acquired each idle tick (the swing itself now repeats at the animation length — the *hit resolution* row — so the cadence is data-pinned, but *that* it re-acquires nearest-in-range is our design); (c) the **target acquisition** now also consults an **owner (player) hostility axis** (two owned combatants of different players are enemies — see the *Combat hostility axis* deviation) and, for an OWNED combatant, a **walk-into-melee advance** drive has landed (an out-of-range enemy in sight is now chased into reach, then struck — see the *Combat walk-into-melee* deviation; the swing-in-place "stays put at range" behaviour this clause originally described now holds only for **unowned** wildlife). The nearest-enemy query is a bounded grid ring search over `TileBuckets` (the ROADMAP tier-3 consumer), not a full scan; (d) the damage now selects the column for **the target's worn armor material** (a settler may carry an `Armor` tier — the *Settler-side Armor stamping* row), no longer always material 0; *which* settler wears *which* class is caller-supplied, not yet a soldier-class→armor binding; (e) a combatant with **no resolvable weapon** does no damage — a **jobless civilian** is genuinely unarmed (only an animal tribe resolves a weapon without a job), and a settler whose `(tribe, job)` pair binds no weapon picks no target. Inert on the goldens/slice (no settler carries `Health` → the combatant scan finds nobody → hash untouched, the [6264132] pattern); the fixture grew a `test_axe` (tribe 1/job 1) + `viking_attack` binding plus an animal tribe 9 (`test_claw`/`wolf_attack`), but combat never fires without a `Health` pool. Hands-on: the REAL `Simulation.step()` schedule — (1) a viking (`test_axe`, 50 net dmg, range 2, `viking_attack` length 4) vs an unarmed frank (job 2, 120 HP): the viking starts the attack atomic (id 81) tick 1, the frank never retaliates (no weapon), 50 net per 4-tick swing drains it `120→70→20→0` and `cleanupSystem` reaps it at tick 13 (one `settlerDied`), viking unharmed at 1000; (2) a viking vs an **armed adjacent wolf** (tribe 9, animal): NEITHER swings (animal excluded both ways) — the wolf is skipped for being an animal, not for being unarmed; (3) a viking vs an adjacent **no-record frank** (tribe 2, absent from `content.tribes`): the viking still engages and reaps it (frank is NOT reclassified as wildlife); (4) a `spawnAnimalHerd` of an aggressive bear (tribe 10, **`jobType: null`**) next to a 300-HP viking, through the FULL `step()` schedule: the jobless bears resolve `test_bearfist` (40 net) **by tribe alone** and swing 36 attack atomics over 80 ticks, draining the viking 300→60 and killing it (1 `settlerDied`) while all 3 bears survive — the tribe-keyed animal weapon binding closing the spawn's no-damage gap. Unit tests cover nearest-in-range pick, friendly-fire-off (same tribe never a target), out-of-range skip, non-combatant (no `Health`) immunity, the unarmed/mid-swing/0-HP-corpse skips, the three animal-exclusion cases, the jobless-animal-resolves-by-tribe case, and the jobless-civilian-stays-unarmed case; two seed-7 runs hash-equal. |
| **Playable-tribes read view** (`playableTribes`/`isPlayableTribe` — the N controllable civilizations, distinguished from animals by the tech graph) | fidelity n/a (read view) | A pure content-only derived view (`systems/readviews/tribes.ts`, the analogue of `goodsGraph`/`combatDamage`): the **N data-defined tribes** scaffolding the ROADMAP Phase-4 item asks for, distinguishing the controllable civilizations from the animal/monster tribes **by the data alone, never a hardcoded name or count ("two")**. `content.tribes` is a flat list of every `[tribetype]` the pipeline extracts — the 5 civilizations AND the 36 animal/monster tribes. The distinguishing signature is the **tech graph**: only a civilization carries `jobEnables` edges (equivalently `{need,train}for*` `jobRequirements` — the two are co-present in the real data, 0 mismatches), an animal tribe being purely an atomic-binding vocabulary with `jobEnables.length === 0`. So `playableTribes` filters on a non-empty `jobEnables` graph (sorted ascending by `typeId` for a stable enumeration), and `isPlayableTribe(content, tribeType)` is the single-tribe membership predicate (an unknown id → false). Its complement `isAnimalTribe(content, tribeType)` returns true only for a **recorded** tribe with an empty tech graph — deliberately NOT `!isPlayableTribe`, because an **unknown** id (no record at all) is `!isPlayableTribe` yet must not be silently reclassified as wildlife; so for the 41 recorded tribes the two predicates partition exactly (XOR), while an unrecorded id is *neither*. The combat targeting drive consumes `isAnimalTribe` to keep an animal out of the player-vs-player enemy predicate (the *Combat targeting drive* row). Adds **no mechanic** (nothing produced/consumed/moved) and invents no classification — the split is read straight off the faithful `jobEnables*` param the pipeline pinned (the *Job→{good,…} tech-graph edges* row above). Hands-on: `playableTribes`/`isPlayableTribe` over the **real IR** via the `@vinland/sim` barrel → exactly **5 playable** (`viking#1, frank#2, byzantine#3, saracen#4, egypt#7` — sorted by typeId, not declaration order), `isPlayableTribe` agrees with the list for **all 41 tribes** (0 disagreements), unknown tribe 999 → false, and all 36 non-playable tribes carry 0 `jobEnables` (the data signature is clean). |
| **Ship-vehicle read view** (`shipVehicles`/`isShipVehicle`/`largestShipCapacity` — the `vehicle_ship` rows, distinguished from land carts by carrying passengers) | fidelity n/a (read view) | A pure content-only derived view (`systems/readviews/vehicles.ts`, the vehicle analogue of `playableTribes` over tribes): the **seed of the ROADMAP Phase-4 "Sea/Northland identity" item** (water travel, boats as mobile stores), classifying the ship/boat vehicle types out of `content.vehicles` **by the data alone, never a hardcoded name**. The discriminator is **`passengerSlots > 0`**: in the real `vehicletypes.ini` the only rows that carry passengers are the two ships (`ship small` `passengerslots 19`, `ship big` `passengerslots 9`) — every cart (`handcart`/`oxcart`) and the `catapult` list `passengerslots 0`. So `isShipVehicle(v)` is `v.passengerSlots > 0`, `shipVehicles(content)` filters that (sorted ascending by `typeId` for a stable enumeration), and `largestShipCapacity(content)` is the max `stockSlots` over the ships (the "boat as mobile store" hold — `ship small` 50, `ship big` 200) or 0 when none. We key on `passengerSlots` (the *semantic* "carries people = a boat") rather than `logicSize`/name; the two ships are **also** the only rows with `logicsize 2` (carts `0`, catapult `1`) and `logiccommander 24` (a ship pilot; carts `25`, catapult `31`) — three independent signals converge, so the reading is sound, not a fixture coincidence. Adds **no mechanic** (nothing embarks, no store moves) — the split is read straight off the faithful `passengerslots` param the pipeline pinned (the *Vehicle type table* row above). **The ship-unlock tech gate has now LANDED** (`tribeShipsUnlocked`, `systems/progression.ts`): the WORLD-aware sibling of `shipVehicles` — the ships a tribe has currently UNLOCKED, i.e. `isShipVehicle` ∩ the **same** `vehicle`-kind `jobEnablesVehicle` gate (`tribeUnlockEnabled`) `carrierCarryCapacity` runs, sorted by `typeId`. A ship is unlocked when no edge gates its `typeId` (an ungated start ship) OR a settler of a gating job (e.g. a shipwright) is alive in the tribe; an absent tribe gates nothing. It composes the two already-pinned halves (the `passengerslots` ship split + the `jobEnablesVehicle` edge) and adds **no new mechanic** (no hull spawned, nothing embarks) — the carrier→vehicle pairing / boats-as-mobile-store ENTITIES stay deferred (the *Carrier* row; Deviations — *Carrier→vehicle pairing*). Hands-on: `shipVehicles`/`largestShipCapacity`/`tribeShipsUnlocked` over the **real IR** via the `@vinland/sim` barrel → exactly **2 ships** (`ship_small#3 stock50, ship_big#4 stock200` — sorted by typeId, not declaration order) out of the 6 real vehicles, the 2 carts + catapult excluded, `largestShipCapacity = 200`. The real ships are BOTH gated (`jobEnablesVehicle` job 9 enables ships 3 & 4; jobs 22/26 also gate ship 3), so a tribe with no settlers fields **zero** ships (the gate bites, unlike the ungated oxcart); spawning a job-9 settler flips the viking unlocked set `[] → [3,4]`, and an absent tribe (9999) → `[3,4]` (gates nothing). |
| **Vehicle cargo allow-list read view** (`vehicleCargoGoods`/`vehicleMayCarry` — the goodtypes a hold may carry) | fidelity n/a (read view) | A pure content-only derived view (`systems/readviews/vehicles.ts`, sibling of the ship-vehicle row above) over the now-extracted `VehicleType.cargoGoods` (the `logicgood` allow-list, *Vehicle type table* row): `vehicleCargoGoods(v)` is the carryable-goods **membership `Set`** and `vehicleMayCarry(v, goodType)` the single-good predicate — the **load gate** the Sea/Northland boat-as-mobile-store (and a cart) checks before accepting cargo. Generic over any vehicle, not ship-only (a cart's hold is filtered the same way). The split is read straight off the faithful `logicgood` param the pipeline pinned; **adds no mechanic** (nothing loads yet — the carrier dispatch still does NOT apply the filter, Deviations — *Carrier→vehicle pairing* (a)). Determinism: a pure `Set`/`includes` over the extracted array; callers test membership (`.has`/`includes`), order-independent, so no canonical-iteration concern; no world, no RNG. Hands-on: over the **real IR** via the `@vinland/sim` barrel — both ships and all carts carry 49 cargo goods (so `vehicleMayCarry(ship, 16) === true`), the catapult's set is empty (`vehicleCargoGoods(catapult).size === 0`, nothing rides in it). |
| **Weapon-classification read view** (`isRangedWeapon`/`rangedWeapons` + `isSiegeWeapon`/`siegeWeapons` off `munitiontype`/`damagetype`; `weaponClassOf`/`weaponsByClass` off `mainType`) | fidelity n/a (read view) | A pure content-only derived view (`systems/readviews/combat.ts`, the weapon analogue of `isShipVehicle`/`shipVehicles` over vehicles): the first CONSUMER of the previously-unread `WeaponType.munitionType`/`damageType`/`mainType` markers (the *Armor + weapon type tables* row's weapon-side extracts), classifying the weapon table by those markers **by the data alone, never a hardcoded weapon name**. `isRangedWeapon(w)` is `w.munitionType !== undefined` — in the real `weapons.ini` only ammo-firing rows carry a `munitiontype` (1 = bow ammo, 2 = catapult projectile), so its mere presence is the data-pinned "this weapon shoots" marker; `isSiegeWeapon(w)` is `w.damageType !== undefined` — only the catapult carries a `damagetype` (the siege/AoE class, value 2), so its presence marks "deals area damage". `rangedWeapons(content)`/`siegeWeapons(content)` `filter` those predicates, returned as **arrays in `content.weapons` source order** — NOT keyed by `typeId`/`(tribeType,typeId)`, which recur per tribe and are reused by animal weapons (the lossless array stance `combatDamage` keeps; a keyed collection would silently drop records). Siege ⊆ ranged (a catapult fires `munitiontype 2`), but the converse fails (a bow is ranged, not siege) — two independent classifications. Adds **no mechanic** (nothing is fired, no AoE resolved) — the split is read straight off the faithful `munitionType`/`damageType` params the pipeline pinned; the *ranged-attack / siege-resolution behavior* (fire-from-afar, area damage) is the still-unbuilt, oracle-blocked half this view seeds. Determinism: a pure `filter` over the plain `content.weapons` array (a fresh array, shared content never mutated); no world, no RNG, no wall-clock. **The third, multi-valued marker now also lands** (`weaponClassOf`/`weaponsByClass`): unlike the two *presence* markers above (each absent on most weapons → a binary filter), `mainType` is a class enum *every* weapon carries (1..7: fist/club/sword/axe/spear/bow/catapult), so its read view is a **grouping** rather than a filter. `weaponClassOf(w)` is `w.mainType` (the field accessor, `undefined` only on a malformed/partial row); `weaponsByClass(content)` partitions `content.weapons` into a `Map<mainType, WeaponType[]>` — one bucket per class, **lossless** values (arrays in source order, the same `combatDamage` stance, since a weapon's `(tribeType,typeId)` isn't unique), a weapon with no `mainType` simply omitted (no `undefined` bucket). The Map's iteration order is insertion = first-class-appearance (the [cef9629] non-canonical-but-order-independent read-view idiom — a display consumer sorts the keys itself). Adds **no mechanic** — the soldier-class→weapon-class roster binding the deferred combat-roster slice will join on this is still unbuilt; this is the third faithful classification seed off the pinned `mainType` param. Hands-on: `rangedWeapons`/`siegeWeapons` over the **real IR** via the `@vinland/sim` barrel → **30 ranged** (25 bows `munitionType 1` + 5 catapults `munitionType 2`) and **5 siege** (the catapults, one per tribe 1/2/3/4/7, all named "catapult"), siege ⊆ ranged confirmed, a `fist`/`sword` is neither, no record dropped (105 weapons in, source order preserved); `weaponsByClass` over the **freshly re-run pipeline IR** → all **105 weapons bucketed** into **7 classes** `{1:25, 2:15, 3:20, 4:10, 5:5, 6:25, 7:5}` (exactly the source `weapons.ini` `mainType` line counts), **lossless** (105 bucketed = 105 total), **0 dropped** (every row carries a `mainType`), class-7's first weapon is the catapult. **The armor-side twin now completes the symmetry** (`armorClassOf`/`armorByClass`): the same multi-valued-`mainType` grouping over `content.armor` (`Map<mainType, ArmorType[]>`, lossless array values in source order), so both combat tables expose their coarse class identically. Note armor's `mainType` (`1` = light/cloth+leather, `2` = heavy/chain+plate) is a *different* axis from the `armorClass` `combatDamage` joins on (that is the per-record `typeId`/`damagevalue` index 1..N); `armorByClass` partitions by the coarse material tier several records share. Adds no mechanic — the soldier-class→armor-tier binding the deferred combat-roster slice joins on is still unbuilt. Hands-on: `armorByClass` over the **real IR** via the `@vinland/sim` barrel → all **4 armor records bucketed** into **2 classes** — class 1 (light): `woolen_armor`, `leather_armor`; class 2 (heavy): `chain_armor`, `plate_armor` (the `mainType {1,1,2,2}` shape read straight from `armortypes.ini`), **lossless** (4 bucketed + 0 unclassified = 4 total). |
| **Boat hull as a mobile store** (`placeBoat` command → a `Vehicle` hull entity carrying a `Stockpile`; the cargo-LOAD gate) | faithful (params: ship split + unlock gate + cargo-load filter) / deferred (movement, embark, water valency) | **The "boats as mobile stores" ENTITY** the ROADMAP Phase-4 Sea/Northland item names — the boat analogue of `placeBuilding`. A `placeBoat{vehicleType,x,y,tribe}` command (through the single CommandSystem mutation seam, `systems/conflict/command.ts`) creates a placed **`Vehicle{vehicleType,tribe}`** hull at (x,y) carrying an **empty** `Stockpile` (the mobile store — a hull is loaded by hauling cargo to it, not pre-seeded like a headquarters; its hold CAPACITY is the ship's `stockSlots`, read off the `VehicleType` like `largestShipCapacity`), and emits `boatPlaced`. **PINNED:** placement is gated by **`tribeShipsUnlocked`** (the ship read-view row above) — only a `vehicle_ship` row (`passengerSlots > 0`, the faithful `passengerslots` split) the tribe has currently UNLOCKED (its `jobEnablesVehicle` edge satisfied) is fielded; a cart, a catapult, an unknown id, or a not-yet-unlocked ship is a recoverable bad command — skipped (still logged so replay stays faithful), the same stance as a tech-gated `placeBuilding`. So both axes the entity reads are pinned to the extracted vehicle IR (the ship/cart split + the unlock edge). **The cargo-LOAD gate now LANDED:** depositing a hauled good INTO a hull's `Stockpile` is filtered by the ship's `VehicleType` — `stockCapacity` (`systems/shared.ts`) gained a Vehicle branch, so a good on the ship's `cargoGoods` (`logicgood`) allow-list gets the whole `stockSlots` hold capacity (it may ride) and a good NOT on the allow-list gets capacity 0 (refused — a carrier never deposits a forbidden good into the boat). The existing `nearestStoreFor` store scan + the `pileup` deposit route through `stockCapacity` unchanged, so the whole load path inherits the filter with **no new system** — the *load half* of the empty hull (`placeBoat` set up the store; this gates what fills it). The `stockSlots` total is applied as a per-good upper bound (a faithful upper bound; the whole-hold-shared-across-goods cap is a deferred refinement). Distinct from the carrier→cart *carry-capacity* path (the *Carrier* deviation (a)): that is how big a BATCH a carrier hauls, still deferred; this is what a boat hull may ACCEPT, now applied. **DEFERRED (no mechanic yet, no oracle):** the hull is still a **static placed store** — boat movement, passenger embark/disembark atomics, and water-valency terrain (which cells a ship floats on — map-decode-blocked) are follow-up slices this hull-entity unblocks. Inert on the goldens/slice (no content places a boat → no `Vehicle` exists → hash untouched, the separate-component pattern). Hands-on: the REAL `Simulation.step()` schedule (`place-boat.test.ts` + `boat-cargo-load.test.ts`, seed 1) — an ungated big ship (typeId 4) placed for the viking → one `Vehicle{4,viking}` hull at (7,8) with an empty `Stockpile` + a `boatPlaced` event; a tech-gated small ship (typeId 3) is **skipped until** a shipwright (job 10) is spawned then **placed**; a land cart / unknown id / wrong-tribe's-shipwright case each places **zero** hulls (still logged); a carrier hauls a carryable plank INTO a hull (deposited, capped at the hold), a non-carryable good gets capacity 0 (refused), and the hold never over-fills; over the **real IR** `stockCapacity(ship_big#4)` resolves **200 for carryable good 16 / 0 for forbidden good 18** (the real 49-good `cargoGoods` allow-list + the 200 `stockSlots` cap drive the gate); two runs hash-equal. |
| **Sea-job read view** (`seaJobs`/`isSeaJob` — the `fisher_sea`/`trader_sea` water trades, distinguished from their land counterparts by the `_sea` id suffix) | fidelity n/a (read view) | A pure content-only derived view (`systems/readviews/jobs.ts`, the job-side analogue of `shipVehicles` over vehicles): the **job-side seed of the ROADMAP Phase-4 "Sea/Northland identity" item** (the `fisher_sea`/`trader_sea` trades it names), classifying the water-borne jobs out of `content.jobs` **by the data alone, never a hardcoded list**. The discriminator is the **`_sea` id suffix** the original `jobtypes` data carries: a sea job is a *distinct jobtype* with its own `typeId` whose only extracted distinguisher from its land counterpart is the name — the sea variants carry the same `baseAtomics [6]` and an **empty** `allowedAtomics` (unlike the land `fisher`'s `36/37/38` cast/catch atomics), because their sea-work atomics are bound per-tribe via `tribetypes` `setatomic`, not listed in `jobtypes`. So the name IS the data's own "this trade works from the water" signal, not an invented classification (the [3826bab] *verify the satisfier is distinguishable BEFORE planning* check confirmed no other extracted param splits sea from land). `isSeaJob(j)` is `j.id.endsWith('_sea')`, `seaJobs(content)` filters that (sorted ascending by `typeId` for a stable enumeration). Adds **no mechanic** (nobody fishes from a boat, nothing embarks) — the split is read straight off the faithful job `id` the pipeline pinned. Determinism: a pure function of `content` over the plain `content.jobs` array, explicitly **sorted** by `typeId`, so the same content yields a byte-identical array every call; no world, no RNG, no wall-clock. Hands-on: `seaJobs`/`isSeaJob` over the **real IR** via the `@vinland/sim` barrel → exactly **2 sea jobs** (`fisher_sea#23, trader_sea#26` — sorted by typeId, not declaration order), `isSeaJob` true for both sea variants and false for their land counterparts (`fisher#22`/`trader#25`), and **no other** of the 55 jobs matches the suffix (the data signature is clean); byte-stable call-to-call. |
| AI **walk-to-bound-workplace drive** (an assigned operator physically reaches ITS station) | approximated (behavior, no oracle) | **The movement half the JobSystem assignment (row above) promised:** a freshly-assigned operator can be standing anywhere — and a pure-operator job (e.g. the carpenter) harvests nothing, so without this it would idle forever and never staff its workplace. `atomicPlanner` (`systems/conflict/ai.ts`) now, for an employed empty-handed settler **not** already on its bound station (`staffsBoundWorkplaceHere` didn't fire) and below the eat/sleep/pray drives + after depositing any load, sets a `MoveGoal` to the **building the JobSystem bound it to** (`JobAssignment.workplace`) via `boundWorkplaceTarget` — provided the binding is still usable (same-tribe **producing** workplace that declares its `jobType` in `workers`, tech-enabled, positioned; else treated as "no station" so the settler isn't stranded). The existing MoveGoal→PathRequest→PathFollow chain then walks it there; once standing on the tile the binding-keyed `staffsBoundWorkplaceHere` pin holds it and the production worker-presence gate runs. A job a resource permits (a woodcutter) still reaches harvest below when it has no binding (the helper returns null), so harvesters are unaffected. **Inert in the golden slice** (its lone operator, the carpenter, spawns already on the sawmill → it is *adopted*-then-pinned, this drive never fires; the only golden change is the adopted carpenter's new `JobAssignment` component — see the row above). **Approximated (behavior, no oracle):** the operator heads for **its own** mill (the binding), not the nearest — which keeps a worker latched to *its* workplace across a brief step-off the tile and lets two same-type workplaces staff independently (the prior "nearest-unstaffed" + tribe-wide-headcount stand-in is gone). The *initial* choice of which mill each settler binds to is the JobSystem's first-canonical-order policy (row above), still *our* deterministic stand-in for the original's undocumented dispatch. Hands-on: the REAL `Simulation.step()` schedule (seed 7) — two idle settlers + two sawmills → each is bound to a distinct mill and walks to/latches onto **its own** station (ended ON its bound mill's tile, not the nearer one); two runs hash-equal; unit tests cover the walk to the bound mill, the pin (an operator on its bound tile gets no MoveGoal), an unbound operator idling (no station yet), heading for ITS mill even when a nearer same-type mill exists, and that a woodcutter still prefers harvesting. |

| Combat **civ-vs-animal aggression** (`combatSystem` drives an aggressive animal vs a civ, both ways) | faithful (aggression flags + provoked-anger flags/duration + net-damage param + atomic id) / approximated (target acquisition) | **The cross-species half of the targeting drive** (the *Combat targeting drive* row's animal-exclusion is now superseded): instead of skipping every animal, `combatSystem` consults a single hostility relation `mayAttack(content, attacker, target)` (`systems/readviews/tribes.ts`) for both attacker-eligibility and per-candidate targeting, so a civ⇄animal fight is consistent in both directions. **PINNED:** (a) **which animals are hostile** is the verbatim `animaltypes.ini` flags — an animal runs an attack drive iff its record sets `aggressive` (`isAggressiveAnimal`, the unprovoked-attack driver), and a civ may target an animal iff that animal is `aggressive` AND not `cannotbeattacked` (`animalCannotBeAttacked` — the decorative-fauna/bee exemption); (b) the **net-damage amount** and **attack atomic id 81** are the same pins as the PvP targeting drive (the `weapontypes`×`armortypes` join `damage["0"]`, and the `setatomic <job> 81` slot). The relation's rules: same tribe → friendly; two animals → no fight (no inter-species wildlife-aggression oracle); civ↔civ → enemies (PvP); civ→animal → only an aggressive, non-exempt animal; aggressive-animal→civ → enemy. The **`hitpointsAdult` stamp source** is now read by `animalHitpoints(content, tribeType)` (the `Health`-component value a spawned animal gets — e.g. a bear's 15000), faithful to the extracted `hitpoints_adult` param. **The PROVOKED-anger half is now wired** (the last `animaltypes.ini` aggression input): a struck **`getAngry`** (but not `aggressive`) animal — a boar/deer hit on its `Health` — is stamped a per-entity **`Anger{until: tick + angryGameTime}`** timer at the AtomicSystem provocation point (`provokeAnger`, reading `isProvokableAnimal`/`angryGameTimeOf`); while the timer is live `combatSystem` treats it like an aggressive animal — it fights the civ **back** (`hostileAnimalNow`) and the civ may target it (`mayTarget`), both directions consistent — then it reverts to passive when the timer lapses (the lapsed component is reaped on the attacker scan). A re-strike **refreshes** the timer, so a continuously-harassed animal stays hostile and one left alone cools off. The flags (`getangry`) and duration (`angryGameTime`) are the verbatim extracted params. **APPROXIMATED (no oracle):** (a) **target acquisition** (nearest enemy in range, ascending-id tie-break) + **swing cadence** + **in-place strike at range** (no walk-into-melee) are the same `approximated` design as the PvP drive; (b) "a civ engages an animal only when it is *aggressive* (hostile) [or provoked]" vs *hunting* passive prey (a deer/cow — the separate `catchable`/hunter mechanic, now landed — see the *Hunter strike on catchable prey* row) is our split — and *what first provokes* a passive `getAngry` animal is **now** the hunter strike (a hunter striking `catchable getAngry` prey stamps the `Anger` timer); (c) animals don't fight each other. Inert on the goldens/slice (no settler carries `Health` → the combatant scan finds nobody → hash untouched). Hands-on: (aggressive) the REAL `Simulation.step()` schedule (seed 1, 5×1 grass) — an aggressive bear (tribe 10, `hitpoints_adult` 15000, `test_bearfist` 40 net dmg) next to a viking trades blows, the **bear is felled** and reaped; a PASSIVE wolf neither attacks nor is attacked; a `cannotBeAttacked` bee is exempt; two animals don't fight. (provoked) the REAL `step()` schedule — a passive `getAngry` boar (tribe 12, `angryGameTime` 10, `test_tusk` 30 net) struck at tick 0 → `Anger{until:11}`, it **swings back** at the viking ticks 1..3; with the viking removed (no re-strike) the timer holds at 11 and the boar **reverts to passive at tick 11**. Unit tests cover all five static hostility cases + the read views + the provoke→fight-back→lapse→refresh lifecycle. |
| Animal **herd spawn/placement** (`spawnAnimalHerd` command places a group of creatures on the map) | faithful (group size + HP + birth-range + leader-presence params) / approximated (scatter pattern + jobType-null + one-shot placement) | **The animal-placement mechanic** the ROADMAP Phase-4 "animals as non-controllable tribes" item names — it finally *puts creatures on the map*, consuming the `herdParams`/`animalHitpoints` read views the prior slices landed. The new `spawnAnimalHerd{tribe,x,y}` command (`systems/conflict/command.ts`, the animal analogue of `spawnSettler`/`placeBuilding` on the one mutation seam) places `max(1, maximumgroupsize)` creatures of the animal `tribe`, each a `Settler` of that tribe carrying a `Health` pool stamped from `animalHitpoints` (`hitpoints_adult`), scattered within `maximumdistancetobirthpoint` of (x,y); when the animal's `searchforleader` is set the herd's lowest-id member is the **leader** and every member records the relation via the new optional `HerdMember{leader}` component (the leader self-referential). **PINNED (the params):** the **group size** (`maximumgroupsize`), the **HP pool** (`hitpoints_adult` via `animalHitpoints`), the **birth-point range** (`maximumdistancetobirthpoint`), and **whether there is a leader at all** (`searchforleader`) are the verbatim-extracted `animaltypes.ini` params — so *how many*, *how tough*, *how far they range*, and *whether they herd* are data-faithful. A non-animal tribe (a civilization — no `animaltypes` record) is bad input and skipped (still command-logged → faithful replay), the same recoverable-boundary stance as an unknown building/job id. **APPROXIMATED (no oracle):** (a) the **scatter pattern** — *where within the range* each creature lands (a deterministic expanding 8-direction ring, no RNG) is *our* design, not pinned (the original's spawn-point/territory placement is below the readable data); (b) creatures spawn at **`jobType: null`** — an animal isn't born into a trade. (This is no longer a *no-damage* gap: the combat targeting now resolves a jobless animal's weapon by **tribe alone** — see the *Combat targeting drive* row — so a spawned aggressive animal *does* do damage; only the placement specifics here stay approximated.) (c) the spawn is a **one-shot placement** — no per-tribe spawn cadence, respawn, or territory upkeep. (The `HerdMember` relation is *recorded* here and now **read** by the follow-the-leader drive — see the *Animal follow-the-leader cohesion* row below — but the rest of the herd-cohesion AI is the undocumented "soul".) An animal is spawned **adult** (no `Age` component → no growth). The **map populator** that *issues* these commands (seeding a map's wildlife) is a later slice. Inert on the goldens/slice (no `spawnAnimalHerd` is issued there → no animal entity → hash untouched, the separate-optional-component pattern of `Health`/`HerdMember`). Hands-on: the REAL compiled `Simulation.step()` schedule — a `spawnAnimalHerd` for an aggressive bear (`maximumGroupSize` 4, `searchForLeader`, range 2, `hitpoints_adult` 15000) → **4 creatures**, leader on the birth point (5,5) + followers at (6,5)/(4,5)/(5,6) all within range 2 and no two stacked, each HP `15000/15000` and `leader=1`; a solitary bee (no group size) → **1 creature**, HP `200/200`, **no** `HerdMember`; a viking (civilization) herd command → **0 spawned, still logged**; two same-seed runs hash-equal (`945e8b75`). Unit tests cover the group placement, scatter-in-range, leader designation, the solitary no-leader case, the non-animal skip, and determinism. |
| Animal **follow-the-leader cohesion** (`herdingSystem` keeps a strayed follower near its herd leader) | faithful (cohesion-radius param) / approximated (walk-back-to-leader behavior, no oracle) | **The first reader of the `HerdMember` relation** the spawn slice recorded — the movement drive that holds a pack together. `herdingSystem` (`systems/movement/herding.ts`, runs just before `aiSystem` so its `MoveGoal` is routed by the same-tick navigation planner) gives each idle **follower** (a `HerdMember` whose `leader !== self`, at rest — no `CurrentAtomic`, not already travelling) that has strayed farther than its `animaltypes.ini` `maximumleaderdistance` from its leader a `MoveGoal` to the **leader's current cell**; the existing MoveGoal→PathRequest→PathFollow chain then walks it back, and it comes to rest once inside the radius. The **leader itself** (`leader === self`, the self-referential spawn marker) and a **solitary** animal (no `HerdMember`) run no drive. **PINNED:** the **cohesion radius** is the verbatim-extracted `maximumleaderdistance` param (surfaced via `herdParams().leaderDistance`) — *how far* a follower may stray is data-faithful (a `maximumleaderdistance` of 0 reads literally as "stay on the leader's cell"). **APPROXIMATED (no oracle):** that a strayed follower walks straight back **to the leader's cell** (rather than flocking to an offset/formation, or wandering while still near) is *our* deterministic design — the original's herd-cohesion AI is the undocumented "soul"; a reaped leader (killed in combat → no `Position`) leaves the follower in place (no re-designation yet). Determinism: no RNG/wall-clock; followers visited in canonical store order, integer leader-distance reads, the only mutation an idempotent `MoveGoal` add. Inert on the goldens/slice (no entity there carries a `HerdMember` → the follower scan finds nobody → hash untouched, the separate-optional-component pattern). Hands-on: the REAL `Simulation.step()` schedule (seed 1, 20×1 grass) — a `spawnAnimalHerd` bear pack (3, `maximumLeaderDistance` 3) with one follower dragged to x=15 (leader at x=0): the next step issues a `MoveGoal` to the leader's cell, and over 61 steps the follower walks back to x=0 and **rests** inside the radius; two same-seed runs hash-equal (`2d2babbf`). Unit tests cover the strayed-follower send-back, the in-range no-op, the leader-runs-no-drive case, the no-interrupt-mid-atomic/already-travelling case, the reaped-leader no-op, the full-`step()` walk-back-and-rest, and determinism. |
| Animal **locomotion pace** (`spawnAnimalHerd` stamps a `MoveSpeed`; `movementSystem` walks each path-follower at its own pace) | faithful (the `movespeed` magnitude) / approximated (the direction of the scale — larger = slower) | **The first consumer of the `locomotionOf` read view** the prior slice surfaced — a creature now *walks* at its data-pinned speed instead of the universal settler pace. A new optional `MoveSpeed{perTick}` component (`components/index.ts`) carries a per-entity walking pace; `spawnAnimalHerd` (`systems/conflict/command.ts`) stamps each herd creature `MoveSpeed{perTick = ONE/movespeed}` when its `animaltypes.ini` record sets `movespeed` (via `locomotionOf().walkSpeed`), and `movementSystem` (`systems/movement/movement.ts`) advances a `PathFollow` carrying a `MoveSpeed` by *its own* `perTick` (else the universal `MOVE_SPEED_PER_TICK`), threading the speed through the drift-free `stepToward` arrival-snap unchanged. So a boar (`movespeed 8`) grazes at `ONE/8 = 0.125` tile/tick (which the settler default has since been recalibrated to match — see the *Movement step speed* row) while a creature whose record omits `movespeed` (walkSpeed 0) carries no `MoveSpeed` and walks the default. **PINNED:** the **pace magnitude** is the verbatim-extracted `movespeed` param — *how fast* relative to the settler base is data-faithful (the integer `ONE/movespeed` truncates deterministically, no accumulated drift since the arrival-snap absorbs the residual the same way `MOVE_SPEED_PER_TICK` does). **APPROXIMATED (no oracle):** the **direction of the `movespeed` scale** — that a *larger* number is a *slower* step (the step-period reading) rather than a higher velocity. This is the only reading consistent with the data's `runspeed < movespeed` on every animal that sets both (a "run" gait should be *faster*, so a smaller `runspeed` number = fewer ticks/tile = the quicker flee), and `movespeed` is absent from the OpenVikings oracle (its sim is a stub), so faster-vs-slower can't be pinned. A `movespeed` of 0 = the engine default (= the settler pace) by omission. **RUN GAIT NOW STAMPED (data on the entity; drive deferred):** `MoveSpeed` gained a second field `runPerTick` = `ONE/runspeed` (or `null` when the record omits `runspeed`), stamped at spawn from `locomotionOf().runSpeed` — so a creature with a `runspeed` (e.g. `animal_13`, `movespeed 10`/`runspeed 8` → `runPerTick = ONE/8`, the *faster* gait since `runspeed < movespeed`) now *carries* its run pace, while the walk pace (`perTick`) is what `movementSystem` reads. This is the "data on the entity before its consumer" discipline (as `Armor`/`cargoGoods` landed): the **magnitude is faithful** (verbatim `runspeed`), but the **flee/charge DRIVE that switches the mover to `runPerTick`** — *when* an animal runs vs walks — is still **deferred (no oracle)**: it is undocumented "soul" behaviour (a startle/pursuit AI the readable `.ini` and the stub oracle don't carry), so the field is **inert today** (the mover never reads it). Wiring that drive is a later slice; this step only ensures the param needn't be re-extracted then. Determinism: a pure content read at spawn (`fx.div(ONE, fromInt(movespeed))` / `runspeed`), no RNG/wall-clock; both paces are `Fixed`s minted via `fx.*` so they hash like every other component (a `null` `runPerTick` hashes as the absence). Inert on the goldens/slice (no entity there carries a `MoveSpeed` → movement reads the universal pace → hash untouched, the separate-optional-component pattern of `Health`/`HerdMember`). Hands-on: the REAL decoded IR (`content/ir.json`, 9/35 animals set `movespeed`, 5/35 a `runspeed`) through the compiled `@vinland/sim` barrel — a `spawnAnimalHerd` boar (tribe 9, `movespeed 8`, group 6) stamps each creature `MoveSpeed.perTick 8192 = ONE/8` (0.125 tile/tick), driven by a `MoveGoal` through the full `Simulation.step()` schedule (planner→pathfinder→movement) advances exactly 0.125/tick (`0 → 0.125 → 0.25 → …`), reaches x=8 at tick 64 (8 tiles × 8 ticks/tile), two same-seed runs hash-equal (`962f6e9c`); and `animal_13` (`movespeed 10`/`runspeed 8`) stamps each of its 6 creatures `perTick 6553 = ONE/10` **and** `runPerTick 8192 = ONE/8`, while `animal_9` (`movespeed 8`, no `runspeed`) carries `runPerTick null`. Unit tests cover the per-entity-pace step (`ONE/8` vs the default `ONE/4`), the eight-step one-tile traversal, the `movespeed`+`runspeed` stamp on each creature, the `runspeed`-omitted→`runPerTick null` case, and the no-`movespeed`→no-`MoveSpeed` case. |
| Animal **map populator** (`seedAnimalHerds` produces the `spawnAnimalHerd` commands that seed a map's wildlife) | faithful (the set of animal tribes) / approximated (where + how many birth points — no per-map oracle) | **The seam that finally puts wildlife on a *real loaded map*** — the AnimalSystem/map-populator the `spawnAnimalHerd` command's doc named as "a later slice". `seedAnimalHerds(content, terrain, options?)` (`packages/sim/src/populate.ts`) is a **pure function**, NOT a per-tick system: given `content` + a `TerrainGraph` (or a raw `TerrainMap` it builds) it returns an ordered list of `spawnAnimalHerd` {@link Command}s, which a caller (a scenario / the app's map loader) enqueues through the one mutation seam — exactly like a UI issuing `placeBuilding`. Seeding happens once at map load, not every tick, and a pure command-producer is replay-faithful for free (the commands land in the log like any other); the `spawnAnimalHerd` command then does the per-herd scatter/leader work. **PINNED:** the **set of animal tribes seeded** is every **recorded** `[animaltype]` in `content.animals` (in canonical ascending-`tribeType` order, deduplicated), or the requested subset that has a record — never a hardcoded list, and a civilization (no `animaltypes` record) is never seeded as wildlife; each herd's size/HP/range/leader are the verbatim `animaltypes.ini` params (via `spawnAnimalHerd`). **APPROXIMATED (no oracle):** *where on the map* each birth point lands and *how many* herds a map gets — the original reads animal birth/spawn points from per-map **scenario data** (below the readable `.ini`; OpenVikings' sim is a stub), so the placement rule here (stride through the terrain's **walkable** cells in canonical row-major order, take every `cellStride`-th, round-robin-assign successive birth points to the canonical-ordered animal tribes, cap at `maxHerds`) is a deterministic *stand-in*, not a pinned placement: birth points spread across land instead of clustering, only walkable cells are chosen, and a map with no walkable cells seeds nothing. Determinism: a pure function of `(content, terrain, options)` — no RNG, no wall-clock, no world mutation (it touches no entity). Inert on the goldens/slice (nothing calls it there → no command → hash untouched). Hands-on: the REAL pipeline IR (`npm run pipeline` → 35 animals, 41 tribes) + a REAL decoded `arabskie_wyspy` map (250×250, 62500 walkable cells) through the `@vinland/sim` barrel — `seedAnimalHerds` round-robins over the real animal tribes (8..19 in a 12-cap sample), **all 12 birth points walkable + animal-tribe + in-bounds**, a deterministic re-run byte-identical, and a full seed (stride 500) places **125 herds across 34 distinct animal tribes**; an end-to-end unit test enqueues a seeded command and runs `step()` → 3 bear creatures placed with a `HerdMember` leader. Unit tests cover walkable-only placement, recorded-animals-only/canonical order, round-robin, `cellStride`/`maxHerds`, the tribe-subset filter (dropping a non-animal id), the no-walkable-cells empty case, determinism (incl. the built-graph path), and the end-to-end spawn. |
| **Hunter strike on catchable prey** (`mayHunt` lets a hunter target a `catchable` animal — the provocation source) | faithful (catchable param + hunter job id + net-damage param + atomic id + weapon reach band) / approximated (target acquisition + no walk-to-prey/harvest-cadaver follow-up) | **The last unconsumed `animaltypes.ini` driver (`catchable`) and the real provocation SOURCE the `Anger` timer waits on.** Before this, the *Combat civ-vs-animal aggression* row noted that *what first provokes* a passive `getAngry` animal was the "not-yet-built hunter strike" — a hit only landed via an aggressive animal or a test. Now `combatSystem`'s per-candidate target check (`mayTarget`) composes a third relation alongside hostility (`mayAttack`) and provoked-anger: the **predation** relation `mayHunt(content, attackerJob, targetTribe)` (`systems/readviews/tribes.ts`) — a civilization **hunter** may strike a `catchable` prey animal a non-hunter combatant leaves alone. **PINNED:** (a) the **prey set** is the verbatim `animaltypes.ini` `catchable` flag (`isCatchableAnimal` — tamable livestock: the real cow tribe 10; vs wild-only fauna); (b) the **hunter trade** is `HUNTER_JOB = 15`, pinned to `jobtypes.ini` `type 15` AND `logicdefines.inc` `JOB_TYPE_HUMAN_HUNTER 15` — the gate is the attacker's *job*, not tribe hostility (only a hunter hunts; a woodcutter/jobless settler leaves prey alone); (c) the strike reuses the **same `attack` atomic id 81** every fighting job binds — the original's `setatomic 15 81 "..._hunter_attack"` (verified in `DataCnmd/tribetypes12/tribetypes.ini`) — and the **net-damage** is the same `weapontypes` `damage["0"]` pin as every other swing, so the hunter's hit drains `Health` and (for a `getAngry` prey) flows through the existing `provokeAnger` path → the `Anger` timer. A `cannotbeattacked` animal stays exempt from a hunter too (same target exemption as `mayAttack`). (d) the **weapon's reach BAND** is now honored — a target must be within `[minRange, maxRange]` Manhattan cells, BOTH the verbatim-extracted `weapontypes` params (`combatSystem`'s `nearestEnemyTarget`). This matters most for the hunter: the real `hunter_bow` is `minimumrange 3, maximumrange 17` (verified in `DataCnmd/types/weapons.ini`), so a hunter **can't fire on adjacent prey** — it must be 3+ cells away (25 of 105 real weapons carry `minRange > 1`: bows 3, crossbows/long_bow 4, catapult 8). A melee weapon (`minRange 1`) still strikes from cell 0/1 unchanged. **APPROXIMATED (no oracle):** (a) **target acquisition** — *nearest catchable prey in the reach band*, the same deterministic stand-in as the PvP/animal drives (the original's hunter-AI target choice is the undocumented "soul"); (b) the hunter **strikes in place** with **no walk-to-prey** advance — and now, with `minRange` enforced, **no retreat-to-range** drive either (a prey too close to fire on is simply not a target this tick). The **`harvest_cadaver` yield IS now wired** — a hunter's killing blow drops the prey's meat onto its back (see the *Hunter cadaver-harvest yield* row below); only the *separate walk-to-corpse* `harvest_cadaver` atomic (atomic 33) stays approximated (the yield lands in place on the killing blow). All a later movement/production slice; (c) hunting is one direction (prey doesn't hunt the hunter — it only fights back once *provoked* via anger). Inert on the goldens/slice (no settler carries `Health` → the combatant scan finds nobody → hash untouched, the separate-optional-component pattern). Hands-on: the REAL pipeline IR through `Simulation.step()` — a real viking **hunter** (job 15, real `hunter_bow` `minRange 3`/`maxRange 17`/`damage 700`) **does NOT fire** on a real catchable prey animal (tribe 10) placed 1 cell away (inside the bow's near reach) but **does** at 3 cells (the `attack` atomic id 81 lands on it); and (synthetic) a viking hunter beside a `catchable getAngry` deer at distance 3 lands the **provoking hit** (deer HP < 1000, `Anger` stamped), then the now-provoked deer fights back. Unit tests cover the hunter→prey strike at distance 3, the minRange near-reach exclusion (adjacent + distance-2 prey both unhittable), a non-hunter leaving prey alone, a hunter NOT hunting non-catchable wild fauna, prey not hunting back, the `cannotbeattacked` exemption, the `isCatchableAnimal`/`mayHunt` read views, and the provoke-via-hunt end-to-end. |
| **Hunter cadaver-harvest yield** (a hunter's killing blow on catchable prey drops the carcass's meat) | faithful (meat good id + `maximumcadaversize` magnitude) / approximated (yield lands on the killing blow, not via a separate walk-to-corpse atomic; 1-cadaver-unit→1-meat-unit) | **The `harvest_cadaver` (atomic 33) follow-up payoff the *Hunter strike* row named as deferred** — the production half of hunting: a kill is now worth something. When a **hunter**'s (`HUNTER_JOB = 15`) completed `attack` is **lethal** (drains a target's `Health` to 0) AND the felled target is `isCatchableAnimal` prey, the AtomicSystem's `resolveHit` → `harvestCadaver` awards the slayer `cadaverYieldOf(content, tribe)` units of `MEAT_GOOD` onto its back via the same `addCarry` carriers use — modeling the original's `viking_hunter_attack` → `viking_hunter_harvest_cadaver` chain (`setatomic 15 33 "..._hunter_harvest_cadaver"`, verified in `DataCnmd/tribetypes12/tribetypes.ini`). **PINNED:** (a) the **good** is `meat` — `goodtypes.ini` `name "meat"` → `type 21` (verified in `Data/logic/goodtypes.ini`; the food good the meat economy also produces, distinct from the `cadaver_meat` *landscapetype* decal id 80, the corpse object on the ground); (b) the **per-kill amount** is the prey's verbatim-extracted `animaltypes.ini` `maximumcadaversize` (`cadaverYieldOf` — the real catchable cow tribe 10 and tribe 19 both carry `maximumcadaversize 4`). So *what good* a kill yields and *how much* are data-faithful. A non-hunter (a woodcutter felling the same cow) harvests nothing, a non-catchable kill (a wolf) yields nothing, and a `maximumcadaversize` of 0 yields nothing — the gate is hunter-AND-catchable-AND-lethal. **APPROXIMATED (no oracle):** (a) the yield lands **in place on the killing blow** rather than via a *separate walk-to-corpse* `harvest_cadaver` atomic (atomic 33 is a distinct animation the hunter would walk to the carcass and run; that movement/animation slice is deferred — here the meat appears when the kill lands); (b) the **1-cadaver-unit → 1-meat-unit** mapping (`maximumcadaversize` 4 → 4 meat) is our reading — the event-vocabulary that would pin the exact meat-per-cadaver is below the readable `atomicanimations.ini` (the `event 21 11`/`event 22 28` tuples in `..._harvest_cadaver` are the undocumented yield/cue encoding, the "fine-tuning by observation" the roadmap notes). One edge guard: a hunter already carrying a *different* good (a planner-bug state for a fighter) drops the meat rather than crashing on `addCarry`'s harvester-bug throw; a hunter already carrying meat merges the units (goods conserved). Inert on the goldens/slice (no settler carries `Health` → no kill → no yield → hash untouched, the separate-optional-component pattern). Hands-on: the REAL pipeline IR (`npm run pipeline` → 35 animals, catchable cow tribe 10 `maximumCadaverSize 4`) through the **full** `Simulation.step()` schedule — a real viking hunter (job 15, real `hunter_bow` 700 dmg, band 3..17) bow-fells a real catchable cow (tribe 10, HP 1000) at distance 3 over several swings; on the killing blow the hunter ends **carrying meat (good 21), amount 4** (= the cow's `maximumcadaversize`) and the corpse is reaped (`cleanupSystem`); two same-seed runs hash-equal (`fff7bb2f`, stores cleared between runs). Unit tests cover the lethal-blow yield (meat 21, amount 4), the non-lethal no-yield, the non-hunter no-yield, the non-catchable no-yield, the already-carrying-meat merge, and the carrying-a-different-good skip (no throw, load untouched). |
| **Settler-side Health stamping** (`spawnSettler{hitpoints}` mints a civilization combatant through the command seam) | approximated (the hitpoint magnitude — humans' HP is below the readable `.ini`) | **Closes the structural gap the *Combat hit resolution* row's APPROXIMATED-(a) named** — a civilization could only *become a combatant* by a test reaching directly into the world (`sim.world.add(e, Health, …)`, as `two-civ-combat`/`populated-map-combat` did), because `spawnSettler` minted a settler with **no `Health` pool**. Now `spawnSettler` carries an optional `hitpoints` field (`commands.ts`); when it is a positive value the handler (`systems/conflict/command.ts`) stamps a full `Health{hitpoints: max, max}` — the settler analogue of `spawnAnimalHerd`'s `hitpoints_adult` stamp — so a civ enters the world as a fighter **FROM THE COMMAND DATA, through the one mutation seam**, replay-faithful like any other command. Omitting `hitpoints` (the default — every golden/vertical-slice/scenario `spawnSettler`) leaves the settler `Health`-less, the separate-optional-component pattern (like `JobAssignment`/`Age`), so the golden hash is **untouched**. A non-positive pool (`0`/negative) stamps nothing (a 0-HP fighter would be reaped the same tick) — only a real pool makes a combatant. **APPROXIMATED (no oracle — the one honest gap):** the **hitpoint magnitude** is *caller-supplied*, NOT pinned to a param — a human's hitpoints are below the readable `.ini` (only `animaltypes.ini` carries `hitpoints_adult`; `jobtypes.ini`/`tribetypes.ini` carry none, and the original manages human HP at runtime via the `atomicanimations.ini` `event 5` CHANGE_HITPOINTS / `event 10` SEND_HITPOINTS vocabulary — `ATOMIC_ANIMATION_EVENT_TYPE_CHANGE_HITPOINTS 5` in `logicdefines.inc` — below this slice). So *that* a civ stamps Health on spawn is the faithful structural shape; *how much* is an approximation calibrated later. Armor on a settler (a `[armortype]` class > 0) now lands too (the *Settler-side Armor stamping* row below). Determinism: a pure handler branch on the command value, no RNG/wall-clock. Hands-on: the REAL `Simulation.step()` schedule (seed 1, 6×1 grass) — a viking civ spawned via `spawnSettler{jobType:1, hitpoints:200}` is stamped `Health{200,200}` through the command seam, then over the real schedule it **launches the attack atomic (id 81)** at a seeded aggressive bear, is **hit back** (its HP drops 200→40), and is **felled+reaped** by tick 40; two same-seed runs hash-equal (`70b3afaf`, stores cleared between runs). Unit tests cover the positive-pool stamp (`Health{1000,1000}`), the omitted-hitpoints non-combatant (no `Health` → golden path), and the non-positive (`0`) no-stamp. |
| **Settler-side Armor stamping** (`spawnSettler{armorClass}` mints an armored combatant; a hit selects the target's worn material column) | faithful (per-material `weapontypes` damage column) / approximated (which settler wears which class — no soldier-class→armor binding yet) | **Closes the last deferred refinement on the combat-hit-resolution row** (the *Combat targeting drive* row's APPROXIMATED-(d): "a settler wears no armor yet, so every target resolves as class 0"). Two parts: (1) a new **separate optional `Armor{armorClass}` component** (`components/index.ts`) a combatant may wear, stamped through the command seam — `spawnSettler` carries an optional `armorClass` field; a positive `[armortype]` tier (1..4) stamps the component (`systems/conflict/command.ts`), the settler analogue of the `Health` stamp, while an omitted / non-positive value leaves it unarmored (the separate-optional-component pattern — golden hash untouched). (2) `combatSystem` now selects the hit's damage **column** for **the target's worn armor material** (`targetMaterial` → its `Armor`'s `materialType`, or 0 when bare) via `weaponDamageVsMaterial` — `weapon.damagevalue[targetMaterial]` — instead of the fixed `damage["0"]`. **PINNED (the damage param):** this is the **same verbatim `weapontypes` column** the `combatDamage` read view tabulates — the original's pre-resolved per-material weapon damage; the system reads the *material actually hit* rather than always material 0, and **subtracts no `blockingValue`** (armor selects the column — the *Armor blockingValue engine role* row). An **out-of-table** class (a `[armortype]`-less 6/7, or a bad stamped value) selects its own column (0 damage if the weapon lists none), the same "armor the data doesn't define does something graceful rather than crashing" stance `combatDamage` takes; the column value is clamped at 0 (a hit never heals). So *how much an armored hit takes off* is now data-faithful per material. **APPROXIMATED (no oracle):** *which* settler wears *which* armor class is **caller-supplied** (`spawnSettler{armorClass}`), NOT yet pinned to a soldier-class→armor binding — the original's per-job/per-unit armor loadout is the soldiers/armor-tier content slice (the *same* "caller-supplied magnitude, structure faithful" stance as the `Health` hitpoint stamp). Inert on the goldens/slice (no settler carries `Armor` → every existing hit still resolves vs class 0 → hash untouched, the separate-optional-component pattern). Hands-on: the REAL `Simulation.step()` schedule (seed 7, 5×1 grass) — a viking woodcutter (`test_axe`, `damage {0:50, 1:60}`) vs a frank: **unarmored** target takes the `damage["0"]` column 50; a **leather-clad** (class 1, material 1) target takes the `damage["1"]` column **60** (no `blockingValue` subtracted — the material-column re-pin — confirmed by the **state hash differing** between the two, the `Armor` component live in the hash); a re-run of the unarmored case is hash-equal (deterministic). Unit tests cover unarmored (material 0 → 50), leather (material 1 → 60), an out-of-table class 2 (its own column, no value → 0), a weapon that lists no column for the material (bearfist vs leather → 0), and the command-seam stamps (positive → `Armor{class}`, omitted/0 → no `Armor`). |
| **Settler-side Weapon stamping** (`spawnSettler{weaponTypeId}` equips a combatant with a *specific* weapon; the CombatSystem attacks through it) | faithful (the weapon's damage/reach params) / approximated (which settler holds which weapon — no soldier-class→weapon loadout yet) | The weapon twin of the *Settler-side Armor stamping* row, and the first self-verifiable slice of the deferred **equip drive** (the *Combat targeting drive* / Phase-4 combat item's oracle-blocked piece). A new **separate optional `Weapon{weaponTypeId}` component** (`components/index.ts`) a combatant may wield, stamped through the command seam — `spawnSettler` carries an optional `weaponTypeId`; a positive `[weapontype]` id stamps it (`systems/conflict/command.ts`), the settler analogue of the `Armor`/`Health` stamp; an omitted / non-positive value leaves the settler on its class default (the separate-optional-component pattern — golden hash untouched). `attackerWeapon` (`systems/conflict/combat.ts`) gains a third resolution path that takes precedence: a worn weapon resolves by **`(settler tribe, weaponTypeId)`** and overrides the default `(tribe, jobType)` first-match — this is what lets one settler of a soldier-class hold a *specific* weapon from the several its class may wield (the data-defined `weaponsForJob` roster, which the scan would otherwise collapse to its first entry). **PINNED:** the worn weapon's stats are the **verbatim extracted `weapontypes` params** — its `damage` table + `[minRange,maxRange]` reach, the same params the default path + `combatDamage` pin; the override only changes *which* `[weapontype]` row a combatant fights with, inventing no numbers. A worn id resolving to **no `[weapontype]` record** (a bad/unknown `(tribe, typeId)`) leaves the combatant **unarmed for the tick** (no silent fall-back to the default) — the "the data doesn't define it → it does nothing" stance the `Armor` out-of-table class takes. **APPROXIMATED (no oracle):** *which* settler holds *which* weapon is **caller-supplied** (`spawnSettler{weaponTypeId}`), NOT yet a soldier-class→weapon loadout; the equip *behavior* (a settler acquiring/carrying the weapon-good, `WeaponType.goodType`) stays oracle-blocked — the same "structure faithful, loadout approximated" stance as `Armor`. Inert on the goldens/slice (no settler carries `Weapon` → the `(tribe, jobType)` default path runs unchanged → hash untouched). Hands-on: the REAL `Simulation.step()` schedule (seed 7, 6×1 grass) — a viking woodcutter equipped `Weapon{11}` (test_spear: damage 70, reach 3..17) vs a frank **4 cells away** issues `{attack, damage:70}` — its default axe (damage 50, maxRange 2) could not reach 4 cells; the same geometry WITHOUT the worn spear issues **0** attacks (behavior genuinely changed), and two same-seed runs hash-equal (`a0347384`, stores cleared between runs). Unit tests cover the default (no Weapon → class weapon, unchanged), the override (worn spear's damage + reach), the worn near-reach (a spear-wielder can't strike an adjacent target), the bad-id-leaves-unarmed (no fallback), and the command-seam stamps (positive → `Weapon{id}`, omitted/0 → no `Weapon`). |

> **How to read this table.** Most Phase-2 rows are `approximated` because the *behavior* axis has no
> automatic oracle — the planner/loop shapes are deterministic design choices, not yet calibrated
> against the running original. That is expected and honest, not a gap to paper over: the
> data-derived *parameters* (durations, gates, capacities) are faithful, and the behavior rows carry
> an explicit "calibration-by-observation pending" until a human watches the original and tunes them.
> The blind spot this file exists to surface is a row silently sitting `faithful` without a named
> source — not an honestly-`approximated` one.

### Render / presentation (Phase 2)
The render layer is a pure consumer of sim state (it never feeds back), so most of it is a *visual*
checklist (depth-sort/projection — see docs/TESTING.md), not a mechanics oracle. The one
fidelity-relevant decision is the **state→sprite join**: which animation a settler shows.

| Concern | Status | Source / how pinned |
|---|---|---|
| Settler state→sprite-frame join (`resolveSpriteFrame`) | approximated | The **join key is faithful**: an `acting` settler carries its numeric `atomicId`, the exact key the original's `tribetypes` `setatomic` maps to an animation. But the *render-side state model* (`idle`/`moving`/`acting` + an orthogonal `carrying` flag, derived from `CurrentAtomic`/`PathFollow`/`Carrying`) and the **which-frame-per-state** choice are *our* coarsening — the original has a richer per-direction/per-atomic animation table not yet bound. Under `?atlas=real` four real `CR_Hum_Body_00` `[bobseq]` ranges are now wired by hand: **idle = the looping `human_man_generic_wait` (1931), played SINGLE-direction** — its 57-frame length isn't a clean ×8, so (like the gallery's `clipDirs`) it's one direction, the WHOLE loop; a standing settler breathes rather than holding a frozen frame (the earlier `idle` was the walk seq held on frame 0) or showing a truncated 1/8 facing-slice; walk (1988); chop (5106, on the harvest atomic); and the **loaded carry gait** `human_man_generic_walk_wood` (4580) bound to the `carrying` override — so a woodcutter hauling its harvest plays the log-on-shoulder cycle, not the empty walk. (Loaded-idle stays a still `standWood` hold: the data has no loaded-wait loop, and a carrier only stands loaded for the brief deposit transient.) This is still a hand-picked subset, not the full extracted `setatomic`→bob table (a later slice); the committed default remains the FREE synthetic atlas. Pixel fidelity stays the OpenVikings oracle's job (Assets row), deferred to a human. |
| Per-job character binding (job → body/head/animation; `SpriteSheet.characters`) | faithful (join keys) / approximated (head pick, weapon-anim borrowings, armed idle) | Settlers now draw a PER-JOB look, the original's `[jobbasegraphics]` join wired to live sim state: the render `DrawItem` carries `Settler.jobType` (+ a `young` flag = the `Age` component) and `Carrying.goodType`, and the sheet's `characters` table (`pickByJob`) picks which body/heads/binding compose the settler. **Faithful (transcribed keys):** the viking table from the mod's `types/humanstype/jobgraphics.ini` (`logictribe 1`) + the real `jobtypes` ids — woman 5 → `cr_hum_body_10`, the soldier family 31..41 → the armoured `cr_hum_body_05` (each weapon class playing ITS weapon's own `[bobseq]` walk: spear 32/33, sword 34/36, broadsword 35/37, shortbow 40, longbow 41), age classes 1..4 (keyed ONLY for `Age`-carrying settlers, the [dc3ef54] collision guard) → baby `cr_hum_body_22` / girl `cr_hum_body_21` / boy `cr_hum_body_20`; every unmapped trade → the generic man `cr_hum_body_00` (faithful — the original's civilian jobs share that body). Each body's binding derives from ITS OWN extracted `bobSequences` (walk/wait names transcribed per body; the baby has no walk and stands its wait). **Approximated:** (a) the per-individual HEAD pick is `entity id % heads` (stable, varied) vs the original's random per-settler head+palette; (b) the axe soldiers 38/39 borrow the broadsword set (the body authors no axe `[bobseq]`) and the sabers 36/37 borrow sword/broadsword; (c) EVERY idle is a breathing loop: the weapon waits (22..29 frames), like the generic waits (57/35/39), are non-×8 strips read as facing-locked single-direction loops (the established `clipDirs` rule — the original plays non-×8 locked to a facing), so an armed soldier breathes holding his weapon; a spec whose wait strip is missing from the IR falls back to holding its walk's first frame per facing (a directional still, the never-crash floor). **Also bound:** every OTHER atomic the sim issues today, where the body authors a sequence — eat 10 / sleep 8 / pray 12 / pickup 22 / deposit 23 for the man AND the woman (`human_{man,woman}_generic_{eat,sleep,pray,pick_up}`; the pick-up stoop serves deposit too — no separate put-down is authored, and a bound atomic wins over the carry override so the depositor stoops as its load leaves). Non-×8 action strips (eat 17, sleep 20, pick_up 19..21) play whole and facing-locked (the `clipDirs` reading); the pray 120 is a clean ×8 and plays directionally. A body without the sequence (the soldiers, the children) falls back to its breathing idle. **Deferred:** soldier ATTACK animations (`..._attack`, non-×8 facing layouts — need the per-sequence facing calibration) — a fighting soldier currently stands its idle pose; the scout(27)/druid(30) head sets (gallery-only today); per-settler palette variants (the Phase-B skin/hair row). Acceptance scene: `?scene=characters` (woodcutter loop + woman + soldier family on one screen). |
| Carry look per good (`carrying.byGood`) | approximated (name join — no readable binding table) | A hauling settler now shows the HAULED GOOD's own carry cycle: the sim's `Carrying.goodType` rides the draw item and keys a per-good table built from the CONTENT the sim runs (demo/scene goods are content-relative ids, so each entry passes its own goods into `resolveSpriteSheet`). The body's `[bobseq]` set authors ~23 `human_man_generic_walk_<good>` variants (wood/stone/bread/fish/grain/iron_gold/…, the woman a 4-variant subset); the join is **by NAME** — good id-slug → sequence suffix, with an alias table (`CARRY_SEQ_SUFFIX`: wheat→grain, iron/gold/coin→iron_gold, every potion→potion, sword_long→broadsword, …) for slugs that don't match verbatim — because NO readable good→carry-animation table exists (the mod ships no goodstype twin; the base binding is encrypted `.cif`). A good with no resolvable cycle falls back to the generic wood-log gait (the previous behaviour for ALL goods), then to the plain walk. **Head-borrow:** 19 of the man's 27 carry variants ship EMPTY head bobs (the head is authored once, on the base walk), so the per-character binding carries a head-side twin of the table (`carryHeadAnims` → `SettlerCharacter.headBinding`): a good whose carry range has no authored head resolves its HEAD through the base walk at the same (facing, frame) offset — the gallery's proven head-reuse rule applied to the game path — so a stone/grain hauler keeps its head; an authored head (wood, food, flour, …) still draws its own range. Unverifiable pairs (does the original haul mushrooms in the `walk_mushroom` basket?) stay eyeball-vs-original checks. |
| Character animation gallery (`?anim`) | faithful (direction count + head-reuse heuristics, data-shaped) / approximated (per-sequence facing order) | The `?anim` mode is a data-driven **animation gallery** — the render twin of the `?scene=all-buildings` catalog for the character: it plays EVERY extracted `[bobseq]` of `cr_hum_body_00` (all ~69 civilian sequences) straight from the atlas (body + head), with a direction selector, so a human can validate each animation. Render-only (no sim), so it shows animations the settler state-machine doesn't bind. Three data-shaped rules, no explicit oracle for any (OpenVikings' sim is a stub): (a) **direction count** = `length % 8 === 0 ? 8 : 1` (`clipDirs`) — a clean ×8 layout is 8-directional (walk 96, pray 120), anything else is single-direction (wait 57, eat 17, jump 21, the unarmed fights 102/126) and plays its whole strip locked to one facing rather than being chopped into fake directions; matches the original's "non-×8 plays locked to `startdirection`" behaviour (`atomicanimations.ini`). (b) **"full" mode** rotates an 8-dir clip through the 8 facings in COMPASS order (`COMPASS_TO_BLOCK`, inverted from the `CR_Hum_Body` facing table) so the character turns in a clean circle, not the jumbled storage order. (c) **head reuse** — ~19 carry-walk variants (`walk_bread/fish/longbow/…`) have EMPTY own head bobs; a walk-layout (len 96) clip with an empty head **borrows the base `human_man_generic_walk` head** at the same (direction, frame) offset (`headBobId`/`headStart`), so it isn't drawn headless (the head faces the walk heading while the body carries the load). **APPROXIMATED (no readable oracle):** the per-8-dir-sequence *facing ORDER* — the numeric-facing labels are pinned to the verified WALK block layout (`0 SW … 7 N`), correct for the locomotion family (walk + carries), but a non-locomotion 8-dir sequence (e.g. `generic_speak`, which also gestures within a block) can lay its blocks out differently, so its per-facing labels can be off; the readable data's `startdirection` lives under tribe-atomic names that don't cleanly join these generic `[bobseq]`s, so this stays eyeball-calibrated per sequence if ever needed. **ROSTER EXPANDED:** the gallery now plays the FULL viking roster (`?char=`, `viking-roster.ts`) — the civilian man plus the **warrior body `cr_hum_body_05`** (its own broadsword / sword / longbow / shortbow / spear / bare-handed set, ~57 sequences — the fighting animations the civilian body never had; the attacks are non-×8 so they play single-direction/full-strip, the same `clipDirs` rule), the woman (`cr_hum_body_10`), the two children (`cr_hum_body_20/21`) and the baby (`cr_hum_body_22`). Each body/head pair is transcribed from the mod's viking `[jobbasegraphics]` (`logictribe 1`), so a look is a real in-game composite, not a guess. A second view (`?view=heads`) is the **looks montage** — the plain walk (`pickWalkRow`) played once per head, so every face/hat is compared side by side (12 civilian looks head_00–03/80–83/90–93, 4 warrior looks head_05–08; all verified to cover the walk range). The **bare `?anim` default** is now the whole-roster montage (every look walking on one screen, `buildRosterCells`); a `?char=` drills into one body. The **baby is body-only** — `cr_hum_head_22` decodes to an empty atlas (its head is baked into the body bob), so the roster models it with no head look and it draws body-only (faithful — the original composites the same empty head). The sim-state wiring has since LANDED for the locomotion/idle/carry subset + the per-job body swap (see the *Per-job character binding* row); the remaining gallery-only animations (fights, needs, social) still await their sim atomics. |
| Character skin/hair variants (`?anim` looks) | approximated (single baked palette) — **deferred** | Every roster body/head decodes with ONE palette (`test_human_00`), so the gallery shows a single skin/hair look per character. In the original, skin + hair are NOT separate art: a base palette is remapped per-settler by a `[RandomPalette]` (`Data/engine2d/inis/humans/randompalette.ini`, e.g. `Vik_Man_Base`/`Vik_Man_Soldier`) that overlays named 16-colour ramps (`skin pale 1..4`, `hair blond/brown/red/…`, resolved as `[GfxPalette16]` `gfxcolorrange` slices of `skin.pcx`/`hair.pcx`) onto index PATCHES of the base — the OpenVikings `CRemapTable` algorithm (base→ramp index remap over a range). Our pipeline bakes one palette into each PNG (`expandBobFrame`, `decoders/atlas.ts`) and has NO `randompalette.ini` decoder yet. Making a **skin-tone axis** faithful is a self-contained **pipeline follow-up**: (1) decode `randompalette.ini` + the ramp `.pcx` sources, (2) reproduce the patch→palette-index remap (the open question is the exact `Patch <slot>`→index-range mapping — nail against `CRemapTable` usage + the base palette bands), (3) bake per-variant PNGs (`<bmd>.<variant>` stems) at the `convertBmdTree` enumeration point, (4) give the gallery a tone selector that swaps the palette in `characterStems`. Tracked as Phase-B; the roster model already carries a `palette` param so the plumbing is ready. Distinct from — but sharing the `randompalette.ini` machinery with — the **player (team) colour** row below. |
| Player (team) colours (the WORLD renderer, all scenes + `?anim` colour montage / `color=N`; the `PalettedSprite` LUT) | faithful (the 10 shipped colours + the clothing-patch remap mechanism) / **divergence** (6 extra colours; whole-body patch scope) | The original gives each unit a per-creature palette in which the `Player NN` ramp — a 16-colour ramp at colour-range 1 of `playerNN.pcx` (indices 16–31) — is bound by the `randompalette.ini` `player_00…09` recipes onto the body's clothing **patches** (patch 10 men's vest = idx 160–175, patch 5 = 80–95 which copies patch 10, patch 15 women = 240–255); the `.bmd` stores palette INDICES, so the colour is decided by the composed palette the index is read through. **We reproduce that at DRAW TIME:** the pipeline emits an **indexed** character atlas (`expandBobFrameIndexed` — palette index in red, mask in alpha; `<bmd>.indexed.png`) + one **`256×16` player-colour LUT** PNG (`player-palette.ts` `composePlayerPalette`: the base body palette `test_human_00` with those clothing patches overwritten by each player's ramp; `buildPlayerLutImage` stacks the 16), and the renderer's custom-shader `PalettedSprite` reads each atlas index through the player's LUT row — one indexed atlas + one LUT texture serve all 16 players (no per-player texture, no flat `Sprite.tint`, which can't do a band-limited ramp remap). **FAITHFUL:** the 10 shipped colours in `TPlayerColorId` order (blue=human default, then red/yellow/cyan/green/purple/grey/orange/neon/black) read straight from `player01.pcx…player10.pcx`; the clothing-patch band + ramp-source were pinned empirically (diffing `player01..10.pcx` → the varying indices; the base `test_human_00` patch 10 IS the visible cyan default vest that remaps to the team colour). **DIVERGENCES (recorded, deliberate):** (a) **10 → 16 colours** — the original ships 10 (`PLAYER_COLOR_ID_MAXIMUM`, repeated for >10 players); the extra six (spring/teal/azure/indigo/magenta/pink) are **hue-rotated from `player01`**, no original equivalent, chosen to fill the hue gaps (needed for the "up to 16 players" goal); (b) **whole-body patch scope** — we remap patches 5+10+15 on EVERY body regardless of gender, where the original binds patch 15 to women / 10 to men, so a viking warrior's patch-15 shields/weapon-aspects also take the team colour (mild over-colouring, chosen so combat units read clearly by team); (c) we use only the primary `Player NN` ramp, not the per-settler-random `Player NNa`/`Player NNb` shade variants (the recipe picks one of three per unit); (d) NO full `randompalette.ini` decoder (skin/hair variety stays the deferred row above). **Demo / sign-off:** `?anim&view=colors` (one walk cell per colour) and `?anim&color=N` (a character's whole animation set — including the warrior's broadsword/bow/spear/fist combat clips — in one colour); colour correctness is the human's call. **Sim ownership → team colour (LANDED, global):** the world renderer now keys each settler's LUT row off the sim `Owner.player` on EVERY scene, not just the gallery — `collectSprites` reads `Owner.player` → `DrawItem.player`, and `SpritePool` draws a settler through a `PalettedSprite` mesh at that row whenever the sheet carries the LUT + indexed characters (real graphics on); an UNOWNED settler (wildlife / neutral) draws LUT row 0 (the base palette). Without the LUT (no `content/`) it degrades to plain baked sprites exactly as before. Verified: the melee scene shows player 0's squad in blue and player 1's in red, same warrior body, only the clothing band recolouring. **Perf note:** a `PalettedSprite` is a custom-shader mesh that bypasses Pixi batching (one draw call per on-screen settler) — the accepted cost for a band-limited ramp remap (render/CLAUDE.md "keep it to characters"; the sim, not the GPU, is the battle-scale wall). |
| Resource (tree) sprite bind (`ls_trees.bmd`) | approximated | The **bind path is faithful**: under `?atlas=real` a wood `Resource` node draws a frame of the decoded `ls_trees.bmd` recoloured by its palette — the `landscapes.cif` `[GfxLandscape]` record's binding, extracted by `extractLandscapeGraphics` (`GfxBobLibs` body+shadow + `GfxPalette` editname + `GfxFrames`), emitted through the same `convertBmdTree` atlas path as the creature bobs. What is *approximated* is the **per-object species + frame pick** (see Deviations): one representative full-grown frame of one species (yew, bob 60) is bound to every wood node, where the original assigns a species + growth/sway frame per pre-placed map object. Pixel fidelity (does the decoded tree match the original) stays the OpenVikings oracle's job, deferred to a human. |
| Gathering-economy graphics — per-good nodes / ground piles / delivery flag (`ResourceTypeBinding` + `StockpileBinding`) | faithful (the good→landscape→gfx join) / approximated (representative pick, fill mapping, single-player flag) | Every gatherable good now draws ITS OWN standing node (a tree for wood, a rock for stone, a clay/iron/gold **mine** decal, a mushroom) and ITS OWN dropped ground pile (an `ls_goods.<good>` heap that grows with the pile's contents), and a bare/empty ground pile draws a **delivery flag** (`ls_temp` player-01 "work extern" flag — the plain flag-on-a-pole the original plants to mark an external work / collection point, which is what a loose ground pile IS; deliberately NOT the building-occupancy "sign" emblem) — replacing the single hardcoded yew bob every `Resource` used to blit and the *invisible* bare `Stockpile+Position` (piles + flags rendered as nothing before). **FAITHFUL — the join is data:** the render consumes the Step-1 `gatheringPipeline` artifact (`buildGatheringPipeline`, the good's `landscapeTo{Harvest,Store}` chain → the `[GfxLandscape]` records that place each stage, joined by `LogicType`), so a good's node is its `landscapeToHarvest` record and its pile is its `landscapeToStore` record — no hardcoded good→bob table. The renderer reads `Resource.goodType` / the pile's dominant good into the `DrawItem` and resolves through a per-good `ResourceTypeBinding`/`StockpileBinding` mirroring the buildings' `BuildingTypeBinding` (a bare bob from the default yew `kindLayers.resource` layer, or a layer-qualified `{layer,bob}` into a loaded `ls_ground`/`ls_goods`/`ls_mushrooms`/`ls_temp` family in `SpriteSheet.families`; an unloaded family is dropped, never a wrong-bob borrow). The good→gfx match is **by id-SLUG** (`goodId`), so a scene/slice's own goodType numbering resolves the right real object (the same slug join the carry looks use). **APPROXIMATED (no explicit oracle — OpenVikings' sim is a stub):** (a) **representative pick** — the lowest-index (`"…01"`) record of each stage stands for the good (wood→yew, stone→"stones 01 khaki", clay/iron/gold→"…mine 01", mushroom→"…agaric 01"), where the original assigns a per-object species/variant from the pre-placed map layer (`map.dat`, not decoded); (b) **fill→frame** — the number of fill states is the record's OWN data (`maxValency`/`frames.length` = 5 for the `ls_goods` piles), and the binding indexes them by the pile's unit count clamped to that count, so *within the data's pile capacity the amount→state map is the identity* (1 unit → smallest heap, 5 → fullest); what's approximated is only that a sim pile can hold MORE than the record's 5 states (it clamps to the fullest heap) — an amount→capacity calibration with no oracle, not a tuned constant; (c) **node is always full-grown** — the top-state frame draws regardless of `Resource.remaining` (the shrink-by-level pick is the Step-4 mining slice); (d) **produced-good piles** (a flag holding a produced good like sword/bread) fall to the placeholder heap — only the ~11 map-gathered goods carry a `gatheringPipeline` (a follow-up can join `good.landscapeType` for all goods); (e) **single-player flag** — v1 draws the `human_player01` "work extern" flag for every flag; a per-owner palette swap (players 02–16 have their own records) is deferred (needs a `DrawItem` owner, the same axis the `PalettedSprite` team colour awaits). Acceptance scene `?scene=gathering` (one node per good + piles at rising fills + a flag). Pixel fidelity stays the OpenVikings oracle's job, deferred to a human. |
| Building per-type sprite bind (`ls_houses_viking.bmd`) | faithful (params, data-extracted) / approximated (atlas coverage + scale) | The **bind path is faithful**: under `?atlas=real` a `Building` draws a frame of the decoded `ls_houses_viking.bmd` recoloured `house01` — a `[GfxHouse]` viking record's binding (mod `budynki12/houses/houses.ini`), decoded through the same universal `.bmd`→atlas path as the trees/creatures (the bob layout is identical — OpenVikings `CBobManager` is one decoder for all `.bmd`). The **per-building-type frame pick now lands** for the types sharing that atlas: a draw item carries its `Building.buildingType` and a `BuildingTypeBinding` keys each type to its own bob — the `[GfxHouse]` `LogicType`→`GfxBobId` join (`VIKING_HOUSE01_BOBS`: home `6`→41, well `10`→131, hive `11`→91, farm `12`→60, bakery `15`→105), **transcribed verbatim from `houses.ini` `[GfxHouse]` (LogicTribeType 1, GfxPalette "house01")**, not the one bob 11 reused for all. Two things stay **approximated**: (a) **atlas coverage** — **all seven viking families are now mapped and loaded** (`BUILDING_FAMILIES`): the default `ls_houses_viking.house01` plus `ls_houses_viking4.house01` (HQ `viking4` bob 34 + animal farm / druid hut / barracks / tower), `ls_houses_viking2.house01` (pottery / joinery / smithy), `ls_houses_viking3.house01` (sewery / armory / mason hut / school), the two palette-skins on a shared `.bmd` — `ls_houses_viking.housemiller01` (mill) and `ls_houses_viking4.housedruid01` (herb hut / temple) — and the two `house02` families that CLOSE the set, `ls_houses_viking.house02` (stock 7/8/9) and `ls_houses_viking2.house02` (brewery 16, coin mint 33), each named family bound via a layer-qualified `{layer,bob}` ref into `SpriteSheet.families`. So **every viking `[GfxHouse]` type now draws its own bob — 0 fall-backs** (the reducer over the real `ir.json` binds all 40 viking typeIds, no drift). The atlas coverage is now complete for the viking tribe; the *other* tribes' families (frank/egypt/saracen/byzantine) remain a later rung; (b) **render scale** — one uniform `BUILDING_SCALE` 0.7 about the feet (native house bobs render ~6–10× the settler — oversized vs the original's house-to-person proportion; a named family inherits this scale, with a per-family `familyScales` override available if a `.bmd` needs its own factor). The uniform scale is the *faithful* choice within a family: the native bob sizes already encode the real relative proportions (well 63×88 vs home 299×340), so scaling them all by one factor preserves that spread rather than flattening it. **DATA-PINNED (extraction landed; render now consuming):** the `(typeId→bob)` join is **extracted into the IR** — `extractBuildingBobs` reads each `[GfxHouse]`'s paired `LogicType <lvl> <typeId>` ⊗ `GfxBobId <lvl> <bobId>` level-tables into the validated `buildingBobs` table (hands-on over the real `houses.ini`: **336 rows / 234 distinct (tribe, typeId) / 6 tribes / 17 palettes**, reproducing the 5 transcribed `house01` mappings *exactly* and recovering the home growth chain t2→bob1 … t6→bob41 the constant dropped). The join is **multi-valued** on `(tribeId, typeId, palette)` — a logic typeId maps to several bobs across build levels (wonders, home tiers) and graphics variants (wall orientations, HQ vs "headquarters house"); the consumer disambiguates by `level`/`editName`. NB the five lumped `[GfxHouse]` brackets (the saracen/egypt blocks pack 4–24 houses under one header) are split per-`EditName` so all 234 types resolve — `extractConstructionCosts`/`extractBuildingGraphics` share the same pre-existing lumping bug (saracen/egypt costs + atlases incomplete), a flagged follow-up. **The render now CONSUMES the join end-to-end across MULTIPLE families** — `content/building-gfx.ts` `buildingBobRefsByType` reduces the table per `(tribe, typeId)` to its canonical row (palette-preference → `editName` disambiguation → max-`level` → lowest-`bobId`, insertion-order-independent) and emits a `BuildingBobRef`: a **bare id** for the default `ls_houses_viking.house01` layer or a **layer-qualified `{layer,bob}`** for a LOADED named family; a row whose family is unloaded is **dropped** (not bound to a wrong bob), then the map **overlays** onto the transcribed `VIKING_HOUSE01_BOBS` to feed `BuildingTypeBinding.byType` — real data wins per type, the constant backs its five known types when the IR is partial/absent (graceful type-by-type degradation, not all-or-nothing). The `editName` axis now disambiguates a genuinely multi-bob typeId: the **HQ** (typeId 1) binds bob 34 `"viking headquarters"` over bob 44 `"…headquarters house"` (docs/SOURCES.md anchor), pinned by `CANONICAL_EDIT_NAME` rather than the coincidental lowest-bob. Over the real regenerated `ir.json` the data path reproduces the signed-off five (6/10/11/12/15) **byte-identically** (so that pixel sign-off carries over), adds the growth-stage typeIds 2/3/4/5/14, and now resolves the viking4 family — HQ→bob34, animal farm 17→30, druid hut 35/36→5/10, barracks 39→25, tower 40/41→15/20. Pixel fidelity stays the OpenVikings oracle's job, deferred to a human. |
| Chopping settler position nudge (`CHOP_NUDGE_X`) | approximated (visual) | The harvest planner stands the woodcutter ON the tree's cell (`ai.ts`, `cell === here`), so at the cell centre its sprite overlaps the trunk and the right-swing axe falls beside it. `buildScene` (`render/src/data/scene.ts`) shifts a mid-chop settler's *drawn* sprite left by `CHOP_NUDGE_X` (−24 px) so the strike lands in the trunk — a render-only anchor offset (the sim position and the depth sort are unchanged, determinism untouched). The magnitude is a by-eye constant a human tunes; a *faithful* fix would stand the settler on an ADJACENT cell facing the tree (a harvest-adjacency the planner doesn't do yet — it still stands the settler ON the tree cell, even though the pathfinder is now 8-connected; a larger slice). |
| **Sound playback** (`@vinland/audio` — action SFX + terrain ambient + jingles) | faithful (mechanism + event→sound map) / approximated (ambient terrain join) / deferred (voices, per-animation SFX, music) | The decoded `.wav`s play positionally over the sim: `@vinland/audio` consumes the SAME read-only snapshot + one-shot events `render` does (never sim state) and per frame (a) fires **action SFX** for bound events — building placement → `Hammer Wood`, the woodcutter chop atomic → `Woodcutter Axe`, production → `Carpenter Saw` — **viewport-culled + attenuated (radially from screen centre AND by camera zoom — a zoomed-out/further camera is quieter, floored so a wide view never goes silent) + stereo-panned** by the exact camera projection the renderer draws with (`computeSpatial` reuses render's `tileToScreen`/`cameraViewport`), so a sound comes from where its sprite is and only while on screen (the user's "only what's on screen makes sound"); (b) plays **non-spatial life-event jingles** (`settlerBorn`→birth 23 / `settlerDied`→death 25 / `buildingFinished`→housebuilt 26, by `MusicType`, full gain, centred); (c) loops **terrain ambient** beds under the viewport, coverage-weighted, cross-faded as the camera moves; (d) plays **settler voice chatter matched to who is on screen** — on-screen settlers speak at ~1.6 clips/s across the visible crowd, each drawing from the pool for THAT settler's **sex/age**: `vikingVoiceClass` mirrors the render roster's job→body split (the `woman` job → female, an `Age`-carrying settler → child, every other adult → male), read straight off the snapshot's `Settler.jobType` + `Age`-presence, so a settler SOUNDS like the body it draws. Viewport-culled + panned, per-settler cooldown so a crowd murmurs with variety without any one settler looping. This **fixed a real bug**: the chatter previously drew every pool uniformly, so women/children came out of an all-male crowd. The **event→sound + voice-pool bindings are faithful by name/`MusicType`** decoded from `soundfx.cif` (`defaultBindings` / `VIKING_VOICE_POOLS` + `vikingVoiceClass`; the mod's `humans/sounds.cif` binds the same voice groups per tribe/sex); the pure director + `onScreenSettlers` + `vikingVoiceClass` are unit-tested headless (the Web Audio playback isn't — an ear check). **APPROXIMATED:** the settler-chatter *trigger* is still a render-side stochastic emitter (the original speaks on selection/idle events we don't model yet); sex/age now match the settler, but voices are viking-only (the sim carries no *tribe* on a settler yet — other tribes' pools exist in the bank); the terrain→ambient join is coarse — `terrainPatterns` already collapses each landscape typeId to ONE representative `GfxPattern`, so a cell inherits only that pattern's `editGroups` (most land → `Meadow Green`), losing the original's per-pattern-group variety. **DEFERRED:** landscape-object ambient (tree birds / stone beds — needs on-screen-object group plumbing from the render map-object layer), unit voice-on-click / event-accurate voices (`humans/sounds.cif` per-tribe/sex `respond ok/no` on selection + `scream` on hit — the ambient chatter above is the murmur, not the response layer), the full per-animation `LogicSoundType`→group table (118 groups extracted, a handful bound), combat/other event SFX. **OUT OF SCOPE:** background music — the original streamed it from CD via `gedx8musicdrv.dll` (DirectMusic); no music files ship (only the 9 jingle wavs). **VALIDATION TOOLS** (the audio equivalents of `?anim`, since an agent can't judge audio): `?scene=sound-showcase` — a ring of woodcutters chopping inexhaustible trees around a store, so the axe fires on every swing, centred + on-screen (its headless half asserts the harvest mechanic the sound rides on); and `?sounds` — a click-to-audition gallery listing every action→sound binding, the voice pools by sex/age, the jingles and the ambient beds (pure model unit-tested; `buildSoundGalleryModel`). Hands-on (real browser, 0 console errors): `?scene=sound-showcase` → repeated `/sounds/static/axe0N.wav` (all three axe clips, and voices all **male** — 0 female/child leaks); `?live` harvest → `/sounds/static/axe0N.wav`; `?map=<id>` → `/sounds/ambient/meadow1.wav`; the `?sounds` gallery ▶ → the clicked wav (all 200). `?sound=off` opts out; a checkout without `content/` degrades to silence. |
| Chop swing length + felling pace (`HARVEST_SWING_LENGTH`, `WOOD_CHOPS_TO_FELL`) | approximated | A harvest atomic's duration IS its bound animation `length` (`ai.ts atomicDuration`), and the 15-frame woodcut bobseq plays tick-locked off `elapsed`; the atomic is removed the tick `elapsed` reaches `length`, so a clean ONE-swing run ending on the impact frame needs `length = CHOP_STRIDE + 1` (16). That length is the ONE global `HARVEST_SWING_LENGTH` (`content/settler-gfx.ts`, derived from the render swing stride) every scene/slice binds — a per-scene number drifted (a scene once set 6, replaying only the axe *windup* then restarting; the "axe never strikes" glitch), so a scene can no longer mistune it. So ONE chop atomic = ONE full axe strike (faithful: the original's collector chop atomic is one swing, ~30 ticks). How many strikes fell a tree is the SEPARATE `WOOD_CHOPS_TO_FELL` calibration held in the ONE global source `catalog/felling.ts` (not re-picked per scene) — currently 6, by-eye (no oracle; OpenVikings' sim is a stub), SCENE-ONLY (the golden fixture's `viking_chop` is unchanged). |

## Deviations (conscious divergences from the original)

Format: `- <mechanic>: <how it differs> — <why> (<commit>)`.

- Instant fell (no falling animation): a felled tree swaps standing → stump + trunk INSTANTLY in one
  tick — the standing `Resource` is destroyed and the `Stump` + `GroundDrop` trunk appear the same tick.
  The original plays a "tree falling" transition (`ls_trees_dead` "tree_dead falling", landscape
  logicType 5) before the trunk settles; v1 skips that in-between and the stump draws the static
  `ls_trees_dead` "tree debris" frame. The falling-animation stage is render polish, deferred to the
  render-ladder's faithful-per-direction-timing rung (ROADMAP Phase 2 render ladder / "the tree falling
  ANIMATION stage"). The MECHANIC (chop → fall → trunk → collect) is faithful; only the transition
  frames are elided. (feat: multi-hit harvest + drop-on-ground)

- Felling calibration (chops-to-fell + per-tree yield): a fellable node's chop count and whole-tree wood
  yield are OBSERVED constants held as content (`GoodGathering.chopsToFell` / `yieldPerNode`), NOT
  extracted — the readable `.ini` carries no such params **for the wood collector**: `goodtypes.ini` wood
  carries only a single `atomicForHarvesting`, and the collector's experience track (job 8, "collector
  wood") has no `baserepeatcounter`. NB `baserepeatcounter` *does* exist in
  `humanjobexperiencetypes.ini` for the sibling gathering jobs (hunter/fisher/farmer — the "repeat the
  work atomic N times" datum this constant stands in for), so it is the likely fidelity pin for **Step
  4**'s per-good yields; it is simply absent for the collector, so wood's chop/yield stays observed for
  now. The extractor emits 0 (= single-hit / pending calibration); the SCENE calibration lives in ONE
  global source (`catalog/felling.ts` — `WOOD_CHOPS_TO_FELL`/`WOOD_YIELD_PER_NODE`, currently 6 chops / 3
  wood), NOT a per-scene number, so every fellable-tree scene stays in step; the golden fixture sets its own
  demonstrative value. A calibration-by-observation pass against the running original (or extracting
  `baserepeatcounter` for the repeat-jobs) will pin the real numbers — a data diff, no code change.
  (feat: multi-hit harvest + drop-on-ground; fix: global felling calibration)

- Tree sprite (per-object species + frame): every wood `Resource` node draws ONE representative tree —
  bob 60 of `ls_trees.bmd` recoloured `tree_yew01`, the `[GfxLandscape] "yew 01"` full-grown frame —
  but the original assigns a *per-object* species + growth/sway frame from the map's pre-placed-object
  layer (`map.dat` `eatd`/`eald`, not yet decoded — docs/SOURCES.md). So a faithful-once-decoded map
  would show varied species/sizes; we show a uniform yew. The bind path itself is faithful (the decoded
  `landscapes.cif` `[GfxLandscape]` graphics record via `extractLandscapeGraphics`); only the per-object
  selection is collapsed to a constant, and the species/frame constants (`TREE_ATLAS`/`TREE_BOB` in
  `app/src/content/building-gfx.ts`) are a first pick a human swaps after eyeballing. (landscape tree-bob bind)

- Terrain ambient scope + join coarseness: the sound layer plays ambient beds keyed on the terrain
  under the viewport, but only the TERRAIN-pattern beds (meadow/water/desert/…), and through a coarse
  join — `terrainPatterns` collapses each landscape typeId to one representative `GfxPattern`, so a cell
  inherits only that pattern's `editGroups` (most land resolves to `Meadow Green`), flattening the
  original's per-pattern-group variety. The LANDSCAPE-OBJECT beds (forest birds over trees, stone beds)
  are not wired — they need on-screen-object group plumbing from the render map-object layer. The
  *mechanism* is faithful (the original keys ambient off pattern/landscape groups); the join fidelity is
  pinned to the approximated `terrainPatterns` table, and the object beds are a recorded follow-up.
  (audio ambient terrain join)

- Building sprite (per-type frame + atlas coverage + render scale): every viking building draws its OWN
  `[GfxHouse]` `LogicType`→`GfxBobId` bob, data-pinned via the extracted `buildingBobs` IR (all 40 viking
  typeIds resolve, 0 fall-backs; the render join `buildingBobRefsByType` in `app/src/content/building-gfx.ts`
  reduces per `(tribe, typeId)` and overlays onto the transcribed `VIKING_HOUSE01_BOBS`). The bind path is
  faithful (decoded `[GfxHouse]` graphics via the universal `.bmd`→atlas path). Approximated: (1) atlas
  coverage — only the viking families load; the OTHER tribes (frank/egypt/saracen/byzantine) are a later
  rung; (2) uniform `BUILDING_SCALE` 0.7 about the feet — native bobs render ~6–10× the settler, one by-eye
  factor per family (`familyScales` overridable). Standing human pixel sign-off: `?scene=all-buildings`
  (all 41 viking types at once, real graphics by default). (building per-type house-bob bind)

- Chopping settler position: a mid-chop woodcutter's drawn sprite is nudged left (`CHOP_NUDGE_X` in
  `render/src/data/scene.ts`) so its right-swing axe lands in the tree it shares a cell with — a render-only
  visual offset (sim position + depth sort unchanged). The faithful behaviour is to stand the settler on
  an ADJACENT cell facing the tree, which needs harvest-adjacency in the planner (it still stands the settler ON the tree; the pathfinder is 8-connected now, but approach-and-face is a separate behavior);
  the nudge is the by-eye stand-in until then. (chop-position render nudge)

- Settler walk pace (acceptance scenes): the gather scenes spawn the woodcutter with an explicit
  `moveSpeed: 8` (ticks/tile — a per-entity `MoveSpeed`, half the universal `MOVE_SPEED_PER_TICK` =
  4 ticks/tile), so it walks a calmer, non-sliding pace for the human sign-off. This is SCENE-ONLY: the
  global default is unchanged, so the sim goldens are untouched (the separate-optional-component pattern).
  Retuning the *global* settler pace to the visually-validated value is a deliberate follow-up — deferred
  here only to avoid the broad golden-hash/trace churn a global change forces mid-slice. The pace
  magnitude has no oracle (OpenVikings' sim is a stub), so the chosen value is a by-eye approximation, not
  a pinned param. (scene settler walk pace)

- Needs — rise rates + drive triggers (hunger / fatigue / piety / enjoyment): `needsSystem` raises each
  bar at a flat per-tick constant (`HUNGER`=ONE/4096, `FATIGUE`=ONE/8192, `PIETY`=ONE/16384,
  `ENJOYMENT`=ONE/32768) and the planner satisfies it at a flat `…_THRESHOLD`=¾·ONE. The original instead
  drains each bar via per-animation `event <at> <type> <value>` deltas on a ~10000-scale bar (type 1=rest,
  2=hunger, 3=leisure, + a pray channel) and restores it on the satisfy atomic. **Pinned:** every reset
  *atomic id* is the original `setatomic` binding (eat 10, sleep 8, pray 12, enjoy 17, make_love 78) and
  those deltas are now extracted (`atomicEventChannelDelta`). **Unpinned/deferred** (no oracle for the
  trigger cadence): the rise rates, the ¾ thresholds, and food identification (by `food_` id prefix — no
  readable `iseatable` flag), until an event-driven needs DRIVE consumes the real deltas. Drive shape: eat
  resets at a store; sleep **in place** (original sleeps at home — needs the home system); pray is
  **target-bound** — walks to the nearest **temple**, inferred by the structural signature
  (no-recipe/no-worker/no-stock `workplace` = `HOUSE_TYPE_WORK_TEMPLE` lt 37, since the building→need link
  isn't a readable flag); `enjoy`/`make_love` share the leisure bar (make_love is a second reset of it, not
  a new need) and have **no** drive — no house carries a distinguishable leisure satisfier, so rise+reset
  are wired but no planner branch chooses them. All are the right "need lapses → satisfy → reset" shape;
  the rates/curve are the recorded faithful target.

- Experience threshold (`needfor*`): `experienceRequirementMet` reads a `need` requirement as "the sum
  of the settler's accrued XP across the line's `experienceTypes` ≥ `amount`", but the original measures
  the threshold per the same below-the-`.ini` XP logic the per-animation `event` deltas live in — for a
  two-`expType` line, whether it means "sum both" or "either alone" has no readable oracle. Summing the
  named tracks is the deterministic reading (a single threshold over the relevant specializations); the
  threshold `amount` and the track keying ARE pinned (the join key is the same `humanjobexperiencetypes`
  typeId the accrual writes), only the multi-track composition is inferred. Refine when the original's XP
  curve is observed. (No `baseRepeatCounter`→tier curve is interpreted yet — also deferred.)

- Production per-cycle ticks: the building-type `recipe.ticks` is pinned to ONE **reference tribe**
  (lowest-`typeId`), but the source's production-animation `length` varies per tribe (e.g. viking
  coiner=200 vs frank coiner=60) and per output good. The merged building-type recipe carries a single
  `ticks`, so the per-tribe / per-output spread is collapsed to the reference tribe's primary-output
  length. The fully-faithful model is a per-tribe (and ideally per-output) recipe-timing table, deferred
  until there is tribe context at the sim/economy layer — strictly more faithful than the prior flat
  `DEFAULT_RECIPE_TICKS`=20 constant, which carried no source at all.

- Reproduction (birth rate + anchor + sex): `reproductionSystem` grows a tribe by **one settler per
  tick** while it has spare housing, births each newborn at the tribe's **lowest-id built `home`** tile,
  and gives it the `baby_female` sex. All three are unpinned: the original's birth cadence, family model,
  where a child appears, and which sex live below the readable `.ini` — there is no birth-rate key in
  `houses.ini`/`tribetypes.ini`, and the `make_love` atomic restores the leisure channel (id 78 →
  channel 3, like `enjoy`), it carries no birth yield. The newborn's **age class** (that it is a *baby*,
  not an adult trade) IS now pinned — `NEWBORN_AGE_CLASS` = `baby_female` (id 1) from `logicdefines.inc`
  `JOB_TYPE_HUMAN_BABY_FEMALE` + the `jobtypes.ini` records (`systems/lifecycle/ageclass.ts`); only *which sex* is
  the deterministic stand-in (the youngest age class, lowest id). The faithful *shape* — "with room to
  live, the colony grows toward the housing ceiling and stops there, as babies" — IS matched (the cadence
  is the gate: deterministic, self-limiting at `housingCapacity`, so the `populationWithinHousing`
  invariant can't be breached). The rate, the per-home anchor, and the sex remain the recorded faithful
  target, deferred until an oracle or calibration-by-observation against the running original. Strictly
  the right "grow into your housing, born young" shape, with no birth-rate constant invented.

- Growth cadence (baby→child→adult): **now LANDED** — `growthSystem` (`systems/lifecycle/ageclass.ts`) matures a
  born settler over `GROWUP_TICKS`=8192 per stage, sex preserved, removing the `Age` component once it is
  adult-eligible (see the GrowthSystem table row). The *structure* (age-class ids + female/male split) is
  faithful; only the **cadence** `GROWUP_TICKS` is the approximated, unpinned constant (no readable
  grow-up-rate key) — the established hunger-rise-style stand-in, calibration-by-observation pending. The
  **earlier known boundary is now closed:** the AI planner skips a still-growing settler (keyed on the
  `Age` component), so a baby/child no longer runs the adult eat/sleep/pray drives — faithful to "a baby
  is cared for, it doesn't self-feed". The `make_love`-driven family/pairing model (which settlers pair,
  whose child it is) remains a separate deferred mechanic.

- Carrier→vehicle pairing (cart logistics): **a conscious, recorded deferral — no per-carrier vehicle
  entity is modelled.** A carrier hauls abstractly at its tribe's best UNLOCKED carry capacity
  (`carrierCarryCapacity`: the largest `stockSlots` over the vehicles `jobEnablesVehicle` has unlocked,
  floor 1 on foot) — the *capacity number* and the *unlock* are both pinned to data, but the original's
  cart logistics are NOT: it assigns a specific vehicle per haul, a carrier visibly fetches/parks a cart
  at a vehicle store, and a vehicle's `logicgood` allow-list (which goods that cart may carry — each
  vehicle lists a per-type subset of good ids; a ship's set differs from a handcart's) constrains the
  haul. **This is oracle-blocked**:
  `vehicletypes.ini` carries only the static type table (`type`/`name`/`logicsize`/`stockslots`/the
  `logicgood`+`logicpassenger` allow-lists/`debug*` cosmetics) — there is **no** carrier-job→vehicle
  binding, no dispatch rule, no "fetch a cart" key; that behavior lives in the engine logic below the
  readable `.ini`, and OpenVikings' sim is a stub (no logistics to diff against). So modelling a
  per-carrier vehicle entity now would be *invented*, not faithful. Two specific sub-divergences are
  folded in: (a) the `logicgood` allow-list is now applied **on the boat-as-mobile-store DEPOSIT side** —
  `stockCapacity` (`systems/shared.ts`) gates a haul INTO a placed `Vehicle` hull by `vehicleMayCarry` +
  `stockSlots` (the *Boat hull* row's cargo-load gate), so a forbidden good is refused at the deposit
  seam — but it is **still not applied on the carrier CARRY-CAPACITY side**: `carrierCarryCapacity` takes
  the best `stockSlots` over the unlocked vehicles regardless of whether that cart's `cargoGoods` permits
  the good being hauled (a ship's 200-slot capacity would size a batch even for a good no cart's allow-list
  permits). The deposit gate is the WHAT-a-hold-accepts half; wiring the filter into the carry-BATCH sizing
  rides with the per-carrier cart entity (the data is ready, the dispatch is the blocker); (b) the carrier *dispatch* (which
  carrier takes which haul) is the nearest-Manhattan stand-in, and "never deliver back into the producer"
  is our unpinned choice. The faithful *shape* — "a carrier moves a batch of finished goods to a store,
  bigger as the tribe unlocks bigger carts" — IS matched; the cart-as-entity, the per-vehicle goods
  filter, and the dispatch policy are the recorded faithful target, deferred to a dedicated vehicle-entity
  slice once an oracle exists (calibration-by-observation against the running original, or a future
  OpenVikings logistics decode). This closes the JobSystem's last unmodelled Phase-3 behavior as a
  *decision*, not a stub: the data is consumed, the divergence is knowable, the faithful path is named.

- Per-tribe construction cost: **a conscious, recorded collapse — `BuildingType.construction` holds one
  cost per `typeId`, not per `(tribe, typeId)`.** The graphics table (`DataCnmd/budynki12/houses/houses.ini`,
  `[GfxHouse]`) keys each `LogicConstructionGoods` line by a `LogicTribeType`, and the SAME logic `typeId`
  carries a genuinely DIFFERENT build cost per civilization: viking/frank/byzantine model a residence as an
  upgrade *chain* (typeId 6 = home level 4 = `27 27`, ornaments only — the incremental tier cost), while
  egypt/saracen model that same typeId as a standalone *full* build (the cumulative `4 5 5 2 … 27 27` list).
  `extractConstructionCosts` collapses this to the **lowest-`LogicTribeType`** record (the deterministic
  "reference tribe" convention `fillBuildingRecipes` already uses for `recipe.ticks`), so a leveled `home`
  reads its OWN tier's upgrade cost — coherent, but it loses the per-tribe spread (a saracen residence costs
  the viking chain's tier cost in the IR, not its own cumulative figure). **A second collapse rides along:**
  within ONE `[GfxHouse]` record a `typeId` can map to more than one `sizeIdx` (e.g. "viking pottery"
  `LogicType {1:21, 2:21}`, and a multi-stage wonder repeats one typeId across rising sizes 0,1,2 with
  cumulative costs `5 5 5 → 6 6 5 5 5 → 7 7 6 6 5 5 5`); the **lowest `sizeIdx`** (the base/first build stage)
  wins, so a wonder reads its first-stage cost, not the cumulative final. Both are **not** oracle-blocked (the
  per-tribe / per-stage data is fully readable); they are deliberate shape choices to keep `construction` a
  single flat field until a system consumes the richer key. The fully-faithful model is a per-`(tribe, typeId,
  sizeIdx)` construction table — the same kind of per-tribe deferral the `recipe` table carries (the
  *Production per-cycle ticks* row, axis (a)) — landed together when a tribe/stage-aware ConstructionSystem
  needs it.

- Settler facing (bob direction layout): the `CR_Hum_Body` bobs do **NOT** lay their 8 directions out as a
  uniform screen-angle rotation, so no `atan2`-bucketing formula can pick them. The true per-block facing
  (a human read off each labelled direction block) is `0 SW, 1 W, 2 NW, 3 NE, 4 E, 5 SE, 6 S, 7 N`.
  `readFacing` (`packages/render/src/data/scene.ts`) therefore maps the settler's grid step **directly** to the
  block facing that step's iso-screen heading via the `STEP_TO_FACING` table (`E→SE→5, S→SW→0, W→NW→2,
  N→NE→3`, plus the four diagonal steps), with no angle/rounding. **Earlier this used a screen-angle
  formula assuming a regular rotation** — it happened to get the SE heading right but rendered other
  headings facing the wrong way (e.g. a settler walking screen-up-left faced down-left). The direct table
  is a faithful match to the asset, not an approximation; `DEFAULT_FACING` is block 5 (SE, toward camera).
  (settler facing — direct grid-step→block table)

- Movement / facing granularity — **8-connected movement + full 8-facing gait (LANDED)**: pathfinding is
  now 8-connected — `packages/sim/src/nav/terrain.ts` `steps()` emits diagonal steps alongside the
  orthogonal `NEIGHBOUR_OFFSETS`, at √2 cost under the octile heuristic, with a no-corner-cut rule (see
  the *A\* pathfinding* row). Because a path leg can now be a **diagonal grid step**, the render's
  `readFacing` (`packages/render/src/data/scene.ts`, `STEP_TO_FACING`) produces the four screen-cardinal
  facings a diagonal maps to (W/E/S/N = blocks 1/4/6/7) as well as the four screen-diagonal facings a
  single-axis step maps to (SW/NW/NE/SE = blocks 0/2/3/5) — so **all eight** `CR_Hum_Body` directions are
  now reachable in both the empty walk and the loaded carry gait (the old 4-facing cap is gone). The
  per-axis `MovementSystem` already walks a diagonal waypoint on both axes, so no movement change was
  needed. **APPROXIMATED (behavior, no oracle):** whether the REAL engine moves on an 8-connected graph
  (vs finer/continuous), and its exact diagonal cost + corner policy, are unpinned — the original's
  humans visibly walk 8 directions, which motivates the choice, but OpenVikings' sim is a stub. Per-
  DIRECTION screen speed is **not** normalised: `stepToward` keeps every cell-step at the same tick cost
  (a diagonal covers √2 tiles in the same ticks), and since the iso projection makes tile→screen speed
  anisotropic anyway, uniform screen speed would be a separate calibration-by-observation, not attempted
  here. (8-connected movement + 8-facing gait)

- RTS unit orders (select + box-select + move for civilians): the player can rubber-band-select the
  human player's units and right-click to send them anywhere (`moveUnit` command → `MoveGoal`), the
  standard-RTS control scheme. The ORIGINAL *Cultures* does NOT box-select-and-move civilian settlers —
  they are autonomous, steered indirectly (the "hand" picks up/drops a settler; professions are assigned;
  soldiers get direct orders). This RTS-style direct control is a **conscious quality-of-life deviation**,
  requested for the rebuild. It is kept faithful *in spirit* by making the order a SOFT, TIMED override,
  not a seizure (see below): the economy AI reclaims the unit after a hold, so autonomy — the soul of
  Cultures — is never disabled, only briefly deferred. The `Owner{player}` ownership model + the 16-player
  cap (`components/ownership.ts`) that gate "whose units you may command" are new scaffolding for the
  ROADMAP's up-to-N-players target, not an original data structure. (unit selection + move orders)

- Player move-order dwell (`MOVE_ORDER_HOLD_CIVILIAN`=50, `MOVE_ORDER_HOLD_SOLDIER`=300 ticks;
  `systems/conflict/orders.ts`): after a moved unit ARRIVES it stands `holdTicks` before the economy AI
  re-tasks it — short for a worker (it returns to its job), long for a combatant (it holds position),
  chosen by whether the unit carries a `Health`/`Weapon` (combatant). The needs drives (eat/sleep/pray)
  still preempt the hold, so a warrior may wander off to eat — the faithful "he'll soon go do his own
  thing" the user described. **Approximated (no oracle):** the two dwell magnitudes + the combatant-vs-
  civilian split are by-eye — the original's exact dwell / whether it even distinguishes the two is below
  the readable `.ini` and OpenVikings' sim is a stub. The interrupt-on-order (a move cancels the current
  atomic) and the per-player check being absent (any owned unit is orderable — no issuing-player field
  until lockstep) are the other unpinned choices. Inert in the goldens (no `moveUnit` in the golden
  stream; `PlayerOrder` is a separate optional component → golden hash untouched). (move-order dwell)

- Combat hostility axis — OWNER-based, binary (`mayTarget` in `systems/conflict/combat.ts`): two OWNED
  combatants of DIFFERENT players are enemies; SAME player are friendly (a player's mixed-tribe army
  never fights itself); at least one side unowned falls back to the existing content relations (tribe
  hostility `mayAttack` + hunter predation + provoked anger, all unchanged). This is what lets a battle
  scene be viking-vs-viking told apart by `Owner.player`, not tribe. **Approximated (our design, no
  oracle):** hostility is **binary** — no alliances / diplomacy / neutrality between players (every two
  distinct players are mutually hostile). The original's multi-player relations are a Phase-5 concern;
  when they land, the "different player ⇒ enemy" branch becomes "different player AND not allied". Inert
  on the goldens (owner is a separate optional component; no owned combatant in the golden stream).
  (combat hostility axis)

- Combat sight radius (`SIGHT_RADIUS_TILES`=8, `systems/conflict/combat.ts`): how far an OWNED combatant
  SPOTS an enemy to advance on it (the walk-into-melee aggro range). Humans carry **NO** readable sight/
  aggro field in the data — only animals have leash radii (`maximumdistancetobirthpoint`/`…staypoint`),
  and OpenVikings' sim is a stub — so this is a single **calibration-by-observation pending** constant
  (step 10), NOT a pinned param. The weapon's `[minRange, maxRange]` reach band (where a swing lands) is
  separate and faithful (verbatim `weapontypes`). Unowned combatants have no advance drive (their search
  radius is just `maxRange`), so their behaviour is unchanged. (combat sight radius)

- Combat walk-into-melee + chase repath cadence (`REPATH_CADENCE`=8, `systems/conflict/combat.ts`): an
  owned combatant whose nearest enemy is in sight but beyond weapon reach walks toward an **approach
  cell** (a walkable cell in the enemy's reach band closest to the unit — so a melee unit stops one cell
  short, not on top of the enemy), then stops and swings once in the band. The chaser re-issues its path
  toward a moving target only every `REPATH_CADENCE` ticks (following its live route in between), so a
  field of chasers never triggers a per-tick full re-path (golden rule 7; the pathfinding budget is
  `PATHFINDING_BUDGET_PER_TICK`=8 anyway). An engaged unit skips economy planning (an `Engagement` marker
  the AISystem reads — the `PlayerOrder`-skip pattern, so needs still preempt). **Approximated (our
  design, no oracle):** the advance-on-the-enemy behaviour, the approach-cell choice, and the repath
  cadence magnitude are the original's undocumented "soul"; this replaces the *Combat targeting drive*
  row's old "attacker stays put at range" approximation for OWNED combatants (the swing-in-place path
  stays for unowned wildlife). Inert on the goldens (no owned combatant carries `Engagement`).
  (walk-into-melee / chase cadence)

- Attack order (`attackUnit` command → `AttackOrder`, `systems/conflict/orders.ts`): selected owned
  combatants get a FOCUSED target they chase and strike regardless of the sight radius, until it dies /
  stops being a valid target (then they revert to auto-engagement). The app maps a right-click on an
  enemy unit to this (`view/unit-controls.ts`); move-order-onto-an-enemy = attack is the ORIGINAL's RTS
  idiom, so this is an **observed-approximation** (the convention is faithful; the exact focus/leash
  behaviour is our deterministic design). It reuses the SOFT-override philosophy of the move order (the
  economy leaves an engaged unit alone via `Engagement`, but needs still preempt) rather than a timed
  `PlayerOrder` hold — an attack order's "hold" is naturally the duration of the fight. Hostility is
  re-validated each tick (a stale/illegal order self-corrects), and the per-player authority check is
  absent until lockstep (any owned combatant is orderable — the same stance as `moveUnit`). Inert on the
  goldens (`AttackOrder` is a separate optional component; no `attackUnit` in the golden stream).
  (attack order)

- Idle-spacing / soft collision (`deStackIdle` in `systems/conflict/ai.ts`, `SPACING_SEARCH_CAP`=48):
  settlers do NOT hard-collide — a walker passes freely through any occupied tile (so a crowd can thread
  the gaps between village buildings without deadlocking) — but they will not come to REST stacked on one
  another. When an owned settler has ARRIVED with nothing left to do (every economic + needs + player-order
  drive above it declined) and shares its tile with a *lower-id* resting owned settler, it steps to the
  nearest free, unblocked cell (a canonical breadth-first ring search); the lowest-id occupant is the
  keeper and holds its ground, so a stack of N spreads to N distinct tiles and then stops (no churn).
  **Faithful in spirit:** the original engine gives every landscape type a per-cell `maximumValency` (the
  extracted `maxValency`, already on the terrain graph) — units cluster only up to a cell's capacity — so
  "don't rest stacked" is the original's model, not an invention. **Approximated (no oracle):** the target
  is *one settler per resting cell* (not the type's real valency number — the readable data gives the cap
  but not how the engine distributes overflow), the de-stack is a last-resort planner drive rather than a
  reservation held during pathing, and the "reluctant to ENTER an occupied tile" pathfinding penalty the
  user also described is deferred (the de-stack at rest already delivers the visible result — units don't
  pile up — so A* stays unweighted; a soft occupancy cost is a later refinement, docs/TECH-DEBT.md). Gated
  on `Owner`, so it only ever moves gameplay units; the unowned golden/economy fixtures build an empty
  occupancy set and are byte-identical. Group move-orders reach distinct tiles via a client-side formation
  (app `view/picking.ts` `formationTiles`), with this de-stack as the sim-side safety net. (idle spacing)
