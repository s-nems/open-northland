# Global real content — run the sim on the extracted `ir.json` (agent prompts)

Goal: stop running the sim on the hand-made `sandboxContent()` ContentSet and instead run it on the
**real extracted `content/ir.json`** (all 65 goods, real per-building `stock`, real recipes, real good
typeIds), globally. No per-scene good set. Once the sim runs on real content, the whole HUD shows real
data **with zero UI changes** — the details panel, tool panel, and slice all read `sim.content`
generically (`view/game-view.ts` `content: sim.content`, `hud/details-panel/model.ts` reads the
ContentSet). This is why the details-panel Magazyn "list a store's accepted goods" work does not need
to wait for this; it benefits automatically when this lands.

**How to use:** run each prompt below, in order, in a fresh session (`/worktree`); merge before the next
(later stages consume earlier outputs). Prompts are self-contained and re-verify facts against sources —
this doc is research output (2026-07-09), not ground truth. When a step merges, tick its box and delete
its prompt block. Delete this file when all steps land.

Research basis (2026-07-09, established by reading the branch + the served `content/ir.json`):

- **The panel already reads content generically** — `sandboxContent()` → `new Simulation({content})` at
  `scenes/runtime.ts:32` (`createSceneSim`) and `slice/vertical-slice.ts:133,203`; the sim just stores it
  (`packages/sim/src/simulation.ts:48`). Content is injected purely at the app boundary; **the sim package
  never fetches**, so the loader stays app-side and the sim stays pure.
- **ir.json is NOT drop-in `parseContentSet`-able on this branch (Stage 0 blocker).** `ContentSet` is a
  `z.strictObject` (`packages/data/src/schema.ts:1394`); the served file carries two keys the branch's
  schema lacks — **`gfxAtomics`** and **`trianglePatternTypes`** — so `parseContentSet(irJson)` throws.
  The file was generated by a newer main-repo `@vinland/data` (symlink → `…/vinland/content`, main at
  `41ea453`). Reconcile first: rebase this branch's `@vinland/data` onto main, or add those two arrays to
  the `ContentSet` strictObject, or strip-before-parse.
- **Good typeIds fully mismatch.** Sandbox fabricates `GOOD_WOOD=1, PLANK=2, COIN=3, STONE=4, MUD=5,
  IRON=6, GOLD=7, MUSHROOM=8` (`game/sandbox/ids.ts`); real ir.json uses engine numbering
  `water=1, mud=2, stone=3, wheat=4, wood=5, iron=6, gold=7, coin=8, … mushroom=14`. Every sandbox good id
  is wrong under real content. Coupling surface (ref counts): `game/sandbox/content.ts` (57),
  `game/sandbox/ids.ts` (34), `scenes/sandbox.ts` (18), `slice/vertical-slice.ts` (6),
  `game/sandbox/place.ts` (3), `scenes/sandbox-queries.ts` (2).
- **Already real (no change):** `catalog/buildings.ts` `VIKING_BUILDINGS` (typeId 1..41) are transcribed
  verbatim and match ir.json; `catalog/atomics.ts` harvest atomics match `goods[].atomics.harvest`.
- **The "dead economy" trap (riskiest).** Real ir.json is graphics-complete but **gameplay-thin**:
  there is **no `plank` good at all** (the slice's woodcutter→joinery→plank economy rests on a fabricated
  good), and `goods[].gathering` fields are all `0` (`chopsToFell/yieldPerNode/depositSize/depositLevels`
  — the extractor never recovered them; the sandbox supplies them as clean-room pins in
  `catalog/felling.ts` + `catalog/mining.ts`). The gathering system stamps `Felling{chopsLeft}`/`MineDeposit`
  from those content fields, so on **raw** real content nothing fells or mines — a settlement that stores
  49 goods correctly but never harvests or produces. The real work is a **re-key + balance overlay**, not a
  swap.
- **Golden blast radius.** Sim-package goldens **do NOT move** — every sim test builds `testContent()` from
  `packages/sim/test/fixtures/content.ts`, never `sandboxContent`. If a sim golden moves, a real-content
  leak crept into the pure sim (stop, per `packages/sim/AGENTS.md`). App-level goldens that **do** move:
  `packages/app/test/{vertical-slice,details-panel,gathering-render,resource-gfx,scenes}.test.ts` and the
  committed 6×1-strip shot PNG (`entries/shot.ts`, `scripts/shot.mjs`). Stockpiles seed from
  `type.stock[].initial` at placement (`packages/sim/src/systems/command.ts:183`), so real per-building
  stock (HQ = 49 slots) flows into the sim with no panel-code change.

Scope note: this is cross-cutting (`@vinland/data` schema + a new app-side Node content loader +
`game/sandbox/*` re-key + `slice` + `scenes` + ~5 app goldens + the shot PNG). It is deliberately its own
plan/branch, NOT part of `feat/original-ui-details` (a focused UI diff) — mixing them buries a risky
architecture change in a UI review and moves the UI goldens twice.

- [ ] 0. Reconcile the schema skew (blocker) — make `parseContentSet(content/ir.json)` +
  `validateCrossReferences` pass end-to-end on the real file.
- [ ] 1. Global real-content loader — one `loadRealContent()` returning a validated `ContentSet`
  (Node `readFileSync` for tests/slice; the existing browser fetch for the app), memoized like `loadIr`.
- [ ] 2. Re-key + balance overlay (riskiest) — real ContentSet as the base, sandbox balance re-keyed to
  real good ids and overlaid onto the goods the extractor left at 0; decide `plank` (pick a real produced
  good or inject a synthetic recipe on a real workplace — no real `plank` exists).
- [ ] 3. Feed sim + panel — point `createSceneSim` and `runSlice`/`runAuthoredSlice` at the merged real
  content; verify no gameplay regression (economy still lives, buildings still place/select).
- [ ] 4. Move goldens intentionally — rebuild the strip PNG; update the five app tests to the real good
  ids/stock; sim-package goldens must stay byte-identical.

---

## Step 0 — Reconcile the schema skew

```text
Make the served content/ir.json parseable by this branch's @vinland/data. content/ is a symlink to
../vinland/content, generated by a NEWER main-repo schema: it carries top-level keys `gfxAtomics` and
`trianglePatternTypes` that this branch's ContentSet (a z.strictObject at packages/data/src/schema.ts
~1394) does not model, so parseContentSet(irJson) throws on unknown keys.

Pick the lowest-risk reconciliation (verify which against the real repo state, do not assume):
(a) rebase this branch's @vinland/data onto main (main already models both keys), or
(b) add `gfxAtomics` + `trianglePatternTypes` arrays to the ContentSet strictObject with the same shapes
    main uses, or (c) strip those two keys before parse (only if they are pure render lanes the sim never
    reads — confirm against schema.ts and the sim systems first).

Verify: a Node script (or a new test) fetches/reads content/ir.json, runs parseContentSet +
validateCrossReferences, and passes with zero errors. `npm run build` + `npm test` + `npm run check`
green. Do NOT yet change what the sim runs on — this step only makes the real file valid.
```

## Step 1 — Global real-content loader

```text
Add ONE app-side loader `loadRealContent(): Promise<ContentSet>` (or sync for Node) that reads
content/ir.json and returns a parseContentSet-validated ContentSet, usable from BOTH the browser app
(fetch) and Node tests/slice (readFileSync) — the sim package must NOT fetch (it stays pure; content is
injected at the app boundary, see scenes/runtime.ts:32). Memoize like content/ir.ts loadIr(). This is the
single source that will replace sandboxContent() at scenes/runtime.ts and slice/vertical-slice.ts. Do NOT
wire it into the sim yet — just land the loader + a test that it returns the full 65-good / 55-building set.
```

## Step 2 — Re-key + balance overlay (riskiest)

```text
Build the merged content: real ir.json ContentSet as the BASE, with the sandbox's clean-room BALANCE
overlaid, re-keyed to real good typeIds. This is NOT a swap — real ir.json is graphics-complete but
gameplay-thin (see the plan's research notes: no `plank` good; goods[].gathering fields are all 0, so
nothing fells/mines without the overlay).

Do: map fabricated→real good ids (WOOD→5, STONE→3, MUD→2, IRON→6, GOLD→7, MUSHROOM→14, COIN→8); overlay
catalog/felling.ts + catalog/mining.ts balance onto the real goods' gathering.chopsToFell/depositSize/
depositLevels (real values are 0); keep the gatherer/carrier/soldier job tuning + atomicAnimations lengths
+ tribe atomicBindings, rebound to the real job ids (or the real equivalents: collector=8, builder=7, …).
Decide `plank` explicitly: either retarget the joinery recipe to a real produced good, or inject a
synthetic recipe on a real workplace, and NAME the approximation in a comment (no real plank exists).

Verify: a headless test builds the merged content and asserts the economy is alive — wood fells, a mine
deposit depletes, the joinery (or its replacement) produces. Determinism preserved (seeded RNG only).
```

## Step 3 — Feed the sim + panel

```text
Point createSceneSim (scenes/runtime.ts) and runSlice/runAuthoredSlice (slice/vertical-slice.ts) at the
Step-2 merged real content instead of sandboxContent(). The panel/tool-panel need NO change (they read
sim.content generically). Sprite-sheet resolution already keys off sim.content.goods (entries/scene.ts:38).

Verify in-browser (uiscale 1): ?scene=sandbox places+selects real buildings; the Magazyn shows a real
store's real accepted-goods larder (HQ ~49 slots) with icons; the economy runs. Screenshot for the human
oracle. No console errors.
```

## Step 4 — Move goldens intentionally

```text
Update the app-level goldens the content switch legitimately moves: rebuild the 6×1-strip shot PNG
(scripts/shot.mjs), and update packages/app/test/{vertical-slice,details-panel,gathering-render,
resource-gfx,scenes}.test.ts to the real good ids/stock. The sim-package goldens
(determinism/golden-trace/hashtrace/fuzz, built from test/fixtures/content.ts) MUST stay byte-identical —
if any sim golden moves, a real-content leak reached the pure sim; stop and reassess
(packages/sim/AGENTS.md). `npm test` + `npm run check` + `npm run build` green.
```
