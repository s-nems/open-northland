# Mushrooms

`atlas-alpha.png` and `asset.json` produce 36 mushroom sprites with the map slot names, frame sizes
and anchors recorded in their runtime manifests.

Run `npm run art -- build terrain/mushrooms`, then `npm run art -- review terrain/mushrooms`. Publish the approved candidate with `npm run art -- publish terrain/mushrooms`.
