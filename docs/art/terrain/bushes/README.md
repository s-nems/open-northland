# Bushes

Two shrub silhouettes each provide empty, flowering and fruiting states. `atlas-alpha.png` and
`asset.json` supply all six exports. Exact map names bind the states to berry-bush rendering.

Run `npm run art -- build terrain/bushes`, then `npm run art -- review terrain/bushes`. Publish the approved candidate with `npm run art -- publish terrain/bushes`.
