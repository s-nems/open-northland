# Sand

`sand.png` is a 512×512 reduction of `master.png`. `sand-materials.json` binds 97 sand/beach
patterns and both sand transition families. Exact names disambiguate patterns sharing a texture
page.

Run `npm run art -- build terrain/sand`, then `npm run art -- review terrain/sand`. Publish the approved candidate with `npm run art -- publish terrain/sand`.
