# Idle source

`source/motion.fbx` is the generated motion; `idle.glb` is its applied source clip.
Requests and API receipts sit beside them. `../../body/model/idle.json` records retargeting
to the shared male body. The woman's model package derives its own relaxed idle from a neutral pose.
