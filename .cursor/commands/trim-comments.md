# /trim-comments — audit comments without hiding structural debt

Follow `.claude/commands/trim-comments.md`. Text typed after the command is its `$ARGUMENTS`
(an explicit package, path, or file scope).
