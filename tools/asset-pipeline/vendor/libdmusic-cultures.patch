Patch over libdmusic 790662e71120ff70359033083eb5a8a5751d5300 (MIT, https://github.com/frabert/libdmusic)
applied by scripts/build-dmrender.sh before building. What it changes and why:

- Common.h littleEndianRead: cast each byte to the target type before shifting; the int-promoted
  shift is undefined beyond bit 31, so every 64-bit field (GUID Data4, reference times) read garbage.
- Region/Instrument/Articulator: also accept the DLS Level 2 chunk ids (rgn2/lar2/art2). The
  Cultures banks are DLS2 files whose region and articulator payloads use the DLS1 layouts.
- Forms/Tracks.cpp: instantiate SequenceTrack for 'seqt' chunks; the class existed but the track
  factory never created it, so sequence-track notes were silently dropped.
- PlayingContext prepareSegment: schedule DX8 pattern tracks (DMPT) and sequence tracks (seqt) as
  absolute-time note messages. Every pattern track of a segment plays its embedded pattern
  simultaneously, looping until the segment header's mtLength (evidence: segments carry several
  DMPT tracks in one group with identical groove ranges, and the 30-measure patterns fit the
  30-measure segments exactly once). The upstream path pushed patterns into a groove-driven picker
  that nothing triggers in segments without a command track, so no notes ever played.
- PlayingContext loadBandTrack: clamp negative physical band times to 0. Cultures band items carry
  -1 ("apply before start"); as unsigned that landed at the end of the message queue and no
  instruments were ever loaded.
- MusicMessages BandChangeMessage: priority above notes, so instrument setup precedes notes that
  share its timestamp.
- MusicMessages music-value resolution: rewritten to the retail synthesizer's algorithm - a
  chord-tone walk over the chord pattern that spills into a scale walk, two-octave scale
  normalization with per-root fallback scales, and one octave down in chord-root play modes. A note
  flagged DMUS_PLAYMODE_NONE inherits its part's play mode; DMUS_PLAYMODE_FIXED plays the raw note.
  Every Cultures pattern note is flagged NONE over chord-root parts, so the old resolver played the
  whole corpus an octave high. Algorithm structure follows the GothicKit dmusic project's
  documentation of the retail binary (MIT); validated by per-note pitch analysis against an
  in-game recording.
- MusicMessages pattern curves: pitch-bend curves (DMUS_CURVET_PBCURVE, 4745 across the corpus,
  previously dropped - every bend and slide froze) schedule PitchBendMessage series next to the CC
  curves in both pattern paths, sharing one curve-shape helper.
- MusicMessages scheduleDx8PatternTrack/scheduleSequenceTrack: the schedulers the DMPT/seqt
  branches above use. Approximations: the default C chord (no segment carries a chord track),
  sequential variation choice per pattern repeat.
- DlsPlayer insertArticulator: unit-correct DLS->SF2 conversion for what the banks author. Time
  scales are timecents in 16.16 fixed (direct copy with clamps; the old seconds round-trip
  overflowed int16 on the 0x80000000 "0 seconds" sentinel), EG1 sustain is 0.1% of full level
  converted to centibels of attenuation (the old time conversion turned an 85.8% sustain into
  -85.8 dB, so held notes faded to silence), and EG hold, per-region pan, LFO/vibrato rate,
  delay and pitch depth are converted instead of dropped. Filter cutoff appears in the banks
  only as the "no filter" sentinel and stays ignored.
- DlsPlayer: no forced 100 ms attack on every region (unauthored attacks are instant), and
  amplitude-to-decibel conversion uses 20*log10.
- DlsPlayer region articulation: a region's lar2 list replaces the instrument-level list (DLS2
  semantics). The main melodic banks author per-region stereo pans and releases there; ignoring
  them collapsed L/R sample pairs into doubled mono and cut every release.
- DlsPlayer WSMP gain: the unit is 0.1 dB steps in 16.16 fixed point (dB = lGain / 655360;
  evidence: banks author +60 raw on near-full-scale drum samples and -330 on audible pads,
  plausible only as +6 and -33 dB). Zones keep relative differences as SF2 attenuation, and an
  instrument's largest positive gain applies as preset gain, since SF2 zones cannot boost.
- DlsPlayer volume curves: note velocity and CC7/CC11 land on the DLS curve (attenuation
  40*log10(127/value)) by passing the squared fraction through the synth's linear-amplitude
  gain; the band's already-squared bVolume then hits the same curve. The old code halved
  velocity (/255) and mixed compensating exponents, which buried high-velocity melody parts
  under full-volume accompaniment.
- dmrender: renderBlock counts interleaved samples, so size the buffer by channels and write
  length/channels wav frames; stereo renders wrote half music, half out-of-bounds reads. The
  buffer is zero-initialised so silent stretches are silence, not heap garbage.
- dmrender -e <file>: dump every player message as JSONL (band-instrument identity rows plus
  frame-stamped note/CC/pitch-bend events) instead of synthesizing, so the pipeline can replay the
  segment through an external synthesizer; the first player of each sub-block advances the shared
  frame clock.

diff --git i/include/dmusic/Common.h w/include/dmusic/Common.h
index 374cb2c..aac77b6 100644
--- i/include/dmusic/Common.h
+++ w/include/dmusic/Common.h
@@ -65,7 +65,7 @@ namespace DirectMusic {
         T result = 0;
 
         for (std::size_t i = 0; i < numBytes; i++) {
-            result |= ptr[i] << i * 8;
+            result |= static_cast<T>(ptr[i]) << (i * 8);
         }
 
         return result;
diff --git i/include/dmusic/DlsPlayer.h w/include/dmusic/DlsPlayer.h
index 48af509..0a32102 100644
--- i/include/dmusic/DlsPlayer.h
+++ w/include/dmusic/DlsPlayer.h
@@ -13,6 +13,7 @@ namespace DirectMusic {
     class DlsPlayer : public InstrumentPlayer {
     private:
         int m_preset;
+        float m_gainBoostDb;
         std::shared_ptr<TinySoundFont> m_soundfont;
 
         static std::unordered_map<an original routine, std::shared_ptr<TinySoundFont>> m_soundfonts;
diff --git i/include/dmusic/MusicMessage.h w/include/dmusic/MusicMessage.h
index 56b56b2..3bcb0cb 100644
--- i/include/dmusic/MusicMessage.h
+++ w/include/dmusic/MusicMessage.h
@@ -19,7 +19,8 @@ namespace DirectMusic {
         NoteOff,
         SegmentEnd,
         PatternEnd,
-        ControlChange
+        ControlChange,
+        PitchBend
     };
 
     class MusicMessage {
diff --git i/src/Articulator.cpp w/src/Articulator.cpp
index 7cc48e3..114f2d5 100644
--- i/src/Articulator.cpp
+++ w/src/Articulator.cpp
@@ -5,7 +5,7 @@ using namespace an original routine;
 using namespace an original routine;
 
 an original routine(const Chunk& c) {
-    if (c.getId() != "art1")
+    if (c.getId() != "art1" && c.getId() != "art2")
         throw an original routine("art1", c.getId());
 
     const std::uint8_t *data = c.getData().data();
diff --git i/src/DlsPlayer.cpp w/src/DlsPlayer.cpp
index 65d57a5..3ac95ce 100644
--- i/src/DlsPlayer.cpp
+++ w/src/DlsPlayer.cpp
@@ -13,6 +13,10 @@ using namespace sf2cute;
 
 std::unordered_map<DownloadableSound, std::shared_ptr<TinySoundFont>> an original routine;
 
+// WSMP gains above 0 dB cannot be expressed as SF2 zone attenuation; they apply as preset gain,
+// keyed by MIDI program within each collection.
+static std::unordered_map<DownloadableSound, std::unordered_map<std::uint32_t, float>> s_presetBoostDb;
+
 static double dwordTimecentsToSeconds(std::int32_t tc) {
     return exp2((double)tc / (1200.0 * 65536.0));
 }
@@ -21,38 +25,103 @@ static std::int16_t secondsToWordTimecents(double secs) {
     return (std::int16_t)(1200 * log2(secs));
 }
 
-// FIXME: Only "envelope" articulators are supported
+// DLS time scales are timecents in 16.16 fixed point; 0x80000000 encodes "0 seconds".
+static std::int16_t timecentsFromScale(std::int32_t lScale) {
+    if (lScale == (std::int32_t)0x80000000) return -12000;
+    double tc = lScale / 65536.0;
+    if (tc < -12000) tc = -12000;
+    if (tc > 8000) tc = 8000;
+    return (std::int16_t)tc;
+}
+
+// DLS EG1 sustain is 0.1% of full level (16.16); SF2 wants centibels of attenuation.
+static std::int16_t sustainLevelToCentibels(std::int32_t lScale) {
+    double permille = lScale / 65536.0;
+    if (permille <= 0) return 1440;
+    if (permille >= 1000) return 0;
+    double cb = -200.0 * log10(permille / 1000.0);
+    return (std::int16_t)(cb > 1440 ? 1440 : cb);
+}
+
+// Pitch-cent scales (LFO frequencies, vibrato depth) are 16.16 fixed too.
+static std::int16_t centsFromScale(std::int32_t lScale, std::int32_t lo, std::int32_t hi) {
+    double cents = lScale / 65536.0;
+    if (cents < lo) cents = lo;
+    if (cents > hi) cents = hi;
+    return (std::int16_t)cents;
+}
+
+// Converts the connection blocks the Cultures banks author: volume/mod envelopes (with hold),
+// per-region pan, LFO rates and delays, and vibrato/LFO pitch depth. Filter cutoff appears only
+// as the 0x80000000 "no filter" sentinel and is ignored, as are one-off velocity/key scalers.
 static void insertArticulator(const an original routine& articulator, std::vector<SFModulatorItem>& mods, std::vector<SFGeneratorItem>& gens) {
+    static const std::uint16_t DST_EG1_HOLD = 0x020c;
+    static const std::uint16_t DST_EG2_HOLD = 0x0310;
+    static const std::uint16_t DST_VIB_FREQUENCY = 0x0114;
+    static const std::uint16_t DST_VIB_STARTDELAY = 0x0115;
+    static const std::uint16_t SRC_VIBRATO = 0x0009;
+
     for (const auto& connBlock : articulator.getConnectionBlocks()) {
-        if (connBlock.usControl == (an original routine)0 &&
-            connBlock.usSource == an original routine &&
-            connBlock.usTransform == an original routine) {
-            SFGenerator gen;
-            switch (connBlock.usDestination) {
-            case an original routine:
-                gen = an original routine; break;
-            case an original routine:
-                gen = an original routine; break;
-            case an original routine:
-                gen = an original routine; break;
-            case an original routine:
-                gen = an original routine; break;
+        if (connBlock.usControl != (an original routine)0 ||
+            connBlock.usTransform != an original routine) continue;
+        std::uint16_t source = (std::uint16_t)connBlock.usSource;
+        std::uint16_t destination = (std::uint16_t)connBlock.usDestination;
+        std::int32_t scale = connBlock.lScale;
 
-            case an original routine:
-                gen = an original routine; break;
-            case an original routine:
-                gen = an original routine; break;
-            case an original routine:
-                gen = an original routine; break;
-            case an original routine:
-                gen = an original routine; break;
+        if (source == (std::uint16_t)an original routine &&
+            destination == (std::uint16_t)an original routine) {
+            gens.push_back(SFGeneratorItem(an original routine,
+                GenAmountType(centsFromScale(scale, -12000, 12000))));
+            continue;
+        }
+        if (source == SRC_VIBRATO &&
+            destination == (std::uint16_t)an original routine) {
+            gens.push_back(SFGeneratorItem(an original routine,
+                GenAmountType(centsFromScale(scale, -12000, 12000))));
+            continue;
+        }
+        if (source != (std::uint16_t)an original routine) continue;
 
-            default:
-                continue;
-            }
-            double secs = dwordTimecentsToSeconds(connBlock.lScale);
-            std::int16_t newValue = secondsToWordTimecents(secs);
-            gens.push_back(SFGeneratorItem(gen, GenAmountType(newValue)));
+        switch (destination) {
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case DST_EG1_HOLD:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(sustainLevelToCentibels(scale)))); break;
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case DST_EG2_HOLD:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case (std::uint16_t)an original routine: {
+            // Mod-env sustain: 0.1% of full level -> SF2 0.1% decrease from full.
+            double permille = scale / 65536.0;
+            if (permille < 0) permille = 0;
+            if (permille > 1000) permille = 1000;
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType((std::int16_t)(1000 - permille))));
+            break;
+        }
+        case (std::uint16_t)an original routine:
+            // Both sides are 0.1% units, full left -500 to full right 500.
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(centsFromScale(scale, -500, 500)))); break;
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(centsFromScale(scale, -16000, 4500)))); break;
+        case (std::uint16_t)an original routine:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        case DST_VIB_FREQUENCY:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(centsFromScale(scale, -16000, 4500)))); break;
+        case DST_VIB_STARTDELAY:
+            gens.push_back(SFGeneratorItem(an original routine, GenAmountType(timecentsFromScale(scale)))); break;
+        default:
+            break;
         }
     }
 }
@@ -111,6 +180,16 @@ static std::shared_ptr<TinySoundFont> convertCollection(an original routine
 
     for (const auto& instr : dls.getInstruments()) {
         std::vector<SFInstrumentZone> zones;
+
+        // WSMP gain is 0.1 dB steps in 16.16 fixed point (dB = lGain / 655360). SF2 zones only
+        // attenuate, so the largest positive gain becomes preset gain and each zone keeps its
+        // relative difference as attenuation.
+        double instrumentBoostDb = 0;
+        for (const auto& reg : instr.getRegions()) {
+            double gainDb = reg.getWavesample().lAttenuation / 655360.0;
+            if (gainDb > instrumentBoostDb) instrumentBoostDb = gainDb;
+        }
+
         for (const auto& reg : instr.getRegions()) {
             auto hdr = reg.getRegionHeader();
             auto wavelink = reg.getWaveLink();
@@ -119,9 +198,12 @@ static std::shared_ptr<TinySoundFont> convertCollection(an original routine
             std::vector<SFModulatorItem> modItems;
             std::shared_ptr<SFSample> sample;
             std::uint16_t keyrangeLow, keyrangeHigh, velrangeLow, velrangeHigh;
-            genItems.push_back(SFGeneratorItem(an original routine, GenAmountType(secondsToWordTimecents(0.1))));
 
-            for (const auto& art : instr.getArticulators()) {
+            // DLS2: a region's articulation list replaces the instrument-level list.
+            const auto regionArticulators = reg.getArticulators();
+            const auto& articulators =
+                regionArticulators.empty() ? instr.getArticulators() : regionArticulators;
+            for (const auto& art : articulators) {
                 insertArticulator(art, modItems, genItems);
             }
 
@@ -160,6 +242,12 @@ static std::shared_ptr<TinySoundFont> convertCollection(an original routine
             }
             sample->set_original_key(static_cast<std::uint8_t>(wavesample.usUnityNote));
             sample->set_correction(static_cast<std::int8_t>(wavesample.sFineTune));
+            double regionGainDb = wavesample.lAttenuation / 655360.0;
+            double attenuationCb = (instrumentBoostDb - regionGainDb) * 10.0;
+            if (attenuationCb > 1440) attenuationCb = 1440;
+            if (attenuationCb > 0) {
+                genItems.push_back(SFGeneratorItem(an original routine, GenAmountType((std::int16_t)attenuationCb)));
+            }
             SFInstrumentZone zone(sample, genItems, modItems);
             zones.push_back(zone);
         }
@@ -169,6 +257,9 @@ static std::shared_ptr<TinySoundFont> convertCollection(an original routine
             std::vector<SFPresetZone>{
             SFPresetZone(instrument)
         }));
+        if (instrumentBoostDb > 0) {
+            s_presetBoostDb[dls][instr.getMidiProgram()] = (float)instrumentBoostDb;
+        }
     }
 
     std::stringstream stream;
@@ -177,7 +268,7 @@ static std::shared_ptr<TinySoundFont> convertCollection(an original routine
 }
 
 static float gainToDecibels(float gain) {
-    return 10 * log10(gain);
+    return 20 * log10(gain);
 }
 
 an original routine(std::uint8_t bankLo, std::uint8_t bankHi, std::uint8_t patch,
@@ -213,11 +304,18 @@ an original routine(std::uint8_t bankLo, std::uint8_t bankHi, std::uint8_t patc
         throw std::runtime_error("Preset not found");
     }
 
+    m_gainBoostDb = 0;
+    auto boosts = s_presetBoostDb.find(dls);
+    if (boosts != s_presetBoostDb.end()) {
+        auto boost = boosts->second.find(patch);
+        if (boost != boosts->second.end()) m_gainBoostDb = boost->second;
+    }
+
     float volFactorRight = sqrt((m_pan + 1) / 2);
     float volFactorLeft = sqrt((-m_pan + 1) / 2);
 
     m_soundfont->setPresetPanning(m_preset, volFactorLeft, volFactorRight);
-    m_soundfont->setPresetGain(m_preset, gainToDecibels(m_volume));
+    m_soundfont->setPresetGain(m_preset, gainToDecibels(m_volume) + m_gainBoostDb);
 }
 
 std::uint32_t an original routine(std::int16_t *buffer, std::uint32_t count, bool mix) noexcept {
@@ -227,7 +325,10 @@ std::uint32_t an original routine(std::int16_t *buffer, std::uint32_t count,
 
 /// Instructs the synthesizer to start playing a note
 void an original routine(std::uint8_t note, std::uint8_t velocity) {
-    m_soundfont->noteOn(m_preset, note, velocity / 255.0f);
+    // DLS velocity curve: attenuation 40*log10(127/vel). The synth applies the passed value as
+    // linear amplitude (20*log10), so the squared fraction lands on the spec curve.
+    float fraction = velocity > 127 ? 1.0f : velocity / 127.0f;
+    m_soundfont->noteOn(m_preset, note, fraction * fraction);
 }
 
 /// Instructs the synthesizer to stop playing a note
@@ -248,8 +349,9 @@ void an original routine(std::uint8_t note, std::uint8_t val) {}
 /// Sends a "control change" message
 void an original routine(an original routine control, float val) {
     if (control == an original routine || control == an original routine) {
+        // Same DLS curve as note velocity: squared value through the linear-amplitude gain.
         m_volume = val;
-        m_soundfont->setPresetGain(m_preset, gainToDecibels(m_volume * m_volume * m_volume * m_volume));
+        m_soundfont->setPresetGain(m_preset, gainToDecibels(m_volume * m_volume) + m_gainBoostDb);
     }
 }
 
diff --git i/src/Forms/Tracks.cpp w/src/Forms/Tracks.cpp
index cbcfd11..f58a056 100644
--- i/src/Forms/Tracks.cpp
+++ w/src/Forms/Tracks.cpp
@@ -57,6 +57,8 @@ an original routine(const Chunk& c)
             m_data = std::make_shared<CommandTrack>(subchunk);
         } else if (id == "mute") {
             m_data = std::make_shared<MuteTrack>(subchunk);
+        } else if (id == "seqt") {
+            m_data = std::make_shared<SequenceTrack>(subchunk);
         } else if (id == "sgnp") {
             m_data = std::make_shared<SignpostTrack>(subchunk);
         } else if (id == "syex") {
diff --git i/src/Instrument.cpp w/src/Instrument.cpp
index 465333d..d644ac3 100644
--- i/src/Instrument.cpp
+++ w/src/Instrument.cpp
@@ -23,7 +23,7 @@ an original routine(const Chunk& c) {
                 for (Chunk rgn : subchunk.getSubchunks()) {
                     m_regions.push_back(Region(rgn));
                 }
-            } else if (subchunk.getListId() == "lart") {
+            } else if (subchunk.getListId() == "lart" || subchunk.getListId() == "lar2") {
                 for (Chunk art : subchunk.getSubchunks()) {
                     m_articulators.push_back(Articulator(art));
                 }
diff --git i/src/MusicMessages.cpp w/src/MusicMessages.cpp
index 9692438..aa70a35 100644
--- i/src/MusicMessages.cpp
+++ w/src/MusicMessages.cpp
@@ -106,124 +106,167 @@ static std::int32_t getMusicOffset(std::uint32_t mtGridStart, std::int16_t nTime
             );
 }
 
-// Returns true if the specified degree is present in a certain scale, and puts the distance from the
-// root note into offset. If not present, returns false.
-static bool getOffsetFromScale(std::uint8_t degree, std::uint32_t scale, std::uint8_t* offset) {
-    assert(offset != nullptr);
+// Music-value resolution follows the retail dmime synthesizer as documented from binary
+// analysis by the GothicKit dmusic project (MIT). Chord-root play modes place the resolved
+// note one octave below the raw octave nibble; a note flagged DMUS_PLAYMODE_NONE inherits
+// the part's play mode. Validated against an in-game recording of the owned copy.
 
-    // FIXME: There is probably a faster way to do this
-    std::vector<int> degrees;
-    for (int i = 0; i < 24; i++) {
-        if (scale & (0x00000001 << i)) {
-            degrees.push_back(i);
+static int countBits(std::uint32_t v) {
+    int count = 0;
+    for (; v != 0; v >>= 1) count += v & 1;
+    return count;
+}
+
+// One replacement scale per chromatic root, substituted when a pattern is degenerate.
+static const std::uint32_t FALLBACK_SCALES[12] = {
+    0xab5ab5, 0x6ad6ad, 0x5ab5ab, 0xad5ad5, 0x6b56b5, 0x5ad5ad,
+    0x56b56b, 0xd5ad5a, 0xb56b56, 0xd6ad6a, 0xb5ab5a, 0xad6ad6,
+};
+
+static std::uint32_t normalizeScale(std::uint32_t scale, std::uint8_t scaleRoot) {
+    // Force the scale to exactly two octaves, rotate it to the root, and re-normalize. The & 12
+    // root mask mirrors the retail binary as documented; the corpus only authors scale root 0.
+    scale = (scale & 0x0FFF) | (scale << 12);
+    scale = scale >> (12 - (scaleRoot & 12));
+    scale = (scale & 0x0FFF) | (scale << 12);
+    if (countBits(scale & 0xFFF) <= 4) {
+        std::uint32_t best = FALLBACK_SCALES[0];
+        int bestScore = 0;
+        for (int i = 0; i < 12; i++) {
+            int score = countBits(FALLBACK_SCALES[i] & scale & 0xFFF);
+            if (score > bestScore) {
+                best = FALLBACK_SCALES[i];
+                bestScore = score;
+            }
         }
+        scale = best;
     }
-
-    if (degree < degrees.size()) {
-        *offset = degrees[degree];
-        return true;
-    } else {
-        *offset = degrees[degrees.size() - 1];
-        return false;
-    }
+    // Copy the second octave into the empty third so long walks stay in scale.
+    if ((scale & 0xFF000000) == 0) scale |= (scale & 0xFFF000) << 12;
+    return scale;
 }
 
 static bool MusicValueToMIDI(std::uint32_t chord, const std::vector<DMUS_IO_SUBCHORD>& subchords, DMUS_IO_STYLENOTE note, DMUS_IO_STYLEPART part, std::uint8_t* value) {
     assert(value != nullptr);
 
-    if (note.bPlayModeFlags == DMUS_PLAYMODE_FIXED) {
-        // In the original Gothic sountrack this is not used, but it might be useful for modding purposes
-        *value = (std::uint8_t)(note.wMusicValue);
+    std::uint8_t mode = note.bPlayModeFlags == DMUS_PLAYMODE_NONE ? part.bPlayModeFlags : note.bPlayModeFlags;
+    if (mode == DMUS_PLAYMODE_FIXED) {
+        *value = (std::uint8_t)(note.wMusicValue & 0x7F);
         return true;
     }
 
-    // TODO: The subchord level should be obtained from the part reference,
-    // but in Gothic's soundtrack it's always set to the first level.
-
-    /*
-    A chord (or a scale) has the following structure: the first 8 bits represent the
-    root of the chord (scale), with the standard MIDI notation:
-    0 -> C
-    1 -> C#
-    ....
-    23 -> B
-
-    The following 24 bits represent which notes are present in the chord, with each
-    bit meaning a semitone from the root (the LSB). Example taken from G2:
-
-    chord = 0x00AB5AB5
-    0   0   A   B   5   A   B   5
-    00000000101010110101101010110101
-    |-------|-|-|-||-|-||-|-|-||-|-|
-    C    W W W HW W HW W W HW W -----> C Major scale
-    */
     DMUS_IO_SUBCHORD subchord;
-    subchord.bChordRoot = 12;
+    subchord.bChordRoot = (chord & 0xFF000000) >> 24;
     subchord.bScaleRoot = 0;
     subchord.dwChordPattern = 0x00000091;
     subchord.dwInversionPoints = 0xFFFFFFFF;
     subchord.dwLevels = 1;
     subchord.dwScalePattern = 0x00AB5AB5;
-
     if (subchords.size() > 0) {
         subchord = subchords[0];
     }
 
-    /* Had to dig hard for this one: https://msdn.microsoft.com/en-us/library/ms898477.aspx
-    Here is how to interpret wMusicValue:
-    First nibble: octave (from -2 to 14 (??? I guess they meant -2 to 13) )
-    Second nibble: chord tone (0 to 15)
-    Third nibble: scale offset (0 to 15)
-    Fourth nibble: accidentals (-8 to 7)
-    */
+    std::uint16_t musicValue = note.wMusicValue;
+    int wrapOffset = 0;
 
-    int octave = ((note.wMusicValue & 0xF000) >> 12);
-    int chordTone = ((note.wMusicValue & 0x0F00) >> 8);
-    int scaleTone = ((note.wMusicValue & 0x00F0) >> 4);
+    // A negative octave nibble transposes up while remembering the octaves to give back.
+    while (musicValue >= 0xE000) {
+        musicValue += 0x1000;
+        wrapOffset -= 12;
+    }
+    // Keep the scale-offset nibble addable without spilling into the chord nibble.
+    std::uint16_t scaleNibblePlus = (musicValue & 0x00F0) + 0x0070;
+    if ((scaleNibblePlus & 0x0F00) != 0) {
+        musicValue = (musicValue & 0xFF0F) | (scaleNibblePlus & 0x00F0);
+        wrapOffset -= 12;
+    }
 
-    // Explanation: the accidentals are represented as a two's complement 4bit value.
-    // We first take only the last four bits from the word, then we shift it left
-    // while keeping it unsigned, so that when we convert it into a signed byte
-    // it has the correct sign. Then we divide by 16 to simulate an arithmetic
-    // right shift of 4, to bring the value back into the correct range
-    int accidentals = (std::int8_t)(note.wMusicValue & 0x000F);
-    if (accidentals > 7) { accidentals = (accidentals - 16); }
-
-    int noteValue = ((chord & 0xFF000000) >> 24) + 12 * octave;
-    std::uint8_t chordOffset = 0;
-    std::uint8_t scaleOffset = 0;
-    if (getOffsetFromScale(chordTone, subchord.dwChordPattern, &chordOffset)) {
-        noteValue += chordOffset;
-        // Is it possible to not find chordTone?
-        // } else if (getOffsetFromScale(chordTone, subchord.dwScalePattern, &scaleOffset)) {
-        //     noteValue += scaleOffset;
-    } else {
-        TRACE_VERBOSE("Note not found: " << noteValue);
+    std::uint16_t root = 0;
+    if (mode & DMUS_PLAYMODE_CHORD_ROOT) {
+        root = subchord.bChordRoot;
+    } else if (mode & DMUS_PLAYMODE_KEY_ROOT) {
+        // Key-root modes are not authored in the supported corpus.
         return false;
     }
 
-    // Start scale offset from resolved chordTone.
-    // Let's draw an example:
-    //      * chordTone is 2, scaleTone is 1
-    //      * in normal chord chordTone 2 is resolved to 5th grade in scale
-    //      * plus scaleTone 1 it makes 6th grade of the scale
-    //      * depending on scale, it might be 8 semitones (minor) or 9 semitones (major)
-    // So, scaleOffset depends on chord's grade
-    // That works even if chord's grade doesn't fit into scale
-    // Chord grade might be off of the scale, so if scaleOffset = 0, don't ruin it
-    if (scaleTone && getOffsetFromScale(scaleTone, subchord.dwScalePattern >> chordOffset, &scaleOffset)) {
-        noteValue += scaleOffset;
+    if ((mode & (DMUS_PLAYMODE_CHORD_INTERVALS | DMUS_PLAYMODE_SCALE_INTERVALS)) == 0) {
+        return false;
     }
 
-    noteValue += accidentals;
-    while (noteValue < 0) {
-        noteValue += 12;
-    }
-    while (noteValue > 127) {
-        noteValue -= 12;
+    std::uint32_t scalePattern = subchord.dwScalePattern != 0 ? subchord.dwScalePattern : 0x00AB5AB5;
+    scalePattern = normalizeScale(scalePattern, subchord.bScaleRoot);
+    std::uint32_t chordPattern = subchord.dwChordPattern != 0 ? subchord.dwChordPattern : 1;
+
+    std::uint16_t chordPosition = (musicValue & 0x0F00) >> 8;
+    std::uint16_t scalePosition = (musicValue & 0x0070) >> 4;
+    int accidentals = musicValue & 0x000F;
+    if (accidentals > 8) accidentals -= 16;
+
+    int noteValue = 0;
+    int noteOffset = 0;
+    std::uint32_t walkPattern = 0;
+    std::uint16_t walkPosition = 0;
+
+    std::uint16_t rootOctave = root % 12;
+    int chordBits = countBits(chordPattern);
+
+    if ((mode & DMUS_PLAYMODE_CHORD_INTERVALS) && scalePosition == 0 && chordPosition < chordBits) {
+        noteOffset = root + accidentals;
+        walkPattern = chordPattern;
+        walkPosition = chordPosition;
+    } else if ((mode & DMUS_PLAYMODE_CHORD_INTERVALS) && chordPosition < chordBits) {
+        walkPattern = chordPattern;
+        walkPosition = chordPosition;
+        // Walk to the chord tone first, then continue the scale from there.
+        if (walkPattern != 0) {
+            while ((walkPattern & 1) == 0) {
+                walkPattern >>= 1;
+                noteValue += 1;
+            }
+        }
+        if (walkPosition > 0) {
+            do {
+                walkPattern >>= 1;
+                noteValue += 1;
+                if (walkPattern & 1) walkPosition -= 1;
+                if (walkPattern == 0) {
+                    noteValue += walkPosition;
+                    break;
+                }
+            } while (walkPosition > 0);
+        }
+        noteValue += rootOctave;
+        noteOffset = accidentals + root - rootOctave;
+        walkPattern = scalePattern >> (noteValue % 12);
+        walkPosition = scalePosition;
+    } else if (mode & DMUS_PLAYMODE_SCALE_INTERVALS) {
+        noteValue = rootOctave;
+        noteOffset = accidentals + root - rootOctave;
+        walkPattern = scalePattern >> rootOctave;
+        walkPosition = chordPosition * 2 + scalePosition;
+    } else {
+        return false;
     }
 
-    *value = noteValue;
+    walkPosition += 1;
+    for (; walkPosition > 0; walkPattern >>= 1) {
+        noteValue += 1;
+        if (walkPattern & 1) walkPosition -= 1;
+        if (walkPattern == 0) {
+            noteValue += walkPosition;
+            break;
+        }
+    }
+    noteValue -= 1;
+
+    noteValue += noteOffset + wrapOffset;
+    int octave = (musicValue & 0xF000) >> 12;
+    noteValue += octave * 12;
+    if (mode & DMUS_PLAYMODE_CHORD_ROOT) noteValue -= 12;
+
+    while (noteValue < 0) noteValue += 12;
+    while (noteValue > 127) noteValue -= 12;
+    *value = (std::uint8_t)noteValue;
     return true;
 }
 
@@ -262,6 +305,17 @@ static T lerp(float x, T start, T end) {
     return (1 - x) * start + x * end;
 }
 
+static float curveValue(std::uint8_t shape, float phase, float startValue, float endValue) {
+    switch (shape) {
+    case DMUS_CURVES_INSTANT: return endValue;
+    case DMUS_CURVES_EXP: return lerp(phase * phase * phase * phase, startValue, endValue);
+    case DMUS_CURVES_LOG: return lerp(sqrtf(phase), startValue, endValue);
+    case DMUS_CURVES_SINE: return lerp((sinf((phase - 0.5f) * PI) + 1) * 0.5f, startValue, endValue);
+    case DMUS_CURVES_LINEAR:
+    default: return lerp(phase, startValue, endValue);
+    }
+}
+
 void an original routine(PlayingContext& ctx) {
     TRACE("Playing pattern");
     ctx.m_patternMessageQueue = MessageQueue();
@@ -316,7 +370,18 @@ void an original routine(PlayingContext& ctx) {
 
                     std::uint32_t timeStart = getMusicOffset(curve.mtGridStart, curve.nTimeOffset, header.timeSig);
                     std::uint32_t duration = curve.mtDuration;
-                    if (curve.bEventType == DMUS_CURVET_CCCURVE) {
+                    if (curve.bEventType == DMUS_CURVET_PBCURVE) {
+                        // Pitch bend curve: values are the raw 0-16383 wheel range, 8192 center.
+                        float startValue = (float)curve.nStartValue, endValue = (float)curve.nEndValue;
+                        for (uint32_t i = 0; i < duration / DMUSIC_CURVE_MESSAGE_SPACING; i++) {
+                            std::uint32_t offset = i * DMUSIC_CURVE_MESSAGE_SPACING;
+                            float phase = (float)offset / duration;
+                            float value = curveValue(curve.bCurveShape, phase, startValue, endValue);
+                            auto msg = std::make_shared<PitchBendMessage>(ctx.m_musicTime + timeStart + offset,
+                                partRef.wLogicalPartID, PChannel, (std::uint16_t)value);
+                            ctx.m_patternMessageQueue.push(msg);
+                        }
+                    } else if (curve.bEventType == DMUS_CURVET_CCCURVE) {
                         if (curve.nStartValue > 127 || curve.nEndValue > 127) continue;
 
                         float startValue = (float)curve.nStartValue / 127, endValue = (float)curve.nEndValue / 127;
@@ -497,4 +562,138 @@ void an original routine(PlayingContext& ctx) {
         player->controlChange(m_control, m_value);
     }
     assert(player != nullptr);
-}
\ No newline at end of file
+}
+
+void an original routine(PlayingContext& ctx) {
+    TRACE("Pitch bend");
+    const auto& channels = getChannels(ctx);
+    std::shared_ptr<InstrumentPlayer> player = nullptr;
+    if (player = findChannel(channels, m_channel, m_channelAlt)) {
+        player->pitchBend((std::int16_t)m_value);
+    }
+    assert(player != nullptr);
+}
+
+// --- DX8 pattern/sequence track scheduling ---------------------------------------------------
+
+// Default performance chord (root C in the top byte) used when a segment carries no chord track;
+// matches the default subchord MusicValueToMIDI falls back to.
+static const std::uint32_t DEFAULT_CHORD = 12u << 24;
+
+// Percussion channel folding, as in playPattern.
+static std::uint32_t foldPercussionChannel(std::uint32_t pChannel) {
+    if (pChannel >= 9 && (pChannel - 9) % 16 == 0) return 9;
+    return pChannel;
+}
+
+// Sequential variation choice: repeat r plays the r-th authored variation, wrapping around.
+static std::uint8_t pickSequentialVariation(const DMUS_IO_STYLEPART& partHeader, std::uint32_t repeat) {
+    std::uint8_t available[32];
+    std::uint8_t count = 0;
+    for (std::uint8_t i = 0; i < 32; i++) {
+        if (partHeader.dwVariationChoices[i] & 0x0FFFFFFF) {
+            available[count++] = i;
+        }
+    }
+    if (count == 0) return 0;
+    return available[repeat % count];
+}
+
+void an original routine(const PatternTrack& track, std::uint32_t segmentLength,
+    std::vector<std::shared_ptr<MusicMessage>>& messages) {
+    const auto& form = track.getPattern();
+    if (form == nullptr) return;
+
+    std::map<GUID, StylePart> parts;
+    for (const auto& part : form->getParts()) {
+        parts[part.getHeader().guidPartID] = part;
+    }
+
+    const auto& patternHeader = form->getHeader();
+    std::uint32_t patternLength = patternHeader.wNbrMeasures * getMeasureLength(patternHeader.timeSig);
+    if (patternLength == 0) return;
+
+    const std::vector<DMUS_IO_SUBCHORD> noSubchords;
+
+    for (const auto& partRefTuple : form->getPartReferences()) {
+        const auto& partRef = partRefTuple.first;
+        auto found = parts.find(partRef.guidPartID);
+        if (found == parts.end()) {
+            throw std::runtime_error("Couldn't find part: " + partRef.guidPartID.toString());
+        }
+        const StylePart& part = found->second;
+        const auto& partHeader = part.getHeader();
+        std::uint32_t pChannel = foldPercussionChannel(partRef.wLogicalPartID);
+
+        std::uint32_t repeat = 0;
+        for (std::uint32_t start = 0; start < segmentLength; start += patternLength, repeat++) {
+            std::uint32_t variation = 1u << pickSequentialVariation(partHeader, repeat);
+
+            for (const auto& note : part.getNotes()) {
+                if (!(note.dwVariation & variation)) continue;
+
+                std::int64_t time = (std::int64_t)start
+                    + getMusicOffset(note.mtGridStart, note.nTimeOffset, partHeader.timeSig);
+                if (time < 0 || time >= (std::int64_t)segmentLength) continue;
+
+                std::uint8_t midiNote;
+                if (MusicValueToMIDI(DEFAULT_CHORD, noSubchords, note, partHeader, &midiNote)) {
+                    messages.push_back(std::make_shared<NoteOnMessage>((std::uint32_t)time,
+                        midiNote, note.bVelocity, 0, partRef.wLogicalPartID, pChannel));
+                    messages.push_back(std::make_shared<NoteOffMessage>((std::uint32_t)(time + note.mtDuration),
+                        midiNote, partRef.wLogicalPartID, pChannel));
+                }
+            }
+
+            for (const auto& curve : part.getCurves()) {
+                if (!(curve.dwVariation & variation)) continue;
+                bool isPitchBend = curve.bEventType == DMUS_CURVET_PBCURVE;
+                if (!isPitchBend && curve.bEventType != DMUS_CURVET_CCCURVE) continue;
+                if (!isPitchBend && (curve.nStartValue > 127 || curve.nEndValue > 127)) continue;
+
+                std::int64_t timeStart = (std::int64_t)start
+                    + getMusicOffset(curve.mtGridStart, curve.nTimeOffset, partHeader.timeSig);
+                std::uint32_t duration = curve.mtDuration;
+                // Pitch bend curves carry the raw 0-16383 wheel range (8192 center); CC curves 0-127.
+                float startValue = isPitchBend ? (float)curve.nStartValue : (float)curve.nStartValue / 127;
+                float endValue = isPitchBend ? (float)curve.nEndValue : (float)curve.nEndValue / 127;
+                auto control = (an original routine)curve.bCCData;
+
+                for (std::uint32_t i = 0; i < duration / DMUSIC_CURVE_MESSAGE_SPACING; i++) {
+                    std::uint32_t offset = i * DMUSIC_CURVE_MESSAGE_SPACING;
+                    float phase = (float)offset / duration;
+                    std::int64_t time = timeStart + offset;
+                    if (time < 0 || time >= (std::int64_t)segmentLength) continue;
+
+                    float value = curveValue(curve.bCurveShape, phase, startValue, endValue);
+
+                    if (isPitchBend) {
+                        messages.push_back(std::make_shared<PitchBendMessage>((std::uint32_t)time,
+                            partRef.wLogicalPartID, pChannel, (std::uint16_t)value));
+                    } else {
+                        messages.push_back(std::make_shared<ControlChangeMessage>((std::uint32_t)time,
+                            partRef.wLogicalPartID, pChannel, control, value));
+                    }
+                }
+            }
+        }
+    }
+}
+
+void an original routine(const SequenceTrack& track,
+    std::vector<std::shared_ptr<MusicMessage>>& messages) {
+    static const std::uint8_t MIDI_NOTE_ON = 0x90;
+
+    for (const auto& item : track.getSequenceItems()) {
+        if ((item.bStatus & 0xF0) != MIDI_NOTE_ON) continue;
+
+        std::int64_t time = (std::int64_t)item.mtTime + item.nOffset;
+        if (time < 0) time = 0;
+        std::uint32_t pChannel = foldPercussionChannel(item.dwPChannel);
+
+        messages.push_back(std::make_shared<NoteOnMessage>((std::uint32_t)time,
+            item.bByte1, item.bByte2, 0, item.dwPChannel, pChannel));
+        messages.push_back(std::make_shared<NoteOffMessage>((std::uint32_t)(time + item.mtDuration),
+            item.bByte1, item.dwPChannel, pChannel));
+    }
+}
diff --git i/src/MusicMessages.h w/src/MusicMessages.h
index f29ecfc..49a220f 100644
--- i/src/MusicMessages.h
+++ w/src/MusicMessages.h
@@ -40,6 +40,9 @@ namespace DirectMusic {
 
         virtual void Execute(PlayingContext& ctx);
 
+        // Instrument setup must precede notes carrying the same timestamp.
+        virtual int getPriority() { return 2; }
+
     private:
         std::map<std::uint32_t, std::shared_ptr<InstrumentPlayer>> instruments;
     };
@@ -182,4 +185,34 @@ namespace DirectMusic {
         std::uint32_t m_channel, m_channelAlt;
         an original routine m_control;
     };
-}
\ No newline at end of file
+
+    class PitchBendMessage : public MusicMessage {
+    public:
+        PitchBendMessage(std::uint32_t time, std::uint32_t channel, std::uint32_t channelAlt, std::uint16_t value)
+            : MusicMessage(time),
+            m_channel(channel),
+            m_channelAlt(channelAlt),
+            m_value(value) {}
+
+        virtual std::shared_ptr<MusicMessage> Clone(std::uint32_t newTime) {
+            return std::make_shared<PitchBendMessage>(newTime, m_channel, m_channelAlt, m_value);
+        }
+
+        virtual MusicMessageType getMessageType() const { return an original routine; }
+
+        virtual void Execute(PlayingContext& ctx);
+
+    private:
+        std::uint32_t m_channel, m_channelAlt;
+        std::uint16_t m_value;
+    };
+
+    // Schedules a DX8 pattern track's embedded pattern as absolute-time messages, looping the
+    // pattern until segmentLength. All pattern tracks of a segment play simultaneously.
+    void scheduleDx8PatternTrack(const PatternTrack& track, std::uint32_t segmentLength,
+        std::vector<std::shared_ptr<MusicMessage>>& messages);
+
+    // Schedules a sequence track's raw MIDI-like note events as absolute-time messages.
+    void scheduleSequenceTrack(const SequenceTrack& track,
+        std::vector<std::shared_ptr<MusicMessage>>& messages);
+}
diff --git i/src/PlayingContext.cpp w/src/PlayingContext.cpp
index 78686e6..e801091 100644
--- i/src/PlayingContext.cpp
+++ w/src/PlayingContext.cpp
@@ -195,7 +195,9 @@ static void loadBandTrack(const TrackForm& track, std::vector<std::shared_ptr<Mu
         DMUS_IO_BAND_ITEM_HEADER2 header = band.first;
         BandForm bandForm = band.second;
 
-        auto message = std::make_shared<BandChangeMessage>(ctx, header.lBandTimePhysical, bandForm);
+        std::int32_t bandTime = (std::int32_t)header.lBandTimePhysical;
+        if (bandTime < 0) bandTime = 0;
+        auto message = std::make_shared<BandChangeMessage>(ctx, bandTime, bandForm);
         messageVector.push_back(message);
     }
 }
@@ -278,6 +280,27 @@ std::shared_ptr<SegmentInfo> an original routine(const SegmentForm& s
                 newSegment->initialTempo = styleForm->getHeader().dblTempo;
                 newSegment->initialSignature = styleForm->getHeader().timeSig;
             }
+        } else if (*header.ckid == 0 && fccType == "DMPT") {
+            // DX8 pattern track: every pattern track of a segment plays its embedded pattern
+            // simultaneously, looping until the segment's length. Scheduled as absolute-time
+            // messages because nothing triggers the groove-driven pattern picker in segments
+            // without a command track.
+            auto patternTrack = std::static_pointer_cast<PatternTrack>(track.getData());
+            if (patternTrack != nullptr && patternTrack->getPattern() != nullptr) {
+                scheduleDx8PatternTrack(*patternTrack, segment.getHeader().mtLength, newSegment->messages);
+                if (patternTrack->getPattern()->getBand() != nullptr) {
+                    auto message = std::make_shared<BandChangeMessage>(*this, 0, *patternTrack->getPattern()->getBand());
+                    newSegment->messages.push_back(message);
+                }
+                newSegment->initialTempo = patternTrack->getStyleHeader().dblTempo;
+                newSegment->initialSignature = patternTrack->getStyleHeader().timeSig;
+            }
+        } else if (ckid == "seqt") {
+            // Sequence track: raw MIDI-like note events with absolute times.
+            auto seqTrack = std::static_pointer_cast<SequenceTrack>(track.getData());
+            if (seqTrack != nullptr) {
+                scheduleSequenceTrack(*seqTrack, newSegment->messages);
+            }
         } else if (*header.ckid == 0 && fccType == "DMBT") {
             loadBandTrack(track, newSegment->messages, *this);
         } else if (*header.ckid == 0 && fccType == "cord") {
diff --git i/src/Region.cpp w/src/Region.cpp
index 0026f08..9d19262 100644
--- i/src/Region.cpp
+++ w/src/Region.cpp
@@ -5,14 +5,14 @@ using namespace an original routine;
 using namespace an original routine;
 
 an original routine(const Chunk& c) {
-    if (c.getId() != "LIST" || c.getListId() != "rgn ")
+    if (c.getId() != "LIST" || (c.getListId() != "rgn " && c.getListId() != "rgn2"))
         throw an original routine("LIST rgn", c.getId() + " " + c.getListId());
 
     for(const Chunk& subchunk: c.getSubchunks()) {
         const std::string& id = subchunk.getId();
         if(id == "rgnh") {
             m_rgnHeader = RegionHeader(subchunk.getData().data());
-        } else if(id == "LIST" && subchunk.getListId() == "lart") {
+        } else if(id == "LIST" && (subchunk.getListId() == "lart" || subchunk.getListId() == "lar2")) {
             for (Chunk art1ck : subchunk.getSubchunks()) {
                 m_articulators.push_back(Articulator(art1ck));
             }
diff --git i/utils/dmrender/src/dmrender.cpp w/utils/dmrender/src/dmrender.cpp
index 2b5b6f2..9252f3b 100644
--- i/utils/dmrender/src/dmrender.cpp
+++ w/utils/dmrender/src/dmrender.cpp
@@ -9,6 +9,7 @@
 #include <dmusic/Tracks.h>
 #include <dmusic/dls/DownloadableSound.h>
 #include <cmath>
+#include <cstdio>
 #include <args.hxx>
 
 #include "../../../src/dr_wav.h"
@@ -16,12 +17,79 @@
 using namespace DirectMusic;
 using namespace an original routine;
 
+// Event-dump mode: instead of synthesizing audio, every player logs the messages it receives as
+// JSONL, timestamped in absolute frames at the render rate. An external synthesizer replays them.
+static FILE* g_eventLog = nullptr;
+static std::uint64_t g_eventFrames = 0;
+static int g_nextEventId = 0;
+
+class EventDumpPlayer : public InstrumentPlayer {
+public:
+    EventDumpPlayer(std::uint8_t bankLo, std::uint8_t bankHi, std::uint8_t patch,
+        DownloadableSound& dls, std::uint32_t sampleRate, std::uint32_t channels,
+        float volume, float pan)
+        : InstrumentPlayer(bankLo, bankHi, patch, dls, sampleRate, channels, volume, pan)
+        , m_id(++g_nextEventId) {
+        std::string dlsName;
+        for (char c : dls.getInfo().getName()) {
+            if ((unsigned char)c >= 0x20 && c != '"' && c != '\\') dlsName += c;
+        }
+        std::fprintf(g_eventLog,
+            "{\"e\":\"inst\",\"id\":%d,\"dls\":\"%s\",\"bankLo\":%u,\"bankHi\":%u,\"patch\":%u,\"vol\":%.6f,\"pan\":%.6f}\n",
+            m_id, dlsName.c_str(), (unsigned)bankLo, (unsigned)bankHi, (unsigned)patch, volume, pan);
+    }
+
+    // The first player of each sub-block advances the shared frame clock; nothing is synthesized.
+    std::uint32_t renderBlock(std::int16_t*, std::uint32_t count, bool mix) noexcept override {
+        if (!mix) g_eventFrames += count / m_channels;
+        return count;
+    }
+
+    void noteOn(std::uint8_t note, std::uint8_t velocity) override {
+        std::fprintf(g_eventLog, "{\"e\":\"on\",\"t\":%llu,\"id\":%d,\"note\":%u,\"vel\":%u}\n",
+            (unsigned long long)g_eventFrames, m_id, (unsigned)note, (unsigned)velocity);
+    }
+    void noteOff(std::uint8_t note, std::uint8_t) override {
+        std::fprintf(g_eventLog, "{\"e\":\"off\",\"t\":%llu,\"id\":%d,\"note\":%u}\n",
+            (unsigned long long)g_eventFrames, m_id, (unsigned)note);
+    }
+    void allNotesOff() override {
+        std::fprintf(g_eventLog, "{\"e\":\"alloff\",\"t\":%llu,\"id\":%d}\n",
+            (unsigned long long)g_eventFrames, m_id);
+    }
+    void channelPressure(std::uint8_t) override {}
+    void polyAftertouch(std::uint8_t, std::uint8_t) override {}
+    void controlChange(an original routine control, float val) override {
+        std::fprintf(g_eventLog, "{\"e\":\"cc\",\"t\":%llu,\"id\":%d,\"cc\":%u,\"val\":%.6f}\n",
+            (unsigned long long)g_eventFrames, m_id, (unsigned)control, val);
+    }
+    void programChange(std::uint8_t) override {}
+    void pitchBend(std::int16_t val) override {
+        std::fprintf(g_eventLog, "{\"e\":\"pb\",\"t\":%llu,\"id\":%d,\"val\":%d}\n",
+            (unsigned long long)g_eventFrames, m_id, (int)val);
+    }
+
+private:
+    int m_id;
+};
+
+static PlayerFactory eventDumpFactory() {
+    return [](std::uint8_t bankLo, std::uint8_t bankHi, std::uint8_t patch,
+        const GUID&, DownloadableSound& dls, std::uint32_t sampleRate, std::uint32_t chans,
+        float vol, float pan) -> std::shared_ptr<InstrumentPlayer> {
+        return std::shared_ptr<EventDumpPlayer>{
+            new EventDumpPlayer(bankLo, bankHi, patch, dls, sampleRate, chans, vol, pan)
+        };
+    };
+}
+
 int main(int argc, char **argv) {
     args::ArgumentParser parser("dmrender renders DirectMusic segments into audio files");
     args::HelpFlag help(parser, "help", "Display this help menu", { 'h', "help" });
     args::ValueFlag<unsigned int> chunkLength(parser, "length", "The length in seconds of the audio to render", { 'l', "length" });
     args::ValueFlag<unsigned int> samplingRate(parser, "sampling rate", "The sampling rate to use", { 's', "sample" });
     args::ValueFlag<unsigned int> numChannels(parser, "channels", "The number of channels to use", { 'c', "channels" });
+    args::ValueFlag<std::string> eventsFile(parser, "events", "Write player events as JSONL instead of synthesizing audio", { 'e', "events" });
     args::Positional<std::string> segmentName(parser, "segment", "The segment to render");
     args::Positional<std::string> outputFile(parser, "output", "The output file");
 
@@ -44,11 +112,19 @@ int main(int argc, char **argv) {
         std::cerr << "dmrender: No input specified." << std::endl;
         return 1;
     }
-   
-    if (!outputFile) {
+
+    bool dumpEvents = static_cast<bool>(eventsFile);
+    if (!dumpEvents && !outputFile) {
         std::cerr << "dmrender: No output file specified" << std::endl;
         return 1;
     }
+    if (dumpEvents) {
+        g_eventLog = std::fopen(args::get(eventsFile).c_str(), "w");
+        if (g_eventLog == nullptr) {
+            std::cerr << "dmrender: Cannot open events file" << std::endl;
+            return 1;
+        }
+    }
 
     int sampleRate = samplingRate ? args::get(samplingRate) : 44100;
     int channels = numChannels ? args::get(numChannels) : 1;
@@ -56,32 +132,35 @@ int main(int argc, char **argv) {
         std::cerr << "dmrender: Invalid number of channels" << std::endl;
         return 1;
     }
-    std::uint64_t length = (chunkLength ? args::get(chunkLength) : 60) * sampleRate;
+    std::uint64_t length = (chunkLength ? args::get(chunkLength) : 60) * sampleRate * channels;
 
-    // Store soundfonts based on their name
-    PlayingContext ctx(sampleRate, channels, an original routine());
+    PlayingContext ctx(sampleRate, channels, dumpEvents ? eventDumpFactory() : an original routine());
     std::cout << "Loading segment...";
     auto segment = ctx.loadSegment(args::get(segmentName));
     std::cout << " done.\nStart playback... ";
     ctx.playSegment(*segment);
     std::cout << " done.\nBegin rendering... \n";
 
-    std::int16_t* buffer = new std::int16_t[length];
+    std::int16_t* buffer = new std::int16_t[length]();
     for (std::uint64_t i = 0; i < length; i += sampleRate) {
         ctx.renderBlock(buffer + i, sampleRate);
         std::cout << "\rProgress: " <<  ceil((i / (float)length) * 100) << "%";
     }
 
-    drwav_data_format format;
-    format.container = drwav_container_riff;
-    format.format = DR_WAVE_FORMAT_PCM;
-    format.channels = channels;
-    format.sampleRate = sampleRate;
-    format.bitsPerSample = 16;
+    if (dumpEvents) {
+        std::fclose(g_eventLog);
+    } else {
+        drwav_data_format format;
+        format.container = drwav_container_riff;
+        format.format = DR_WAVE_FORMAT_PCM;
+        format.channels = channels;
+        format.sampleRate = sampleRate;
+        format.bitsPerSample = 16;
 
-    drwav* dr = drwav_open_file_write(args::get(outputFile).c_str(), &format);
-    drwav_write_pcm_frames(dr, length, buffer);
-    drwav_close(dr);
+        drwav* dr = drwav_open_file_write(args::get(outputFile).c_str(), &format);
+        drwav_write_pcm_frames(dr, length / channels, buffer);
+        drwav_close(dr);
+    }
 
     delete[] buffer;
 
