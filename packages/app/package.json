{
  "name": "@open-northland/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc --build && vite",
    "build": "tsc --build && vite build",
    "preview": "vite preview",
    "shot": "tsc --build && node scripts/shot.mjs",
    "menu-backdrops": "tsc --build && node scripts/menu-backdrops.mjs"
  },
  "dependencies": {
    "@open-northland/audio": "*",
    "@open-northland/data": "*",
    "@open-northland/render": "*",
    "@open-northland/sim": "*"
  },
  "devDependencies": {
    "@open-northland/content-resolver": "*",
    "vite": "^8.1.4"
  }
}
