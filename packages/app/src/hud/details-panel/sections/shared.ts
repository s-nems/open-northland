/** Top padding that vertically centers a font-10 line in a {@link import('../layout.js').ROW_H} row —
 *  the one drawing metric every details-panel section (building, settler, compact) shares. */
export const ROW_TEXT_PAD = 2;
