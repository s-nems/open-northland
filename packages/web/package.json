{
  "name": "@open-northland/web",
  "version": "0.0.0",
  "private": true,
  "description": "OpenNorthland web shell - the browser-hosted installer and game host for opennorthland.org/game",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "tsc --build && node scripts/bundle.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "dependencies": {
    "@open-northland/asset-pipeline": "*",
    "@open-northland/content-resolver": "*",
    "@open-northland/installer": "*",
    "@open-northland/vfs": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
