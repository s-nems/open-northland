{
  "name": "@open-northland/web-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "scripts": {
    "build": "tsc --build",
    "bundle": "esbuild dist/main.js --bundle --platform=node --format=esm --target=node22 --outfile=build/server.mjs",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@open-northland/content-resolver": "*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1"
  }
}
